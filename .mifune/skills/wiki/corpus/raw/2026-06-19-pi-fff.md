# Source snapshot — fff Pi agent extension

- **Source**: https://github.com/dmtrKovalenko/fff (README, "Pi agent extension" section)
- **Captured**: 2026-06-19 (UTC) via `gh api repos/dmtrKovalenko/fff/readme`
- **Captured for**: `wiki/pi-fff.md`

---

## Pi agent extension

### Install

```bash
pi install npm:@ff-labs/pi-fff
```

### Modes

Three operating modes, switchable at runtime with `/fff-mode`:

| Mode                     | What it does                                                                      |
| ------------------------ | --------------------------------------------------------------------------------- |
| `tools-and-ui` (default) | Adds `ffgrep` and `fffind` tools, replaces `@`-mention autocomplete with FFF.     |
| `tools-only`             | Only tool injection. Keeps pi's native editor autocomplete.                       |
| `override`               | Replaces pi's built-in `grep`, `find`, and `multi_grep` with FFF implementations. |

Env vars: `PI_FFF_MODE`, `FFF_FRECENCY_DB`, `FFF_HISTORY_DB`. Flags: `--fff-mode`, `--fff-frecency-db`, `--fff-history-db`.

### Agent-facing tools

- `ffgrep`. Content search. Accepts `path`, `exclude` (comma, space, or array; leading `!` optional), `caseSensitive`, `context`, and cursor pagination. Auto-detects regex, falls back to fuzzy on zero exact matches, rejects `.*`-style wildcard-only patterns up front.
- `fffind`. Path and filename search. Matches the whole repo-relative path, not just the filename. Frecency-aware. The weak-match detector flags scattered fuzzy noise before it floods the agent's context.

### Commands

- `/fff-mode [tools-and-ui | tools-only | override]`. Show or switch the mode.
- `/fff-health`. Picker, frecency, and git integration status.
- `/fff-rescan`. Force a rescan.

Source: `packages/pi-fff/`.

---

## Package facts (verified 2026-06-19)

- `@ff-labs/pi-fff` `latest` dist-tag = `0.9.5`. `pi` field: `{ "extensions": ["./src/index.ts"] }`.
- Depends on `@ff-labs/fff-node: "*"`; resolved to `@ff-labs/fff-node@0.9.5` on 2026-06-19.
- `@ff-labs/fff-node` optionalDependencies provide prebuilt binaries: `@ff-labs/fff-bin-{linux-x64-gnu, linux-x64-musl, linux-arm64-gnu, linux-arm64-musl, darwin-x64, darwin-arm64, win32-x64, win32-arm64}`.
- Verified install + ESM load on the sandbox host (Debian glibc 2.36, x64): resolved `@ff-labs/fff-bin-linux-x64-gnu`, native `libfff_c.so` present (~5.5 MB), `import('@ff-labs/fff-node')` exposes `FileFinder`, `findBinary`, `binaryExists`, etc. No Rust toolchain required.

## What changes (from README)

- Frecency memory: files you open rank higher next time; warm-up from git touch history runs automatically.
- Definition-first hinting: lines that look like code definitions are classified Rust-side.
- Smart-case with auto-fuzzy fallback; zero-match queries retry as fuzzy.
- Git-aware annotations: modified/untracked/staged files are tagged.
