# Tools

Use the local CLI tools available in the sandbox (`git`, `gh`, `docker`, `docker compose`, `make`, `pnpm`, and project scripts) after verifying they are installed in the current environment.
