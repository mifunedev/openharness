# Trajectory input

Close the session with `/retro`: surface ≥1 falsifiable hypothesis with cited session evidence, assign each a verdict (supported / refuted / inconclusive), and promote a supported, sufficiently-confident lesson to `memory/MEMORY.md` as one bullet — without duplicating an existing rule or IDENTITY principle.

## Session observation
A first-round critic council on PR #270 used 3 critics (PM / Implementer / Critic) with implicit, overlapping lenses and shipped a rule that laundered RAO's RL-training results as inference-time guarantees — a HIGH-severity faithfulness gap. A second-round council on the same PR used 5 critics with explicit, DISTINCT lenses (faithfulness, codebase-fit, practical, safety, editor) and surfaced the gap on the first reply.
