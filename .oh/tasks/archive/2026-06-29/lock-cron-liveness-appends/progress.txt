# progress

## Codebase Patterns

- Cron/runtime liveness lines are shared runtime logs; prefer piping whole records into `scripts/locked-append.sh <target>` over raw `>>` appends.

## US-001 — 2026-06-20 03:16 UTC

**Title**: Runtime liveness uses locked append
**Files changed**: scripts/cron-runtime.ts, scripts/__tests__/cron-runtime.test.ts
**Commit**: this commit
**Result**: PASS

### What I did
Routed runtime `log()` and shell-wrapper liveness command generation through `scripts/locked-append.sh`. Updated cron runtime tests to read isolated `.cron.log` output from the locked append path and added command-shape coverage.

### Learnings for future iterations
`CRONS_DIR` is captured at module load in tests, so locked append output should be read from the hoisted test cron directory rather than by spying on `appendFileSync`.

---

## US-002 — 2026-06-20 03:16 UTC

**Title**: Cron prompt liveness guidance uses locked append
**Files changed**: .oh/crons/cleanup-tasks.md, .oh/crons/eval-weekly.md
**Commit**: this commit
**Result**: PASS

### What I did
Replaced raw blocked/success liveness snippets in weekly cron prompts with `scripts/locked-append.sh .oh/crons/.cron.log` pipelines.

### Learnings for future iterations
Prompt examples are executable guidance; guard them with probes just like runtime code.

---

## US-003 — 2026-06-20 03:16 UTC

**Title**: Regression coverage and docs
**Files changed**: .oh/evals/probes/locked-append-critical-path.sh, .oh/crons/README.md, wiki/cron-runtime.md, wiki/README.md, CHANGELOG.md
**Commit**: this commit
**Result**: PASS

### What I did
Extended the locked append probe to cover cron runtime and weekly cron prompts, and documented the shared-log invariant in the cron docs, wiki, and changelog.

### Learnings for future iterations
When wiki impact is required, refresh both the entity page and `wiki/README.md`; the index probe catches stale dates/order.

---

STATUS: COMPLETE
