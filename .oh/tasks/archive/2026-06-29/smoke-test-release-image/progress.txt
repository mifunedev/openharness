# progress

## US-001 — 2026-06-22 00:24 UTC

**Title**: Gate release publish on boot smoke
**Files changed**: `.github/workflows/release.yml`
**Commit**: add106b
**Result**: PASS

### What I did
Split the release workflow's Docker publish path into build, smoke, and push steps. The build now tags the image with both GHCR tags and the `sandbox-${SANDBOX_NAME}` alias consumed by compose, then runs `scripts/sandbox-boot-smoke.sh` with `--no-build` before any push.

### Learnings for future iterations
The release smoke can reuse the existing compose smoke script by matching the compose image alias; no new workflow-specific smoke script is required.

---

## US-002 — 2026-06-22 00:25 UTC

**Title**: Document and verify release gate behavior
**Files changed**: `.github/workflows/release.yml`, `CHANGELOG.md`
**Commit**: add106b
**Result**: PASS

### What I did
Added a workflow comment documenting why the smoke step must stay between build and push. Added a changelog entry for the release hardening and validated the workflow ordering with a targeted parser command.

### Learnings for future iterations
Keep release publish gates explicit in step names and comments because the safety property depends on ordering, not just the presence of a smoke command.

---

STATUS: COMPLETE
