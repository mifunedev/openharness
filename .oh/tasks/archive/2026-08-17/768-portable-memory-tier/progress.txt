Task: 768-portable-memory-tier
Branch: task/768-portable-memory-tier
Base: e4af96d4
Executor: First Mate advisor (inline; plan/critique delegated to sub-agents)

--------------------------------------------------------------------------
PLAN PHASE
--------------------------------------------------------------------------
architect  — solution shape. Chose the resolver fix over a new tracked tier.
             Identified five files hand-rolling main-worktree resolution and
             two existing probes guarding the same class at single call sites.
pm         — task breakdown, acceptance criteria, scope boundaries.
designer   — routing legibility. Argued for a new tracked repo-facts tier.
             Rejected (see prd.md §4), but its wording corrections were
             adopted: the docs named the wrong unit, and that is why the
             defect survived two prior point-fixes.

critic #1 (correctness/blast radius) — 2 findings adopted:
  - /retro SKILL.md:247 discards the resolved $MEM and passes a relative
    literal, so the resolver fix alone misses the most frequent writer. FIXED.
  - shared ledger removes the previous write isolation between worktrees.
    Recorded as a known trade-off in evidence.md §8, follow-up deferred to
    #767's territory.
critic #2 (premise/scope) — 2 findings adopted:
  - the disproof rested on a self-reported date in a file with no history.
    Corroborated independently via .oh/memory/2026-07-19/log.md.
  - the incident narrative overstates the re-derivation: the session already
    knew the answer from provider-private memory (transcript line 518).
    Recorded as a GAP in evidence.md §3 and stated in the PR body.

--------------------------------------------------------------------------
USER STORIES
--------------------------------------------------------------------------
US-001  Anchor oh-path memory to the main worktree                    PASS
        memory matches main from a worktree; crons/evals/tasks/context/
        worktrees still branch-scoped; absolute overrides verbatim;
        precedence untouched; degrades to the old root on git failure,
        garbage output, or no repo, always exit 0. evidence.md §4.
        NOTE: shellcheck is not installed in this container — not run.
        Recorded as a gap, not claimed as a pass.

US-002  Stop seeding a private stub in each worktree                  PASS
        Fixed by US-001 with no change to the seeding logic; only the
        comment naming the wrong unit was corrected. evidence.md §4.

US-003  Correct the docs that name the wrong unit                     PASS
        No 'local-per-instance' remains in memory-protocol.md,
        ensure-memory-file.sh, or retro/SKILL.md. Two-tier model, tier
        table, and IDENTITY.md boundary left intact; no new tier.
        SCOPE DEVIATION: retro/SKILL.md was edited after all — two path
        corrections (lines ~216 and 247), not the log/gate ordering that
        #767 owns. Stated in the PR body so the sibling agent sees it.

US-004  Guard the anchor with one rejection-verified probe            PASS
        One new probe. Fails on exactly (a) and (b) against the unfixed
        script with (c)/(d) at zero; passes against the fixed one; the
        result flips on a script swap inside a fixed worktree. Topology
        and paths only, so CI and a developer worktree agree.
        evidence.md §5. Full suite: 100 pass / 0 regression / 3 skip.

US-005  State the disproof in the PR                                  PASS
        PR body names claim 1 false, cites MEMORY.md:25 and the dated log
        corroboration, reports the narrowed causal claim from critic #2,
        states what is NOT fixed, and records both rejected alternatives.

US-006  Ship the task artifacts and the changelog entry               PASS
        findings.md, prd.md, prd.json, progress.txt, evidence.md staged
        with git add -f. CHANGELOG entry first under ### Fixed.

--------------------------------------------------------------------------
OUT OF SCOPE (stated, not silently dropped)
--------------------------------------------------------------------------
- Cross-clone / cross-provider portability. MEMORY.md stays gitignored.
  The issue's second question is left open for the maintainer.
- A new tracked repo-facts tier. Rejected in prd.md §4.
- Consolidating the five hand-rolled main-worktree resolvers onto oh-path.
- Lock-guarding the MEMORY.md append (#767's file).
- Ledger growth/pruning: 79 lessons and unbounded. Named, not addressed.

STATUS: COMPLETE
