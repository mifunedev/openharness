# Critique — firstmate-executor

Generated 2026-08-12 15:22 UTC; reviews `prd.md` post-/prd, pre-/ralph. Round 1.

## Critic A — Implementer lens

CRITIC_A — IMPLEMENTER LENS
[SEVERITY: H] [STORY: US-002] [prd.json declares "priority = dependency order" (§6 table header) but US-002 (firstmate.sh, priority 2) has an AC "Renders the session prompt from the US-003 template" — a hard dependency on US-003 (priority 3), which runs later | [EVIDENCE: .oh/tasks/firstmate-executor/prd.json lines 32-51 vs 52-72; prd.md §6 "priority = dependency order"] | [RECOMMENDATION: swap priorities so the template/renderer (US-003) lands before firstmate.sh (US-002), or split firstmate.sh's rendering call behind a stub interface it can land without the real template]
[SEVERITY: H] [STORY: US-006] [autopilot's own executor validation gate `case "$EXECUTOR" in ship-spec|delegate-advisor|ralph) ;; *) echo "ERROR: invalid AUTOPILOT_EXECUTOR=$EXECUTOR"...` is never named in the AC list for adding the firstmate arm — without editing this exact line, `EXECUTOR=firstmate` fails hard even after the case-arm and defer logic are added | [EVIDENCE: .claude/skills/autopilot/SKILL.md:204 `case "$EXECUTOR" in ship-spec|delegate-advisor|ralph) ;; *) echo "ERROR..."`; US-006 ACs in prd.json only mention "gains an EXECUTOR=firstmate arm... mirroring delegate-advisor"] | [RECOMMENDATION: add an explicit AC: "the `case "$EXECUTOR" in ship-spec|delegate-advisor|ralph)` validation list gains `firstmate`"]
[SEVERITY: H] [STORY: *] [AGENTS.md/CLAUDE.md are not two independent files — CLAUDE.md is a symlink to AGENTS.md (`CLAUDE.md -> AGENTS.md`) — but three stories (US-004, US-005 implicitly, US-008) phrase the AC as "AGENTS.md and CLAUDE.md are updated in lockstep... diff produces zero output," which invites an implementer to edit CLAUDE.md as a second target; any tool that does atomic replace-on-write (temp+rename) instead of in-place truncate will silently sever the symlink and permanently break future byte-identity | [EVIDENCE: `ls -la CLAUDE.md` → `CLAUDE.md -> AGENTS.md`; CLAUDE.md itself states "CLAUDE.md is a provider-compatibility alias... If both are real files and conflict, stop"] | [RECOMMENDATION: state explicitly in every touching AC "edit AGENTS.md only; CLAUDE.md is a symlink and must not be independently written," and add a `readlink CLAUDE.md` check to the new probe]
[SEVERITY: H] [STORY: US-001,US-002,US-009] [The design's core rationale — "no `agent get` / has-session oracle" — was verified WRONG live against the pinned herdr 0.7.4 in this sandbox: `herdr agent get <name>` exists, returns clean `agent_info` (exit 0) for a live target and clean `agent_not_found` (exit 1) for an absent one; it functions as a has-session oracle while the process is alive. Multiple ACs hard-forbid it ("The file contains none of: ... `herdr agent get`") based on this unverified premise, forcing the more complex/TOCTOU-prone `mkdir` lock + `agent list | jq` path | [EVIDENCE: live test — `herdr agent start critic-test-probe2 ...` then `herdr agent get critic-test-probe2` → `{"result":{"agent":{...}},"type":"agent_info"}` exit 0; prd.md §5 "Gaps: ... no `agent get` / has-session oracle"] | [RECOMMENDATION: re-verify the actual failure mode (likely: pane/agent disappears once the underlying process exits, not "command absent") and either narrow the forbidden-command rationale or drop the ban if `agent get` is safe while the target is alive]
[SEVERITY: M] [STORY: US-001] [Technical Considerations cites "ralph.sh:428-431's foreground tee" as prior art for the foreground launch branch, but actual lines 428-431 (`if ! command -v tmux ...; then ... exec bash "$SCRIPT_PATH" --loop ...; fi`) have NO `| tee` at all — ralph's no-tmux fallback does not log to a file today | [EVIDENCE: .oh/scripts/ralph.sh lines 428-431 read verbatim above; prd.md §5 table row "Launch (foreground) | inline exec, still tee'd — matches ralph.sh:428-431"] | [RECOMMENDATION: correct the citation before implementers copy it verbatim; note explicitly that the foreground `| tee` is NEW behavior beyond ralph's parity, not a match]
[SEVERITY: M] [STORY: US-006] [autopilot-executor-toggle.sh pins the literal substring `[--executor=ship-spec|delegate-advisor|ralph]` in the argument-hint via `grep -Fq`; the natural, expected edit — extending the bracket to advertise firstmate (`...|ralph|firstmate]`) — breaks that exact fixed-string match and fails the very probe US-006's AC requires to stay green | [EVIDENCE: .oh/evals/probes/autopilot-executor-toggle.sh:32 `grep -F 'argument-hint:' "$SKILL" | grep -Fq '[--executor=ship-spec|delegate-advisor|ralph]'`] | [RECOMMENDATION: either leave the argument-hint bracket untouched and document firstmate only in prose, or update the probe's pinned literal in the same PR and say so explicitly in the AC]
[SEVERITY: M] [STORY: US-003] ["Step-order equivalence" between the new session-prompt.md and implement.yml/pr.yml is unverifiable as scoped: both YAMLs are ~8-line generic `query:` directives with a single one-time `/compact`, no "quality checks" step, no `Submitted-by:` mention, and no bounded-retry/BLOCKED concept — yet the template must additionally encode per-story `/compact`, max-3 bounded AUDIT-FAIL, and a dual-channel terminal contract that the source YAMLs don't contain, and a NEW bash probe (US-009) must mechanically assert this "equivalence" between free-form markdown and short YAML lists | [EVIDENCE: .oh/prompts/advisor/implement.yml and pr.yml full contents (8-item `query:` lists, one `/compact`); prd.json US-003 AC list vs US-009 AC "session-prompt template is step-order-equivalent to the advisor prompt pack"] | [RECOMMENDATION: define the equivalence check as a concrete, literal-grep-able mapping (e.g., an ordered keyword list both files must contain in the same relative order) before US-009 is built, not left to implementer interpretation]
[SEVERITY: M] [STORY: US-004] [`.oh/scripts/lib/session-runner.sh` (US-001) is described as shared load-bearing infra reusable by "any executor," yet only `firstmate` (skill) and `.oh/scripts/firstmate.sh` are added to `.claude/protected-paths.txt` in US-004's AC — the library itself is omitted from the registration despite the file's own stated rule ("ship a new orchestrator-load-bearing skill or script → ADD it here in the same PR") | [EVIDENCE: .claude/protected-paths.txt header rule; prd.json US-004 AC ".claude/protected-paths.txt gains the bare skill name `firstmate`... and the path `.oh/scripts/firstmate.sh`" — no mention of session-runner.sh; prd.md §5 "any executor can launch a long-lived agent session through the same ladder"] | [RECOMMENDATION: add `.oh/scripts/lib/session-runner.sh` to protected-paths.txt in the same PR]
[SEVERITY: M] [STORY: US-010] [FR-16/allow_nested=false states "the build session must never launch herdr," but US-010's herdr-mode smoke run necessarily calls `herdr agent start` from whatever session is executing this PRD's own build — if that outer session is itself herdr-hosted (the standard workflow per AGENTS.md Lifecycle: attach → run `herdr` as the primary interactive workspace), this is exactly the nested-herdr case the design forbids, with no documented isolation mechanism (bare shell, CI runner, SSH) to avoid it | [EVIDENCE: prd.md FR-16 "the build session must never launch herdr (allow_nested=false)"; AGENTS.md Lifecycle §5 "Start the primary interactive workspace immediately after attaching: `herdr`"; prd.md §11 dogfood note "this build still runs under ralph — the P10 smoke run is firstmate proving itself"] | [RECOMMENDATION: specify explicitly which shell/session context P10 must run in to avoid nesting herdr inside herdr, and add a preflight check that refuses to run the herdr-mode smoke test from inside an active herdr pane]
[SEVERITY: L] [STORY: US-010] [The "tmux-mode run with herdr masked off PATH" AC doesn't specify the masking mechanism; if implemented via renaming/moving the shared `/usr/local/bin/herdr` binary rather than a subshell-scoped `PATH=` override, it is a hidden destructive/disruptive operation against a shared sandbox tool other concurrent sessions may depend on, with no explicit restore-on-failure gate stated | [EVIDENCE: prd.json US-010 AC "A tmux-mode run with `herdr` masked off PATH"; no mechanism specified] | [RECOMMENDATION: state explicitly "mask via a scoped `PATH` override in the subshell that launches firstmate.sh, never by moving/renaming the herdr binary"]
[SEVERITY: L] [STORY: US-001] [No existing `.oh/scripts/lib/*.sh` sourceable-library precedent exists in this repo (only `maintenance/` and `__tests__/` subdirs exist under `.oh/scripts/` today); if session-runner.sh follows the repo's own convention of `set -euo pipefail` at file scope (as ralph.sh does at line 56), sourcing it into firstmate.sh will silently mutate the caller's shell-option state for the remainder of its execution — a pattern `shellcheck` does not flag | [EVIDENCE: `find .oh/scripts -maxdepth 1 -type d` → only `maintenance`, `__tests__`; .oh/scripts/ralph.sh:56 `set -euo pipefail`] | [RECOMMENDATION: explicitly scope `set -euo pipefail` decisions in session-runner.sh's functions rather than file-top-level, or document that it is caller-must-set-strict-mode-itself, and add a vitest case exercising it sourced into a caller with different option state]
[SEVERITY: L] [STORY: US-003,US-008] [Both stories bundle 2+ independently-verifiable deliverables under one AC list and one "priority": US-003 = governance-sensitive prompt content AND an unlocated "renderer" (implied code, no file path given, no dedicated test); US-008 = protected-path norms rewrite AND AGENTS/CLAUDE lockstep AND CHANGELOG AND a full new ~900-word schema-conformant wiki entry with raw snapshot + README regen — each is really 2 stories merged, raising blast radius if one half fails | [EVIDENCE: prd.json US-003 AC "A renderer substitutes <slug>, branch, and issue number into the template" has no file path or test AC; US-008 ACs span sandbox-processes.md rewrite + AGENTS/CLAUDE + CHANGELOG + 8 distinct WIKI: sub-ACs] | [RECOMMENDATION: split the renderer into its own testable unit (likely inside US-002's firstmate.sh, stated explicitly), and consider splitting US-008 into a norms-doc story and a wiki-entry story so a wiki schema failure doesn't block the norms fix or vice versa]

## Critic B — User lens

CRITIC_B — USER LENS
[SEVERITY: H] [STORY: US-006] [FINDING] Autopilot pass-through lets an unattended, overnight "lights-out" run (README's own framing) launch a long-lived firstmate session with no total session budget — only per-story AUDIT-FAIL retries are bounded (max 3); there is no cap on total stories, wall-clock time, or token spend, unlike ralph's explicit 50-iteration ceiling. | [EVIDENCE: PRD §8 FR-11/FR-12; §15 Open Question 2 admits the herdr `--timeout` value "is not yet chosen"; README.md "agents build on a schedule... while you're away"] | [RECOMMENDATION] Add a session-wide iteration/time/cost cap (or explicit Non-Goal deferring it) before wiring `EXECUTOR=firstmate` into autopilot's unattended path; do not let the first live use of an unbounded executor be an unsupervised overnight run.
[SEVERITY: H] [STORY: *] [FINDING] Audience misalignment: `.oh/context/USER.md`/README.md describe a single-developer, single-project, "concise and action-oriented" harness ("One project, one sandbox"), yet this PRD permanently registers a third executor, a new skill, a new runner ladder, two new probes, and a ~900-word wiki entry as protected/indefinite infrastructure — while the actual payoff (flipping the default, ever sunsetting ralph) is explicitly deferred with no scheduled bar ("N green runs" not yet chosen). | [EVIDENCE: `.oh/context/USER.md` "Be concise and action-oriented"; README.md "One project, one sandbox"; PRD §9 Non-Goals ("Flipping the default executor" and "Removing... ralph.sh" both out of scope); §15 Open Question 1] | [RECOMMENDATION] Confirm with the operator that carrying three indefinitely-maintained, protected-path executors is worth it for one project, or scope this to a smaller spike that doesn't commit `firstmate` to protected status until the default-flip decision is actually made.
[SEVERITY: M] [STORY: *] [FINDING] Premature optimization: the stated pain point ("ralph re-sends prompt.md up to 50 times") is asserted rhetorically, not measured against this operator's real builds, before committing to permanent ladder/wiki/probe infrastructure. | [EVIDENCE: PRD §1 Context cites only "the latest round of models does well by linking taskgraph and prd.json" with no cost/time/reliability data on current ralph performance; §12 Success Metrics measure only reaching `STATUS: COMPLETE`, not firstmate-vs-ralph comparison] | [RECOMMENDATION] Add a success metric comparing firstmate to ralph on a real (non-throwaway) task — token cost, wall-clock, or story-completion reliability — before the default-flip follow-up is scheduled.
[SEVERITY: M] [STORY: US-001, US-002] [FINDING] Missing rollback/escape hatch: `runner_teardown` is defined in the library but no story exposes an operator-facing abort/kill for a stuck long-lived session, and stale-lock recovery after a hard crash (`kill -9` leaving `/tmp/firstmate-<slug>.lock`) is untested. | [EVIDENCE: PRD §5 "Recovery" row covers only automated death-without-sentinel and mid-run herdr loss; US-001's "≥3 degrade cases" test runner *detection*, not lock staleness; US-002 AC has no manual-abort case] | [RECOMMENDATION] Add an AC/CLI surface (e.g. `firstmate.sh --kill <slug>`) that clears the lock, kills the herdr agent/tmux session, and records the outcome in `progress.txt`; add a stale-lock-after-crash test.
[SEVERITY: M] [STORY: US-010] [FINDING] The live two-mode smoke test spins up real herdr agents and tmux sessions plus log files but has no stated teardown step — given herdr agents "may never restart the server" and no `agent get` oracle exists, an orphaned smoke-test agent/session could persist indefinitely after evidence.md is captured. | [EVIDENCE: PRD §6 P10 row and US-010 ACs list creation/verification steps but no cleanup step for the throwaway slug, its lock file, or its herdr/tmux session] | [RECOMMENDATION] Add an explicit teardown AC to US-010 (call `runner_teardown`, delete the throwaway task folder, confirm no lingering `agent list`/`tmux ls` entry) before the PR is marked ready.
[SEVERITY: M] [STORY: US-003, US-008] [FINDING] Naming collision risk: "First Mate" already names an established supervisory-delegation role/charter (`.oh/agents/advisor.md`, `.oh/context/rules/first-mate.md`, referenced in CLAUDE.md's Skills table), and this PRD overloads the same name for a build-executor session (`firstmate-<slug>`) derived from that role's own prompt pack — future readers (and the new wiki entry) may conflate "First Mate the role" with "firstmate the executor." | [EVIDENCE: PRD §1 derives session-prompt from `.oh/prompts/advisor/*`; §5/§6 introduce `firstmate-<slug>` naming; CLAUDE.md documents "First Mate role charter" as a separate on-demand doc] | [RECOMMENDATION] Either confirm the overload is intentional and add a disambiguation note to `.oh/skills/firstmate/SKILL.md` and the wiki entry, or rename the executor to avoid the collision.
[SEVERITY: L] [STORY: US-002] [FINDING] No AC addresses cross-executor lock collision — what happens if `--executor=firstmate` is invoked on a task folder already mid-flight under `ralph` (not yet `STATUS: COMPLETE`)? The sentinel short-circuit only covers the already-complete case. | [EVIDENCE: US-002 ACs cover sentinel-complete short-circuit and the firstmate-specific `mkdir` lock, but nothing checks for a concurrently-running ralph process on the same slug] | [RECOMMENDATION] Add a Non-Goal explicitly deferring cross-executor collision handling, or add a guard/AC that detects an in-progress task under a different executor and refuses to launch.
[SEVERITY: L] [STORY: US-001, US-003] [FINDING] Interaction between `allow_nested=false` and `/delegate`'s own `--runner herdr|tmux|bg` ladder is unstated: if `/delegate` runs inside a firstmate build session and its own ladder tries herdr, does it hard-error (per the "unavailable override = hard error" rule) or silently fall back? | [EVIDENCE: PRD §5 Nesting row states "inner fan-out is `/delegate` only" but doesn't say how `/delegate`'s runner selection is constrained inside a nested firstmate session] | [RECOMMENDATION] Add an AC clarifying that `/delegate` invoked inside a firstmate session must not select herdr (env-based signal or explicit `--runner tmux` default), and a probe asserting it.
[SEVERITY: L] [STORY: *] [FINDING] No session-wide budget/timeout is set for the herdr watch beyond "value not yet chosen" (Open Question 2), and there is no Non-Goal bounding total session length — this is a genuine scope gap, not just an open question, given the session is explicitly "long-lived." | [EVIDENCE: PRD §5 Watch row fixes command shape (`--timeout <ms>`) but not the value; §15 defers resolution to empirical P10 smoke run] | [RECOMMENDATION] Either add a Non-Goal ("session-wide timeout tuning is out of scope, ships with a conservative default") or make timeout selection an explicit AC of US-010 rather than an open question resolved informally.
[SEVERITY: L] [STORY: *] [FINDING] No Non-Goal addresses herdr version drift (pinned 0.7.4) — CLI flag/output changes in a future herdr release could silently break the `agent list | jq` parsing the idempotency check depends on. | [EVIDENCE: PRD §11 names herdr 0.7.4 gaps as design drivers but no FR/Non-Goal pins the version assumption or states what happens on drift] | [RECOMMENDATION] Add a Non-Goal ("compatibility beyond herdr 0.7.4 is out of scope") or a probe that checks `herdr --version` before trusting parsed CLI output.
[SEVERITY: L] [STORY: *] [FINDING] No lifecycle is defined for `/tmp/firstmate-*.log` files or the US-010 throwaway task folder — neither is mentioned in Non-Goals, and it's unclear whether the protected `.oh/crons/cleanup-tasks.md` cron correctly sweeps a firstmate-produced task folder. | [EVIDENCE: PRD §9 Non-Goals (10 items) omits log/task-folder retention; US-010 creates a "throwaway 2-story task folder" with no deletion step] | [RECOMMENDATION] Add a Non-Goal stating log/task-folder retention defers to the existing cleanup-tasks cron, and explicitly verify (not assume) that cron correctly matches firstmate-produced folders.
[SEVERITY: L] [STORY: *] [FINDING] Protected-path touches confirmed non-violating: `ralph.sh` (zero-diff, unchanged), `sandbox-processes.md` (add-never-replace), and `ship-spec` skill (additive `--executor=firstmate` arm) are all edited but not deleted/deprecated, and `firstmate`/`firstmate.sh` are correctly staged as new protected-paths.txt entries in the same PR per that file's own rule — no override note is required because no removal is proposed. | [EVIDENCE: `.claude/protected-paths.txt` lines 21 (`ralph`), 25 (`ship-spec`), 58 (`sandbox-processes.md`); PRD §9 Non-Goals explicitly rules out removing/deprecating ralph] | [RECOMMENDATION] No action needed on this finding itself, but `/spec critique` (PRD §14 already requires it) must verify each "additive only" claim against the actual diff at build time rather than trusting the PRD's self-description.

## Synthesis (round 1)

- **High-severity findings**: 6 (A: 4, B: 2). Orchestrator verification: both repo-facts H claims re-verified live — `CLAUDE.md -> AGENTS.md` symlink confirmed via `ls -la`; `herdr agent get` confirmed present in herdr 0.7.4 (`agent_not_found` exit 1 on absent target, clean oracle semantics). One H (B's audience-misalignment finding, `[STORY: *]`) is **mitigated by recorded operator decision**: the Captain locked opt-in rollout, indefinite ralph retention, and the deferred default-flip via AskUserQuestion (recorded in prd.md § Captain decisions), so carrying the third executor is an explicit operator choice, not an unexamined default. The remaining **5 H findings are unmitigated** and require PRD revision.
- **Medium-severity findings**: 9
- **Low-severity findings**: 9 (one is an informational protected-paths confirmation, not a defect)
- **Recommendation (round 1)**: HALT → `/approve` DENIED; routed back to `/spec plan` for revision.

---

# Round 2 — revised PRD (revision 1 applied all round-1 H + M/L folds)

## Critic A — Implementer lens (round 2)

RESOLVED: all four round-1 CRITIC_A highs verified mitigated at AC level (priority swap; autopilot validation-list AC; CLAUDE.md-symlink rewording + readlink probe AC; agent-get premise corrected with mkdir lock retained as launch-claim guard).

NEW FINDINGS:
[SEVERITY: H] [STORY: US-001,US-003,US-004,US-010] runner_teardown's herdr mechanism named a nonexistent verb — herdr 0.7.4 has NO `agent stop`/`agent kill`; the real primitive is `herdr pane close <pane_id>` (live-verified: exit 0 ok; `agent get` afterwards → `agent_not_found` exit 1). | US-004 AC said "herdr agent list → stop that agent"; no AC pinned the real verb | Pin `pane close` in US-001/US-004/US-009.
[SEVERITY: M] [STORY: US-002] US-002 carried an AC whose evidence (renderer test file) does not exist until US-003 — structurally unverifiable at US-002's completion boundary. | Move the bullet to US-003.
[SEVERITY: M] [STORY: US-008,US-010] Post-US-010 wiki confirmation pass was dangling — no story owned executing it. | Add explicit US-010 AC with mechanical grep gate.

## Critic B — User lens (round 2)

RESOLVED: both round-1 CRITIC_B highs verified mitigated (FIRSTMATE_TIMEOUT_MS session budget with US-006 inheritance AC; §2 Captain decision 5 recording explicit operator acceptance of the third executor).

NEW FINDINGS:
[SEVERITY: H] [STORY: *] Q0 premise defect: herdr panes execute on the HOST (live-verified: pane reported legion-laptop/ryaneggz/no dockerenv vs caller container 34263ba23a57/sandbox/dockerenv) — a herdr-mode build would violate the sandbox boundary; §14 routed to build with no gate on resolving this. | Promote Q0 to a plan-blocking premise gate.
[SEVERITY: H] [STORY: US-010] Single throwaway slug reused for both smoke modes + US-003's sentinel short-circuit ⇒ whichever mode runs second short-circuits to a no-op, trivially "passing" without exercising that runner. | Require two distinct slugs.
[SEVERITY: M] [STORY: US-010] Smoke-run commits would land permanently in the PR branch history — no isolation/squash step. | Isolate in a disposable never-merged worktree/branch.

**Round-2 verdict**: 3 new H → second revision required. Orchestrator independently re-verified Q0 (container vs host fingerprints) and the pane-close teardown semantics before revising.

---

# Round 3 — twice-revised PRD (revision 2 applied Q0 context-gate, two-slug smoke, commit isolation, pane-close teardown, renderer ownership, wiki pass)

## Critic A — Implementer lens (round 3)

RESOLVED: all three round-2 tracked items verified mitigated at AC level (pane-close pinned across US-001/US-004/US-009 + FR-9c; Q0 execution-context fingerprint gate as third detect conjunct, independently re-verified live; US-010 rebuilt with two slugs / conditional herdr arm / commit isolation / teardown / mechanically-gated wiki pass).

NEW FINDING:
[SEVERITY: H] [STORY: US-006/US-001/FR-6a] US-006 claimed out-of-range FIRSTMATE_TIMEOUT_MS is "rejected", but the only specified mechanism was bare `${FIRSTMATE_TIMEOUT_MS:-14400000}`, which passes 0/negative through — no validation AC existed anywhere; a stray `=0` makes the ceiling 0 or (if herdr treats `--timeout 0` as no-timeout) unbounded. | Add a validating helper + rejection unit tests.
VERDICT: NOT-CONVERGED

## Critic B — User lens (round 3)

RESOLVED: all three round-2 tracked items verified mitigated at AC level (context gate as real third conjunct with hard-error override; two distinct slugs with short-circuit rationale; smoke-commit isolation with machine-checked clean-history assertion + teardown).

NEW FINDING:
[SEVERITY: H] [STORY: US-001/FR-2a] The permanent runner_detect probe pane itself launches `herdr agent start` on every future firstmate invocation, and the HERDR_ENV/HERDR_PANE_ID nesting guard was scoped only to US-010's one-off smoke launch — invoked from inside a herdr pane (the documented default workflow), the gate either hard-fails every launch or silently nests, untested either way. | Add the nesting guard to runner_detect itself (US-001) with a unit test.
VERDICT: NOT-CONVERGED

---

# Round 3 remediation + targeted re-verification

Both round-3 highs were remediated surgically by the orchestrator (revision 3):
- **A's finding** → shared validating `resolve_timeout_ms` helper as the ONLY budget source for all three consumers (POSIX integer > 0; 0/negative/non-numeric/empty → default 14400000 + logged rejection; herdr `--timeout 0` unreachable by construction); unit tests for unset/valid/0/-1/abc; §5 rows, US-001 ACs, FR-6a, US-006 AC, US-009 probe AC all updated in both files.
- **B's finding** → NESTING GUARD as the zeroth check of `runner_detect` (before any probe pane): `HERDR_ENV=1`/`HERDR_PANE_ID` markers ⇒ skip probe entirely, herdr ineligible, degrade to tmux, reason logged; vitest case injects HERDR_ENV=1 and asserts no `herdr agent start` is invoked; container-boundary caveat recorded (markers don't cross a container hop — the fingerprint gate remains the backstop in this deployment); §5 Detection row, US-001 AC, FR-2a, US-009 probe assertion updated in both files.

Each round-3 critic re-verified its own finding against the revised text:

- Critic A verdict: **MITIGATED** — "Every consumer (herdr `--timeout`, tmux/foreground poll ceiling, autopilot pass-through) is now pinned to route exclusively through `resolve_timeout_ms` (US-001 ACs, FR-6a), which is unit-tested against 0/-1/abc/unset/valid and asserted by the US-009 probe, closing the prior gap where `${FIRSTMATE_TIMEOUT_MS:-14400000}` would silently pass non-positive values through unrejected."
- Critic B verdict: **MITIGATED** (after one drift fix) — first re-verification caught that the US-009 nesting-guard probe assertion had been added to prd.json but not prd.md; the orchestrator fixed the drift and Critic B confirmed: "prd.md's US-009 'Execution-context gate assertion' AC now explicitly requires session-runner-ladder.sh to assert the nesting guard is present in runner_detect, matching prd.json — the two artifacts agree and the regression-prevention gap I flagged is closed."

## Synthesis

- **High-severity findings**: 11 total across three rounds; **11 mitigated at AC level** (final two re-verified by their own finding critics, recorded above).
- **Medium-severity findings**: 12 across rounds; all folded into ACs or explicitly deferred via Non-Goals.
- **Low-severity findings**: 9; folded or deferred via Non-Goals.
- **Recommendation**: PROCEED — with the §2 decision-6 orchestrator amendment (execution-context gate / herdr refused out-of-environment) explicitly flagged for Captain review at the PR gate.
