# progress

## US-001 — 2026-07-02 06:38 UTC

**Title**: Fix host prerequisites to include make/build-essential
**Files changed**: .oh/docs/installation.md, README.md, .oh/docs/quickstart.md, .oh/docs/intro.md
**Result**: PASS

### What I did
Made installation.md the canonical Prerequisites home — added a make/build-essential row (with why + per-OS install) to the table. README:7/38, quickstart:7/11, intro:17 now list make and link to the canonical Prerequisites section. Fixed the two "Docker with Compose and Git" prose lines in installation.md too.

### Learnings for future iterations
- The grep gate must use a DIRECTORY arg (grep -rn "Docker + Git" README.md .oh/docs/) — a .oh/docs/*.md glob silently skips harnesses/ and integrations/. Gate now returns 0.

---
## US-002 — 2026-07-02 06:39 UTC

**Title**: Add harness.yaml git: key reference; collapse README Configure prose to a pointer
**Files changed**: .oh/docs/quickstart.md, README.md
**Result**: PASS

### What I did
Added a git: section (user_name/user_email) to the quickstart harness.yaml example, two rows to the config-key table (git.user_name, git.user_email), and a note that crons/autopilot/slack/compose sections exist. Shrank README "Configure (optional)" prose to a 2-sentence pointer to the quickstart table; kept the secrets-stay-out-of-git warning.

### Learnings for future iterations
- README Configure was PROSE, not a table (critic caught the gap-audit imprecision). No table removal needed.
- Bash guard blocks any command whose text contains the literal dotenv substring; keep it out of commit messages / heredocs.

---
## US-003 — 2026-07-02 06:41 UTC

**Title**: Document the private-origin + upstream clone-and-own pattern (retain Railway/curl)
**Files changed**: .oh/docs/installation.md, README.md
**Result**: PASS

### What I did
Enriched installation.md's "Clone-and-own" into the full validated pattern (clone → make sandbox/shell → inside-sandbox gh auth SSH → gh repo create --private → SSH origin + upstream remotes → push), with an HTTPS/installer fallback note. Added a "Recommended: clone-and-own" lead callout at the top of the README Install section and enriched Option C with the same commands + a deep link. Renamed the installation.md heading to a clean-anchor form.

### Learnings for future iterations
- Did NOT reletter Options — added a lead callout instead, so README:165 "Option B above" (Fork-and-clone) stays valid. Gates: Railway button (L24) + curl (L31) intact; Options A/B/C coherent.
- installation.md anchor = #clone-and-own-private-origin-and-upstream-recommended (GitHub slug of the heading); reused by the README link and will be reused by github.md (US-004).

---
## US-004 — 2026-07-02 06:42 UTC

**Title**: Document GitHub SSH auth path in integrations/github.md
**Files changed**: .oh/docs/integrations/github.md
**Result**: PASS

### What I did
Added an "SSH authentication (recommended for private origin + upstream)" section: (A) the interactive gh auth login SSH path (generate key, paste token, needed scopes incl. admin:public_key) and (B) the entrypoint auto-keygen behavior (ed25519 at ~/.ssh, uploads pubkey when GH_TOKEN has admin:public_key; idempotent; cites entrypoint.sh:275-309). Reframed the intro line to point SSH users at the clone-and-own flow; preserved the existing HTTPS/PAT + PR + persistence content.

### Learnings for future iterations
- Cross-link anchor reused from US-003: ../installation.md#clone-and-own-private-origin-and-upstream-recommended.

---
## US-005 — 2026-07-02 06:43 UTC

**Title**: Prepend a Confirmed-setup runbook to integrations/debugmcp.md
**Files changed**: .oh/docs/integrations/debugmcp.md
**Result**: PASS

### What I did
Inserted a "## Confirmed setup (runbook)" section before "## Feasibility": (1) codex login --device-auth, (2) install microsoft/DebugMCP (ozzafar.debugmcpextension v2.0.1) on the VS Code machine, (3) attach-to-container (Lifecycle B/C), (4) MCP binds :3001, Codex/Claude pre-registered, (5) verify. The full feasibility analysis is preserved unchanged below.

### Learnings for future iterations
- Extension ID is ozzafar.debugmcpextension (Marketplace name "microsoft/DebugMCP"); operator-attach path is the CONFIRMED one; headless container-side still open.

---
## US-006 — 2026-07-02 06:44 UTC

**Title**: Verify and correct the Claude auth command
**Files changed**: .oh/docs/harnesses/claude-code.md, .oh/docs/quickstart.md
**Result**: PASS

### What I did
Verified live in the running openharness container (docker exec -u sandbox): Claude Code v2.1.198 exposes `claude auth login`, `claude auth status`, `claude auth logout` (no bare `claude login`). Rewrote the claude-code.md Authentication section to use `claude auth login` + `claude auth status` (noting bare `claude` also triggers OAuth), and updated the quickstart harness-list line to match.

### Learnings for future iterations
- Live verification confirmed the command; no `> Unverified` admonition needed. The sandbox container name is `openharness`.

---
## US-007 — 2026-07-02 06:47 UTC

**Title**: Align Slack/gateway docs and add a read-only Run-and-verify subsection (sandbox-only)
**Files changed**: .oh/docs/integrations/slack.md, .oh/docs/harnesses/hermes.md
**Result**: PASS

### What I did
Reconciled hermes.md's "independent surfaces" wording to gateway.sh ground truth: both Pi and Hermes Slack gateways run under the SAME .oh/scripts/gateway.sh lifecycle in sibling sessions (client-slack-pi = pi-messenger-bridge; client-slack-hermes = `hermes gateway run`), each with its OWN Slack app. Added a "Run and verify (read-only)" subsection to BOTH docs (sandbox-only run commands, `tmux attach -r`, detach Ctrl-b d, log tails). Converted slack.md's observation-only `tmux attach -t client-slack-pi` occurrences to `-r`. Cross-linked the two.

### Learnings for future iterations
- Live-verified: `gateway status` prints "· client-slack-pi  stopped  (gateway pi)" / hermes sibling; both stopped now. `tmux attach -r` is valid (tmux 3.3a) — returns "can't find session" when stopped, not "unknown flag". `gateway`/`make gateway` are sandbox-only (need pi/hermes on PATH).
- Left the pre-existing stale `.oh/context/rules/sandbox-processes.md` links (now .oh/skills/t3/references/) untouched — out of this story's scope.

---
## US-008 — 2026-07-02 06:51 UTC

**Title**: Remove unused ryaneggz/mifune repo references (6 named sites only)
**Files changed**: README.md, AGENTS.md, .oh/docs/intro.md, .oh/docs/contributing.md, .oh/docs/harnesses/overview.md
**Result**: PASS

### What I did
Edited only the 6 named sites (no global find/replace): README two spots now describe multiple-harnesses + pi-messenger-bridge; intro bullet re-pointed to slack; contributing "Installing a harness pack" rewritten as "Multi-agent messaging (Slack)" describing the REAL bridge mechanism (npm into .pi/bridge, --extension, gateway.sh) instead of the wrong git-clone-into-workspace pack contract; overview drops the pack clause; AGENTS drops the legacy-pack sentence. Left the legitimate ryaneggz/pi-messenger-bridge fork pin (different repo) intact.

### Learnings for future iterations
- Preserve-check held: CHANGELOG.md 14 occurrences and archive 64 occurrences both UNCHANGED. Removal grep over README/AGENTS/.oh/docs = 0.

---
