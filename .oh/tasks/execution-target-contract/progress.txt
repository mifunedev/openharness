# progress

## Codebase Patterns

- **The task folder is tracked but gitignored.** `.oh/tasks/<slug>/` matches a `.gitignore` rule,
  yet `prd.md` / `prd.json` / `progress.txt` are already tracked (added with `-f` when scaffolded).
  A plain `git add .oh/tasks/...` fails with "paths are ignored" AND aborts the whole `git add`
  invocation — including the non-ignored paths listed alongside it. Stage the task folder with
  `git add -f` in a **separate** command from the rest of the story's files.
- **Quality-check script names.** From the repo root: `pnpm run typecheck` (no hyphen; delegates to
  `npm --prefix .oh/cli run typecheck` → `tsc --noEmit`) and `pnpm test` (vitest, 39 files / 481
  tests, ~5s). `pnpm run type-check` does not exist. Root `lint` is a no-op.
- **New RFCs must be indexed in TWO places.** `.oh/docs/rfcs/README.md` (the `## Index` table) and
  `.oh/docs/README.md` (the flat RFC bullet list, ~line 74-78). CHANGELOG #686 records the repo
  paying for a missed index entry, so treat both rows as part of "create an RFC", not scope creep.
- **Ground-truth the socket/dockerd facts before citing them.** Verified this iteration:
  `.devcontainer/docker-compose.yml:20-26` states the host socket is NOT mounted by default;
  `.oh/scripts/docker-compose.sh:136` appends `docker-compose.docker-sock.yml` only when
  `truthy()` (`:63`) accepts `sandbox.docker_socket` / `DOCKER_SOCKET`; `.devcontainer/Dockerfile:37`
  installs `docker-ce-cli` + `docker-compose-plugin` and no `dockerd`.
- **Writing any `.env` path trips the secret-exposure guard, and `rm -rf "$var"` trips the safety
  net.** Verifying the docker-sock overlay needs `DOCKER_SOCKET=true bash .oh/scripts/docker-compose.sh
  --repo-dir <dir> --print-argv config` (the script reads the env var before `.devcontainer/.env`,
  `docker-compose.sh:133`) — never write a `.env` fixture. Use literal `/tmp/<name>` paths, not
  `$(mktemp -d)` captured in a shell var, when a cleanup `rm -rf` follows. Heredoc-piped
  `git commit -F -` is also blocked; use repeated `-m` flags for multi-paragraph messages.
- **Scratch-verify new library code before committing, then delete the scratch.** A temporary
  `.oh/cli/src/__tests__/zz-<story>-scratch.test.ts` run with
  `npx --prefix .oh/cli vitest run <path>` exercises code whose real tests belong to a later story,
  without colliding with the file that story must create. Delete it and confirm with `git status`
  before staging.
- **The base compose bind is `..:${OH_PROJECT_ROOT:-/home/sandbox/harness}`** — a host path mapped
  to a fixed in-target path, so host and target paths are NOT identical in general. The RFC's
  "identical-path mapping only" stance is therefore a **contract-level** restriction (nothing above
  the seam translates paths; `resolveProjectRoot` yields one string used for both file reads and
  compose invocation), not a claim about the compose file. Do not restate it as a deployment fact.

---

## US-001 — 2026-08-10 23:05 UTC

**Title**: Ratify the Phase-0 brain/hands boundary in the RFC layer
**Files changed**: `.oh/docs/rfcs/rfc-brain-hands-boundary.md` (new), `.oh/docs/rfcs/rfc-runtime-support.md`, `.oh/docs/rfcs/README.md`, `.oh/docs/README.md`, `.oh/tasks/execution-target-contract/prd.md`, `CHANGELOG.md`
**Commit**: e9c893d7
**Result**: PASS

### What I did

Authored `.oh/docs/rfcs/rfc-brain-hands-boundary.md` covering all eight content ACs: the authority
clause in the header, the brain/hands responsibility table (ralph, cron, autopilot, memory as
brain; `oh gateway` brain-side by design), the eval split as a capability **rule** rather than a
list, the four-class state taxonomy (`WorkspaceState` / `ExecutionState` / `HarnessAuthState` /
`SessionState`) with § 4.1 documenting the Hermes known-violation, § 5 the workspace stance with
§ 5.1 calling the two-field shape speculative, and § 6 the `attach()` synchrony decision plus its
`contractVersion: 2` migration path. Amended `rfc-runtime-support.md` at all five required sites:
§ Purpose and the § 1 A1/A3 rows now say the host socket is opt-in and off by default and the image
ships the docker CLI but no `dockerd`; § 8 gained Sysbox as a net-new item 1 with the seven existing
entries renumbered down by one in their original relative order; § 9's DinD open decision is struck
through and marked ANSWERED with cross-links to #731, #733, and the new RFC. Added the `### Added`
CHANGELOG entry and both index rows.

Per the Captain's build note 1, the boundary rationale was **moved out of** `prd.md` (the US-001
"Artifact (a)" narrative and the "Why `attach()` is synchronous" prose) and replaced with citations
into the RFC's numbered sections. `wc -w` before: **5931**; after: **5590** (−341, net decrease as
required). Docs-only surface: `git diff --stat` confirms no file under `.oh/cli/` was touched.

### Verification

```
$ pnpm run typecheck
> openharness@0.1.0 typecheck
> (test -d .oh/cli/node_modules || npm --prefix .oh/cli ci --ignore-scripts) && npm --prefix .oh/cli run typecheck
> @mifune/openharness@0.2.0 typecheck
> tsc --noEmit
        (clean — no diagnostics)

$ pnpm test
 Test Files  39 passed (39)
      Tests  481 passed (481)
   Duration  4.43s
```

### Learnings for future iterations

- **The Hermes known-violation has real evidence** — don't invent it. `.oh/docs/harnesses/hermes.md`
  shows Hermes bundling brain (memory, auto-skills, scheduling, sub-agent delegation) with hands
  (its own container sandboxing across local/Docker/SSH/Singularity/Modal), with all state including
  `auth.json` fused in the project-local `.hermes/` blob; and its dashboard is toggled via a compose
  overlay (`.devcontainer/docker-compose.hermes-dashboard.yml`), i.e. brain-side policy riding on
  substrate config. Both facets are written into RFC § 4.1.
- **US-002's `target.ts` header must dodge probe C2.** The RFC's § 6 rationale names `docker exec`
  and `lifecycle.ts:298` freely — that is fine in a doc. The `target.ts` header comment must NOT:
  cite "the codebase bans shell strings; every subprocess invocation is argv-array form" and "the
  existing synchronous process-runner seam" instead. Verified line numbers for that header:
  `LifecycleRunner` at `lifecycle.ts:49-53`, `spawnSync` at `:59`, `runShell` at `:298`.
- **`RunResult` confirmed for US-003.** `.oh/cli/src/commands/lifecycle.ts:39-46` has
  `status | error | stdout` and no `stderr`, while the capture branch already passes
  `stdio: ["ignore", "pipe", "pipe"]` (`:61`) and discards `r.stderr` (`:63-68`). The additive
  `stderr?: string` change US-003 specifies is a two-line edit exactly where the PRD says it is.
- **RFC § 6 is now the citable source** for why `runShell` must stay sync. US-004 should link it
  rather than re-argue it — the authority clause makes restating it a defect.

---

## US-002 — 2026-08-10 23:12 UTC

**Title**: Define the provider-neutral `ExecutionTarget` contract
**Files changed**: `.oh/cli/src/lib/execution/target.ts` (new, 143 lines)
**Commit**: 21816693
**Result**: PASS

### What I did

Authored `.oh/cli/src/lib/execution/target.ts` as types-and-interface only — zero imports, zero
runtime code — exporting `ExecutionStatus`, `ExecutionCapability`, `ExecRequest`, `ExecResult`, and
`ExecutionTarget` exactly as the PRD sketch specifies: `argv: string[]` (no `command` field), the
five-state status union including `"absent"`, the six-member capability union with `"snapshot"` as a
literal only, `capabilities(): Promise<ReadonlySet<ExecutionCapability>>`, required `ExecResult.stderr`,
and `attach?(request: ExecRequest): number` **synchronous** while every other method stays async.
The file header records the three deliberate refinements to #733's sketch plus the `contractVersion: 2`
migration path, worded generically per the C2 constraint, and cites `rfc-brain-hands-boundary.md`
§ 5 / § 5.1 / § 6 rather than restating them (the RFC's authority clause).

Docker-noun surface is one two-line window: the `| "docker"` literal at `target.ts:65` and its doc
comment at `:64`. Everything else in the file — header included — is clean.

### Verification

Probe dry-runs against the **final committed** text (US-002 AC + US-005 C2/C3 pre-check):

```
C2 (Docker nouns, excluding the "docker" literal line + its doc-comment line): PASS
C3 (grep 'snapshot('):                                                        PASS (no match)
no runtime imports (grep '^\s*(import|require|export .* from)'):              PASS (zero)
no `command:` field:                                                          PASS (prose only)
exports: ExecutionStatus, ExecutionCapability, ExecRequest, ExecResult, ExecutionTarget
attach signature at :136 → `attach?(request: ExecRequest): number;`           PASS (not a Promise)
```

Cited line numbers re-verified against real files: `lifecycle.ts:49-53` is `LifecycleRunner`, `:59` is
`spawnRunner`; RFC `## 5` at :118, `### 5.1` at :139, `## 6` at :159.

```
$ pnpm run typecheck
> @mifune/openharness@0.2.0 typecheck
> tsc --noEmit
        (clean — no diagnostics)

$ pnpm test
 Test Files  39 passed (39)
      Tests  481 passed (481)
   Duration  3.80s
```

Test count is unchanged from US-001 (39/481) — expected: this story adds no tests, US-005 does.

### Learnings for future iterations

- **US-005's probe C2 has a pinned exclusion window.** The only Docker nouns in `target.ts` are on
  `:65` (`  | "docker"`) and its doc comment on `:64`. Implement C2 as: drop any line matching
  `\| "docker"` **and the single line immediately above it**, then grep the remainder
  case-insensitively for `container|compose|image|volume|dockerd|docker`. That exact awk filter is
  already dry-run green against the committed file. Do not write C2 as a whole-file grep with a
  `docker`-line-only exclusion — it would trip on the doc comment.
- **C3's grep must be `snapshot(`, not `snapshot`.** The capability literal and its doc comment both
  say "snapshot"; only the `(` distinguishes a method declaration. The doc comment is deliberately
  worded to avoid an adjacent open paren.
- **No CHANGELOG entry for US-002 — that is correct, not an omission.** US-002's AC list has no
  CHANGELOG criterion because a types-only internal file has no user-visible impact; US-004 carries
  the `### Changed` entry for the behavior-preserving reroute. Do not add a second entry.
- **US-003's DI seam already has two precedents to copy.** `RemoteRunner` (`lib/remote.ts:33-37`) and
  `LifecycleRunner` (`commands/lifecycle.ts:49-53`) are the same shape:
  `(cmd, args, opts) => RunResult` with a module-private `spawnRunner` default and an optional
  `run?` option on the public options interface. `lib/remote.ts`'s `RunResult` **already has
  `stderr?: string`** (`:29`) — so US-003's additive `stderr?` on the lifecycle `RunResult` is
  matching an existing in-repo convention, not inventing one.
- **`ExecRequest.stdio`'s default is deliberately left to the implementation** ("Omitted → the
  implementation's documented default"). US-003 may pick the adapter's default freely without
  contradicting the contract.

---

## US-003 — 2026-08-10 23:20 UTC

**Title**: Implement `DockerComposeExecutionTarget` by delegating to existing machinery
**Files changed**: `.oh/cli/src/lib/execution/runner.ts` (new), `.oh/cli/src/commands/lifecycle.ts`, `.oh/cli/src/lib/execution/docker-compose-target.ts` (new), `.oh/cli/src/lib/execution/index.ts` (new)
**Commit**: `a21476d0` (extraction checkpoint) then `f78603fd` (adapter + factory)
**Result**: PASS

### What I did

Two commits, in the order the ordering AC demands. **`a21476d0`** extracted the process-runner seam
into `lib/execution/runner.ts` — `RunResult`, `LifecycleRunner`, the real `spawnRunner`, plus the two
helpers both callers need (`assertSpawned`, `requireLifecycleScript`) and the seam's two error classes
— with `lifecycle.ts` re-exporting `LifecycleRunner`/`RunResult` so no import path breaks
(`lifecycle.test.ts` imports both types from there). `RunResult` gained `stderr?: string`, populated
by `spawnRunner` from the stderr `spawnSync` already piped and discarded. That commit is the
**standalone green checkpoint**: no adapter existed, and typecheck + 39 files / 481 tests passed.

**`f78603fd`** landed `docker-compose-target.ts` and `index.ts` on top. The adapter delegates every
compose operation to `.oh/scripts/docker-compose.sh` — no `-f <overlay>` list is assembled in
TypeScript — and detects the socket opt-in exactly as pinned: `--print-argv config` through the
injected runner (`stdio: "capture"`), then a scan of the `-f` file list for a path ending in
`docker-compose.docker-sock.yml`. `capabilities()` always carries `"exec"`/`"pty"`. `exec()` returns
the genuinely captured stderr and `""` only for inherited stdio (documented in the method). `index.ts`
exports the internal `resolveExecutionTarget()`; no harness.yaml key, CLI flag, or env var selects a
target. No caller is rerouted yet — that is US-004.

### Verification

The `--print-argv` oracle, run against the real script in both socket states:

```
$ bash .oh/scripts/docker-compose.sh --repo-dir /tmp/oh-cap-probe --print-argv config
docker / compose / -f / …/docker-compose.yml / config
$ DOCKER_SOCKET=true bash .oh/scripts/docker-compose.sh --repo-dir /tmp/oh-cap-probe --print-argv config
docker / compose / -f / …/docker-compose.yml / -f / …/docker-compose.docker-sock.yml / config
```

A throwaway scratch test (deleted before staging — US-005 owns the real test file) exercised the
adapter through the factory with a fake runner, 5/5 green: provision argv identical to today's
`runSandbox` call (`bash <script> --repo-dir <root> up -d --build`, `{stdio:"inherit"}`, exactly ONE
call) plus the `--no-build`/`OH_SANDBOX_IMAGE` variants and the `status: 17` → `ExecutionExitError`
path; attach argv identical to today's `["exec","-it","-u","sandbox",<name>,"zsh"]`, synchronous,
propagating 126 and throwing on ENOENT; capabilities in both socket states; `exec()` returning
`{exitCode:0, stdout:"hi", stderr:"warn"}` from a fake `{status:0, stdout:"hi", stderr:"warn"}`; and
the missing-script / missing-container errors.

```
$ pnpm run typecheck
> @mifune/openharness@0.2.0 typecheck
> tsc --noEmit
        (clean — no diagnostics)

$ pnpm test
 Test Files  39 passed (39)
      Tests  481 passed (481)
   Duration  4.21s
```

Test count unchanged (39/481) — expected: US-005 adds the tests.

### Learnings for future iterations

- **US-004's wiring is already determined by the adapter's failure semantics** (documented in the
  adapter's file header): a child that never ran throws `ExecutionSpawnError` whose message is
  byte-identical to today's `assertSpawned` text (`failed to run bash <script> (…)`,
  `failed to run docker exec <name>`); `attach()`/`exec()` return non-zero codes; `provision()`
  throws `ExecutionExitError` carrying the code, because `provision(): Promise<void>` has nowhere to
  return it. So `runSandbox` becomes
  `try { await target.provision(); return 0 } catch (e) { if (e instanceof ExecutionExitError) return e.exitCode; throw e }`
  — which reproduces `expect(await runSandbox(…)).toBe(17)` (lifecycle.test.ts:130) exactly. And
  `runShell` must catch `ExecutionSpawnError` with `code === "ENOENT"` and rethrow its own
  ``docker is required for `oh shell` but was not found on PATH`` (lifecycle.test.ts:436); any other
  `ExecutionSpawnError` propagates unchanged.
- **`resolveExecutionTarget()` returns `ResolvedExecutionTarget`** = `ExecutionTarget &
  Required<Pick<…,"provision"|"attach">>`, so US-004 calls `target.attach(…)` directly and gets
  `number`, not `number | undefined` — no `attach?.()` dance, and `runShell` keeps its `: number`
  signature.
- **The adapter never guesses a container name.** `container` is an option with no default; `attach`,
  `exec`, and `status` throw if it is unset. US-004 keeps the precedence chain (positional >
  `sandbox.name` > `DEFAULT_CONTAINER_NAME`) in `lifecycle.ts` — which is what the
  boundary-inversion criterion requires — and passes the resolved name to the factory. `runSandbox`
  passes no container (it needs none), so its one-call assertion (lifecycle.test.ts:114-120) holds.
- **`lifecycle.test.ts:114` asserts `calls` with `toEqual` on the whole array**, so `provision()` must
  stay exactly ONE child process. Do not let US-004 slip a `capabilities()` (a second, `--print-argv`
  child) into the `runSandbox` path.
- **Two micro-extensions beyond the literal AC text, both deliberate.** (1) The runner opts gained an
  optional `timeoutMs`, honored via `spawnSync`'s `timeout`, so `ExecRequest.timeoutMs` is real
  rather than silently ignored — same reasoning the PRD applies to `stderr`, and it mirrors
  `RemoteRunner`'s existing `opts.timeoutMs`. (2) `assertSpawned`/`requireLifecycleScript` moved into
  `runner.ts` alongside the runner instead of being copied, so the "missing lifecycle script …" and
  "failed to run …" strings exist in exactly one place. Both are additive; every existing fake runner
  still typechecks.
- **`capabilities()` advertises neither `"files"` nor `"ports"`** — the contract declares no method
  for either, so claiming them would be a capability no caller could use. If US-005 or a later slice
  wants them, add the method first.

---

## US-004 — 2026-08-10 23:28 UTC

**Title**: Route `oh sandbox` and `oh shell` through the contract
**Files changed**: `.oh/cli/src/commands/lifecycle.ts`, `.oh/skills/wiki/corpus/oh-cli-portable-lifecycle.md`, `.oh/skills/wiki/corpus/README.md`, `CHANGELOG.md`
**Commit**: `ca5c73cf`
**Result**: PASS

### What I did

`runSandbox` now resolves a target via `resolveExecutionTarget({projectRoot, run, build: !useNoBuild, env})`
and calls `provision()`, translating `ExecutionExitError` back into its own return code (which is
exactly the old `return r.status ?? 1`); spawn-level failures still propagate. `runShell` calls
`target.attach({argv: ["zsh"], user: "sandbox"})` — no `["exec","-it",…]` literal survives anywhere in
`lifecycle.ts` — catches `ExecutionSpawnError` with `code === "ENOENT"` and rethrows the byte-identical
``docker is required for `oh shell` but was not found on PATH``. Both doc comments were rewritten to
describe delegation rather than a literal invocation, and `runGateway`'s doc block gained the
brain-side rationale (its body is untouched). Revised the wiki entry to describe the seam as a
verb/side/route/delegates-to table, and added the `### Changed` CHANGELOG entry.

### Verification

Compat oracle, all mechanical:

```
$ git diff --stat -- .oh/cli/src/__tests__/lifecycle.test.ts     (empty — zero assertion edits)
$ grep -n "export function runShell" .oh/cli/src/commands/lifecycle.ts
290:export function runShell(opts: ShellOptions, io: LifecycleIO): number {
   → the signature line is CONTEXT in `git diff`, not a changed line
$ grep -n '"exec"' .oh/cli/src/commands/lifecycle.ts              (none)
$ grep -cF 'docker exec -it -u $(SHELL_USER) $(SHELL_CONTAINER) zsh' Makefile   → 1
$ git diff --stat -- Makefile                                    (empty)
$ wc -w .oh/skills/wiki/corpus/oh-cli-portable-lifecycle.md       → 836   (was 657, cap 900)
$ bash .oh/evals/probes/wiki-readme-index.sh
PASS: .oh/skills/wiki/corpus/README.md Index matches the git-tracked corpus/*.md frontmatter
```

```
$ pnpm run typecheck
> @mifune/openharness@0.2.0 typecheck
> tsc --noEmit
        (clean — no diagnostics)

$ pnpm test
 Test Files  39 passed (39)
      Tests  481 passed (481)
   Duration  4.51s
```

Test count still 39/481 — expected: US-005 adds the tests. The point of this story is that the
count did not move while the routing did.

### Learnings for future iterations

- **The `oh update` follow-up issue (US-005 AC) is real and now has a second data point.** The wiki
  entry's `/home/sandbox/project` claim, line-cited to `init.ts:503`, is FALSE — that line writes
  `/home/sandbox/harness`, and `grep -rn "sandbox/project" .oh/cli/src/` has zero hits. The
  "retrofit gap" bullet stated the inverse of current behavior too. Both were removed as part of the
  required prose trim. **I did not re-audit the entry's other citations** (the `cli.ts` / `remote.ts`
  paragraphs are as ingested on 2026-07-03) — that is a known, deliberate scope boundary, not a
  claim they are correct.
- **`runSandbox` keeps a `requireLifecycleScript` preflight even though `provision()` re-checks it.**
  This is deliberate, not redundancy: the check must fire BEFORE the `image mode: …` / `no-build
  mode: …` log line so an incomplete `.oh/` payload fails in the same order it did before. No test
  pins that ordering today; US-005 could add one. The call is idempotent and spawns nothing.
- **Nothing in `runSandbox` may call `capabilities()`.** It shells a second `--print-argv` child, and
  `lifecycle.test.ts:114` asserts the whole `calls` array with `toEqual` — exactly one child. The
  socket opt-in that `runSandbox` prompts for and the `"docker"` capability the adapter discovers are
  two different reads of the same fact; do not merge them.
- **US-005's probe C4 has a clean target.** After this story `lifecycle.ts` contains no `"exec"`
  string literal at all, so an argv-literal check (`["exec", "-it", …]` constructed outside the
  adapter) is unambiguous. The surviving prose does say "docker exec" in `runGateway`-adjacent
  comments and the CHANGELOG, which is exactly why C4 must not be a text grep.
- **Bumping a wiki entry's `updated:` re-sorts the README index.** `wiki-readme-index.sh` builds the
  expected table with `sort -r` on `<updated> <slug>`, so the row MOVES — editing it in place leaves
  the probe red. Move the row and change its `Tags`/`Updated` cells in the same pass.

## US-005 — 2026-08-10 23:40 UTC

**Title**: Prove no regression — tests + tier-A probe
**Files changed**: `.oh/cli/src/__tests__/execution-target.test.ts` (new), `.oh/scripts/__tests__/compose-args.test.ts`, `.oh/evals/probes/execution-target-contract.sh` (new, `+x`)
**Commit**: `a2b3bf9a`
**Result**: PASS

### What I did

Three surfaces plus the two execute-phase follow-ups.

**`execution-target.test.ts`** (20 cases, injected fake runner only — no real subprocess): exact
`provision()` argv in one child (`bash <script> --repo-dir <root> up -d --build`) plus the
`--no-build`/`OH_SANDBOX_IMAGE` variant and the `ExecutionExitError(17)` path; exact `attach()` argv
(`["exec","-it","-u","sandbox","my-box","zsh"]`) with `typeof code === "number"` asserted, so the
contractVersion-1 sync decision is pinned by a test rather than by convention; capability discovery
in BOTH socket states via a synthetic `--print-argv` dump, including the `-f` argv the adapter uses
to ask; a minimal `exec()` case proving `{status:0,stdout:"hi",stderr:"warn"}` →
`{exitCode:0,stdout:"hi",stderr:"warn"}` (stderr genuinely plumbed, not hardcoded) and a second case
mapping `cwd`/`env`/`user`/`timeoutMs` onto flags; and a dedicated `stdio:"inherit"` block proving
the streaming path is exercised, not only `"capture"`.

**`compose-args.test.ts`** gained one equivalence case: the adapter's `provision()` argv is captured
through a fake runner, then the REAL vendored script (copied into the fixture, with
`harness-config.sh` beside it) expands both the adapter's tail and the frozen pre-#733 tail
(`up -d --build`, the same verb the Makefile pins) through `--print-argv`. The two compose argv
arrays are identical, and are asserted against a literal expectation so two identical empties cannot
pass. The per-invocation `mktemp` harness-env path is normalized to `<harness-env>` before comparing.

**`execution-target-contract.sh`** — tier A, exactly five checks, standard 3-state oracle.
C4 and C3 run on a comment-STRIPPED copy of the source (a character-scanner `awk` that removes
`/* */` and `//`), so surviving doc prose about the old `docker exec` invocation can neither trip
nor falsely satisfy them; C4 then collapses whitespace and looks for an argv literal beginning
`["exec"` or the adjacent pair `"exec","-it"`. C2 excludes exactly the `"docker"` capability literal
line and the doc-comment line above it (located by line number, not by pattern-guessing) and bans
substrate nouns everywhere else. C5 is a whole-line `grep -Fx` after stripping the recipe tab.
SKIPPED (exit 2) when `.oh/cli/src` or `Makefile` is absent (a vendored `.oh/` payload), and also
when comment-stripping fails to yield recognizable code — never a silent green.

**Follow-ups (both required by AC, both done in this session):** issue
[#738](https://github.com/mifunedev/openharness/issues/738) filed under EPIC #731 for the `oh update`
manifest gap, and linked from PR #736's description under a new "Open questions resolved into
tracked work" section; comment `5249379675` posted on EPIC #731 (a comment, not a body edit)
assigning the identical-path-mapping vs. openharness-cloud#104 question to the #734 **planning**
spec.

### Verification

Probe verified BY REJECTION — all five checks, each broken in turn and restored:

```
--- C1: target.ts moved away        → exit=1  "…target.ts is absent — the execution contract has no home"
--- C2: appended "// a container …" → exit=1  "…names a substrate outside the "docker" capability literal (143:…)"
--- C3: appended snapshot(): …      → exit=1  "…declares a snapshot method (145:export interface Snapshotter …)"
--- C4: re-added ["exec","-it",…]   → exit=1  "…constructs an engine argv literal beginning with "exec""
--- C5: -it → -i in the Makefile    → exit=1  "…no longer contains the verbatim line `docker exec -it -u …`"
--- restored → git status clean of edits, probe exit=0
```

C2 dry-run against the FINAL committed header (AC-required, closes critique M on the header wording):

```
capability literal at line 65; exclusion window = lines 64-65
banned-noun hits in the header (lines 1-40):  (none)
banned-noun hits in the whole file outside the exclusion window:  (none)
```

Compat oracle, across the WHOLE slice (not just this commit):

```
$ git diff --stat 72a8e7e2..HEAD -- .oh/cli/src/__tests__/lifecycle.test.ts   (empty)
$ git diff --stat 72a8e7e2..HEAD -- Makefile                                  (empty)
```

Full probe suite (97 probes, 30s timeout each):

```
PASS=94 REGRESSION=2 SKIPPED=2 OTHER=0
non-green: cc-safety-net-wiring.sh  (.pi/npm/package.json pins a different cc-safety-net than 1.0.6)
           next-dev-prod.sh         (a live `next dev` on :3005 — host process state)
$ bash .oh/evals/probes/execution-target-contract.sh   → PASS (exit 0)
```

Both reds are pre-existing and untouched by this slice: `git diff --name-only 72a8e7e2..HEAD`
lists no `.pi/` path, and the second reads live host processes, not repo state.

```
$ pnpm run typecheck
> @mifune/openharness@0.2.0 typecheck
> tsc --noEmit
        (clean — no diagnostics)

$ pnpm test
 Test Files  40 passed (40)
      Tests  502 passed (502)
   Duration  3.81s
```

39 files / 481 tests → 40 / 502 (+1 file, +21 tests) with zero existing assertions edited.

### Learnings for future iterations

- **No CHANGELOG entry, deliberately.** US-001 (`### Added`, the RFC) and US-004 (`### Changed`,
  lifecycle routing) carry the user-visible impact; the PRD gives US-005 no CHANGELOG AC because
  tests and a probe change nothing an operator sees. Don't add one retroactively.
- **`.oh/scripts/__tests__/*.test.ts` CAN import CLI source.** `../../cli/src/lib/execution/index.js`
  resolves under vitest (one root project, `vitest.config.ts` includes all three test globs, and
  Vite maps the TS-style `.js` specifier to the `.ts` file). It is NOT covered by
  `pnpm run typecheck`, which only runs `tsc --noEmit` inside `.oh/cli/` — type errors in that
  directory surface only at vitest transform time, so run the file, don't trust typecheck.
- **`--print-argv` output is not byte-stable across invocations.** The harness env file is a
  per-invocation `mktemp` path (`openharness-harness-yaml-env.XXXX`), so any argv-equality assertion
  between two runs must normalize it first. The pre-existing tests in that file dodge this by
  asserting `argv[5]` positionally with `toContain`; a whole-array `toEqual` cannot.
- **Comment-stripping is what makes a "no engine argv" probe honest.** A naive `grep -q "docker exec"`
  over `lifecycle.ts` is red today purely from prose (`runGateway`'s doc block still explains why it
  is NOT routed). Strip comments first, then match argv-literal shape — and sanity-check that the
  stripper still yields recognizable code, exiting 2 rather than 0 if it does not.
- **Probes are discovered by glob, not registered.** `/eval` globs `.oh/evals/probes/*.sh`; there is
  no index to update. A new probe with no `RESULTS.md` row is emitted as `new-pass` on the next run.
  Adding a hand-written row would be churn, so this story does not touch `RESULTS.md`.
- **Open questions become issues, not task-folder notes.** Both follow-ups were AC-mandated for a
  reason recorded in the PRD: archived task folders vanish and their flags with them. Verify the
  claim before filing — `.oh/manifest.json`'s `include` list genuinely has no `docs/**`, and
  `shouldShip()` returns false for anything unmatched, so #738 is a real defect and not a guess.

---
STATUS: COMPLETE
