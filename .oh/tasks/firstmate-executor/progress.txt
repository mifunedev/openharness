# progress

## Codebase Patterns

- **Shell tests are vitest, not bats.** Shell scripts are tested from
  `.oh/scripts/__tests__/*.test.ts` by spawning `bash -c "source <script>; <snippet>"`
  (see `ralph.test.ts` for the original of this pattern). Tests placed under
  `.oh/skills/` NEVER run — `vitest.config.ts` includes only
  `.oh/scripts/__tests__/**`, `.pi/**/__tests__/**`, `.oh/cli/**/__tests__/**`.
- **Stub external CLIs via a fixture bin dir on PATH.** `session-runner.test.ts`
  writes `herdr`/`tmux` stubs whose behavior is driven entirely by `STUB_*` env
  vars and which append every invocation to `$STUB_CALLS`. Two PATH shapes:
  *isolated* (PATH = fixture dir only, with coreutils symlinked in) proves a tool
  is genuinely ABSENT; *prepended* (fixture + real PATH) merely shadows it. The
  calls file is also the proof of a NEGATIVE — "the guard never invoked herdr" is
  asserted as "the calls file does not exist".
- **`pnpm run test` does not work here; use `npx vitest run`.** The root `test`
  script is `vitest run`, but vitest is not linked into `node_modules/.bin` in
  this worktree. `pnpm run typecheck` works normally.
- **shellcheck is not installed in the sandbox and there is no sudo.** Run it as
  `docker --config /tmp/dockercfg run --rm -i koalaman/shellcheck:stable - < <file>`
  — feed the file on STDIN, because a `-v "$PWD:/mnt"` bind mount resolves
  against the HOST filesystem (the docker socket is the host's), so the
  container-side path does not exist. A clean `/tmp/dockercfg/config.json`
  containing `{}` works around `error getting credentials`.
- **CI's shellcheck glob is `.oh/scripts/*.sh` — it does NOT cover
  `.oh/scripts/lib/*.sh`.** (`.github/workflows/ci-harness.yml:126`,
  `release.yml:92`.) The new library is shellcheck-clean, but nothing in CI
  enforces that yet. Extending the glob touches `.github/workflows/`, which is
  outside US-001's ACs and is push-restricted for HTTPS tokens — flagged for a
  human/later story rather than done here.
- **Angle brackets are the repo's placeholder notation — so a "closed set of
  placeholders" needs a second notation for runtime-fill text.**
  `.claude/skills/ship-spec/templates/prompt.md` mixes real render-time tokens
  (`<slug>`, `<issue>`) with illustrative ones the *session* fills (`<NNN>`,
  `<story title>`, `<YYYY-MM-DD>`), which makes "every `<...>` in the body is a
  placeholder" false. `session-prompt.md` splits them: `<angle>` = renderer
  substitutes, `{curly}` = session fills at runtime, declared in the header.
  A one-line `grep -o '<[^ <>]*>' | sort -u` then IS the closed-set oracle.
- **A contract recorded verbatim in a file's own header makes an ordering
  probe vacuous unless the header is excluded.** The anchor list lives in the
  header in the asserted order, so every anchor's first occurrence would be in
  the header. `session-prompt.md` ends its header with a literal
  `END CONTRACT HEADER -->` marker line and declares the ordering scope as
  body-only; US-009's probe must skip to that line before comparing.
- **A comment starting `# shellcheck <word>` is parsed as a DIRECTIVE, even in
  prose.** `# shellcheck does not flag it.` inside a header block fails the file
  with `SC1073/SC1072: Couldn't parse this shellcheck directive`. Reword so the
  literal `shellcheck` is never the first word after `# ` (e.g. "which no linter
  flags"). Mid-sentence mentions are fine — `session-runner.sh` has several.
- **`set -o pipefail` turns `producer | head -n 1` into a landmine.** `head`
  closing the pipe early gives the producer SIGPIPE (141), and a no-match `grep`
  gives 1; either sinks the whole command substitution, and a trailing
  `|| var=""` then CLOBBERS the value the substitution already captured. Wrap the
  producer instead: `var="$( { producer || true; } | head -n 1)"`.
- **A sourced library must not `set` shell options at file scope**, and must not
  use `set -u` even function-scoped: under `set -u` an unbound expansion
  terminates the whole shell, which from a sourced library means killing the
  CALLER. Express strictness as explicit argument validation that RETURNS
  non-zero. Same reason libraries return errors instead of calling `exit`.
- **Provider skill dirs are blanket symlinks — never hand-mirror a skill.**
  `.pi/skills`, `.claude/skills`, and `.codex/skills` all point at
  `../.oh/skills`, so writing `.oh/skills/<name>/SKILL.md` exposes it under all
  three providers with zero provider-side edits. `skills-vendored.sh` pins this.
  Same shape at the root: `CLAUDE.md -> AGENTS.md`, so edit `AGENTS.md` only.
- **`.claude/protected-paths.txt` entries are matched with `grep -Fxq` — whole
  line, exact.** One legacy entry (`.oh/agents/advisor.md`) carries a trailing
  inline comment, so the format tolerates one, but a consumer matching that path
  exactly would miss it. Put rationale on its own `#` line above the entries.
- **A new `.oh/skills/<name>/SKILL.md` registers into the CURRENT session**
  immediately (a `<system-reminder>` announces it). Free confirmation that the
  frontmatter parsed — a malformed `description:` surfaces at write time, not at
  the next session.
- **AGENTS.md's Skills table is curated, not exhaustive.** 43 skill dirs exist;
  ~25 have rows (`herdr`, `fanout`, `weigh`, `rlm`, `blog`, `sync`, `approve`,
  `critique`, `benchmark` have none). A missing row is not evidence a skill is
  absent — check `ls .oh/skills/`.
- **`.oh/tasks/<slug>/` is gitignored; task bookkeeping needs `git add -f`.**
  A plain `git add` of `prd.json`/`progress.txt` fails with "paths are ignored by
  one of your .gitignore files" even though the files are already tracked.
- **Prove "region X is unmodified" by extracting the region from both sides and
  diffing it** — not by reading hunk headers. `git show HEAD:<file> | awk
  '/^### <marker>/{f=1} f' > /tmp/old` vs. the same awk on the worktree copy,
  then `diff`. Hunk headers alone can't distinguish "untouched" from "shifted by
  an insertion at the boundary", which is exactly the failure an
  additive-only AC is guarding against.
- **A probe pin belongs to ONE target file — read which shell variable the grep
  takes.** `autopilot-executor-toggle.sh` greps `$SKILL` (autopilot),
  `$SHIP` (ship-spec), `$CRON`, and `$AGENTS` in one file; the argument-hint pin
  at L32 is `$SKILL`-only, so the same-looking edit is free in one skill and
  requires a lockstep probe update in the other. Never generalize "that string
  is pinned" across files without checking the variable.

---

## US-001 — 2026-08-12 10:30 UTC

**Title**: `.oh/scripts/lib/session-runner.sh` — the runner ladder
**Files changed**: `.oh/scripts/lib/session-runner.sh` (new),
`.oh/scripts/__tests__/session-runner.test.ts` (new)
**Commit**: ad9b5d4d
**Result**: PASS

### What I did

Wrote the sourceable runner-ladder library and its 46-case vitest suite. The
library defines the five required functions (`runner_detect`, `runner_launch`,
`runner_verify_cwd`, `runner_alive`, `runner_teardown`) plus `resolve_timeout_ms`,
`runner_watch`, `runner_abort`, `runner_install_abort_trap` and the shared
naming/log/lock path helpers US-003 will reuse. herdr eligibility is the zeroth
nesting guard plus three conjuncts (`command -v herdr`, the two-literal
`herdr status` health predicate, the execution-context fingerprint gate);
every other outcome degrades down the ladder with the reason written to the
firstmate log, while an explicit `--runner`/`OH_RUNNER` that cannot be honoured
is a hard error. `resolve_timeout_ms` is the single budget source and rejects
`0`/negative/non-numeric/empty/out-of-range. All checks are green: 527/527
vitest across 40 files (incl. an untouched `ralph.test.ts`), `pnpm run
typecheck` clean, shellcheck clean at BOTH default and `-S warning` severity,
`git diff` empty for `.oh/scripts/ralph.sh` and `.oh/prompts/`, and no
`SKILL.md` touched.

### Learnings for future iterations

- **The fingerprint gate is provably symmetric.** The caller-side fingerprint and
  the probe-pane fingerprint are produced by the SAME snippet
  (`RUNNER_PROBE_SCRIPT`, run locally via `bash -lc` and in the pane via
  `herdr agent start ... -- bash -lc`), so "gathered the same way" is true by
  construction. Do not re-derive one of the two sides separately.
- **`herdr status` verified live in this sandbox**: `status: running` and
  `compatible: yes` are indented lines under a `server:` block — a plain
  `grep -q 'status: running'` matches. There is no single "healthy" field, which
  is exactly why the predicate is those two literals.
- **The `| tee` is built ONCE** into the shared `inner` command string and used by
  all three launch branches, so the behavioral contract holds while a naive
  `grep -c 'tee'` over the file counts 1, not 3. The US-009 probe must assert the
  literal's PRESENCE (or the behavior), never a count of three.
- **`--no-focus` appears twice on purpose**: once in the session launch, once in
  the detection probe pane. A probe asserting "every herdr launch has
  `--no-focus`" should compare the count of `herdr agent start` occurrences with
  the count of `--no-focus`.
- **US-003 should source, not re-derive.** `runner_lock_path`, `runner_log_path`,
  `runner_session_log_path`, `runner_agent_name` and `runner_tmux_session` are
  already here and honour `RUNNER_TMPDIR` (default `/tmp`, overridable for
  tests). `runner_abort` is the whole exit-path contract in one call.
  `runner_teardown` recovers the pane id from `herdr agent get` when the calling
  process did not launch the session — that is what `firstmate.sh --kill` needs.
- **Deliberate deviation recorded in the file header**: the foreground branch does
  NOT `exec` (as the PRD sketch shows). `exec` would replace the shell and
  forfeit the FR-9a exit contract, so the command runs as a supervised child
  watched by the same bounded poll tmux mode uses. The foreground `| tee` is new
  behavior, not ralph parity.
- **Testing "the budget was rejected" needs a negative timing assertion.** You
  cannot assert a 4-hour ceiling directly; instead run the watch with
  `FIRSTMATE_TIMEOUT_MS=0` under a short harness timeout and assert the process
  was KILLED (`signal` set, no `RC=` printed) rather than having returned — which
  proves `0` did not become the ceiling.

---

## US-002 — 2026-08-12 10:36 UTC

**Title**: session-prompt template + renderer contract
**Files changed**: `.oh/skills/firstmate/templates/session-prompt.md` (new)
**Commit**: 86b1928b
**Result**: PASS

### What I did

Wrote the skill-owned session prompt that drives one long-lived First-Mate
session over the whole `prd.json` task graph — a derivative of
`.oh/prompts/advisor/implement.yml` + `pr.yml` step order, with `.oh/prompts/`
left zero-diff. The body encodes the task-graph load, per-story `/compact`,
the exact per-story cycle (implement → quality checks → commit with a
`Submitted-by:` trailer → validate `acceptanceCriteria` → flip `passes: true`
→ append progress), First-Mate-only certification, the max-3 bounded
AUDIT-FAIL → `BLOCKED` rule, the dual-channel `STATUS: COMPLETE` contract,
resume semantics, and the herdr/`/delegate` policy stated as prompt-level
policy rather than a server guarantee. Both of this story's machine-checkable
contracts live in the file's own header and were verified mechanically:
the closed placeholder set (`grep -o '<[^ <>]*>' | sort -u` over the whole
file returns exactly `<branch>`, `<issue>`, `<slug>`) and the 8-anchor
relative ordering in the body (lines 91 → 98 → 110 → 130 → 167 → 190 → 191 →
192, strictly increasing). `pnpm run typecheck` clean; 527/527 vitest across
40 files; no `SKILL.md` created or modified.

### Learnings for future iterations

- **The anchor list US-009's probe must hardcode is, in order:**
  `dependency graph`, `/compact`, `acceptanceCriteria`, `passes: true`,
  `/audit implementation`, `evidence.md`, `/retro`, `Ready PR` — with pack
  provenance `implement.yml:23,24,25,25,27,—,28` and `pr.yml:23,24,25,25,—,
  28-32,33,34`. The probe must compare **first occurrence** and must start
  **after** the `END CONTRACT HEADER -->` line, or it passes vacuously on the
  header's own verbatim copy of the list.
- **The three anchors that were nearly impossible to place naturally** are
  `evidence.md`, `/retro`, and `Ready PR` — they belong to `/ship-spec`
  Stages 11-13, which US-005 leaves unmodified, so the session must NOT run
  them. They appear in §6 as an explicit *handoff* sentence ("`/ship-spec`
  owns the tail") which satisfies the ordering without falsely claiming the
  session does that work. Do not "fix" that sentence into an instruction.
- **`/audit pr` is deliberately NOT an anchor** even though it is `pr.yml:27`:
  it sits between anchors 5 and 6 in the pack but is out of session scope, and
  including it would have forced the template to instruct work US-005
  reserves for ship-spec. The header records this exclusion, and US-009's
  probe must not add it back.
- **US-003's renderer has exactly three tokens to substitute** — `<slug>`,
  `<branch>`, `<issue>` — and `<issue>` is BARE DIGITS: the template already
  writes `#<issue>` where a `#`-prefixed reference is wanted, so the renderer
  must not prepend one. Curly-brace text is not its business.
- **`git commit -F -` with a heredoc is blocked by the Bash safety net** in
  this environment ("could not be safely analyzed (strict mode)"), and so is
  chaining `cd`+`git add`+`git commit` in one call. Use separate calls and
  repeated `-m` flags — each `-m` becomes its own paragraph, which is enough
  for a full commit body plus the `Submitted-by:` trailer as the final `-m`.

---

## US-003 — 2026-08-12 10:50 UTC

**Title**: `.oh/scripts/firstmate.sh` — the executor entrypoint
**Files changed**: `.oh/scripts/firstmate.sh` (new, executable),
`.oh/scripts/lib/task-contract.sh` (new), `.oh/scripts/__tests__/firstmate.test.ts` (new)
**Commit**: e970f885
**Result**: PASS

### What I did

Wrote the opt-in executor entrypoint and its 37-case vitest suite. The slug +
four-file validation went into a new **shared sourced helper**
`.oh/scripts/lib/task-contract.sh` (the AC's preferred option) carrying
ralph.sh's exact message strings with a provenance comment naming
`ralph.sh:394-414` as their source; `ralph.sh` stays zero-diff. The entrypoint
composes: whole-line `STATUS: COMPLETE` short-circuit → cross-executor ralph
guard (`tmux has-session -t <bare slug>`) → `runner_detect` → atomic
`mkdir` launch claim with a `runner_alive` cross-check and stale-lock reclaim →
`render_session_prompt` → `runner_launch` + `runner_verify_cwd` → launch banner
→ `runner_watch`. `--kill <slug>` is the escape hatch. Every failure path runs
`runner_abort` (teardown → lock removal → `FIRSTMATE-INCOMPLETE`), and an
`INT`/`TERM` trap is installed the moment the lock is claimed. Checks: 564/564
vitest across 41 files (`ralph.test.ts` untouched and green), `pnpm run
typecheck` clean, shellcheck clean at BOTH default and `-S warning` on
`firstmate.sh` and `task-contract.sh`, and `git diff` empty for
`.oh/scripts/ralph.sh` and `.oh/prompts/`.

### Learnings for future iterations

- **`.oh/scripts/lib/task-contract.sh` is a THIRD new lib file.** US-004's AC
  names only `firstmate.sh` and `lib/session-runner.sh` for
  `.claude/protected-paths.txt`. Adding `lib/task-contract.sh` alongside them is
  the consistent call (same "load-bearing shared infra" rationale), but it is
  NOT in US-004's AC text — decide deliberately rather than by omission.
- **The renderer STRIPS the contract header.** `render_session_prompt` drops
  everything through the `END CONTRACT HEADER -->` line before substituting, so
  the anchor bookkeeping never reaches the session. US-009's probe reads the
  TEMPLATE (not the rendered output), so this is invisible to it — but any
  future check that greps a *rendered* prompt for anchors will find only the
  body's copies.
- **Deliberate deviation from PRD §5's launch sketch, recorded in the file
  header**: the sketch is `<harness> "/goal <rendered-prompt>"`. There is no
  `/goal` skill in this repo (`ls .oh/skills/` has no `goal` entry) and a ~7KB
  argv nested inside `bash -lc '…'` is a quoting hazard, so the prompt is
  written to `$FIRSTMATE_PROMPT_FILE` and piped in on stdin — ralph.sh:191's
  proven shape. US-008's wiki entry and US-004's SKILL.md should describe the
  stdin shape, not the `/goal` one.
- **`FIRSTMATE_HARNESS_CMD` is the hermetic-test seam** and US-010 will want it:
  it fully overrides the launched command, and the session's exports
  (`FIRSTMATE_SESSION=1`, `FIRSTMATE_SLUG`, `FIRSTMATE_TASK_DIR`,
  `FIRSTMATE_PROMPT_FILE`) reach it. The end-to-end "watch to completion" test
  uses it plus `RUNNER_POLL_INTERVAL_S=1` to drive a real foreground run to the
  sentinel in ~50ms.
- **`--no-watch` deliberately leaves the lock claimed**, because with nobody
  watching, nobody enforces the budget or runs the exit path. The banner says so
  and points at `--kill`. Do not "fix" it by releasing the lock — that would
  reopen the TOCTOU window the atomic `mkdir` closes.
- **The tmux stub needs to be TARGET-AWARE.** firstmate asks `tmux has-session`
  about two different names — the bare slug (ralph guard) and
  `agent-firstmate-<slug>` (its own liveness oracle) — so a stub with one global
  `STUB_TMUX_HAS_SESSION` rc cannot express "a ralph session is live but mine is
  not". `firstmate.test.ts`'s stub parses `-t` and matches against a
  space-separated `STUB_TMUX_SESSIONS` list.
- **The herdr stub's probe output must carry the MARKER.**
  `runner_local_fingerprint` strips `FIRSTMATE-FINGERPRINT ` before returning,
  so a stub echoing that bare value back produces "no fingerprint obtained", not
  a match. Re-add the marker (`matchingProbeOutput()` in the suite).
- **CHANGELOG is US-008's, by the PRD's own allocation.** No `## [Unreleased]`
  entry was added here; US-001 and US-002 did the same. US-008's AC
  ("CHANGELOG.md gains an entry under `## [Unreleased]`") is the single entry
  for the whole feature — it must cover `firstmate.sh` and the ladder, not just
  the `sandbox-processes.md` change.

---

## US-004 — 2026-08-12 10:55 UTC

**Title**: `/firstmate` SKILL.md + protected-paths registration
**Files changed**: `.oh/skills/firstmate/SKILL.md` (new),
`.claude/protected-paths.txt`, `AGENTS.md`
**Commit**: bf276399
**Result**: PASS

### What I did

Wrote the 323-line `/firstmate` SKILL.md — executor contract (opt-in third arm,
ralph still default, `STATUS: COMPLETE` as the invariant terminal interface),
the herdr → tmux → foreground ladder with its zeroth nesting guard and three
eligibility conjuncts, the naming contract, the `FIRSTMATE_TIMEOUT_MS` session
budget via `resolve_timeout_ms`, the watch matrix (progress.txt is the authority
in every mode) with per-mode liveness oracles, the recovery matrix
(FIRSTMATE-INCOMPLETE / PR stays draft + resume comment / mid-run herdr loss
degrades to file-polling / stale lock is reclaimable), the per-mode manual kill
procedure, per-slug concurrency guidance, and the env-var table. It opens with
the executor-vs-role-charter disambiguation table. Registered `firstmate` plus
three script paths in `protected-paths.txt` and added the `/firstmate` Skills row
to `AGENTS.md` only. Checks: `readlink CLAUDE.md` → `AGENTS.md` and
`diff AGENTS.md CLAUDE.md` empty (verified again after the commit), 564/564
vitest across 41 files, `pnpm run typecheck` clean, 14 plausibly-affected probes
green (incl. `skills-dir-clean`, `skill-paths`, `first-mate-charter`,
`autopilot-executor-toggle`, `ralph-fallback-order`, `spec-family-contract`),
and `git diff --stat` empty for `.oh/scripts/ralph.sh`, `.oh/prompts/`, `.pi/`.

### Learnings for future iterations

- **`.oh/scripts/lib/task-contract.sh` WAS added to `protected-paths.txt`**, one
  path beyond US-004's two named ones, on the identical "load-bearing shared
  infra" rationale plus that file's own same-PR rule. US-003 flagged the choice;
  this is the resolution. No probe pins the file's entry set (only
  `markitdown-wiki-ingest.sh` greps it, for its own path), so the addition is
  probe-safe.
- **`protected-paths.txt` is consumed with `grep -Fxq` — whole-line exact
  match.** `.oh/agents/advisor.md` carries a trailing inline comment, so the
  format tolerates one, but any consumer matching that path exactly would break.
  Put rationale on its own `#` line above the entries, not after them.
- **Pi/Claude/Codex needed ZERO provider edits** — `.pi/skills`,
  `.claude/skills`, and `.codex/skills` are all blanket symlinks to
  `../.oh/skills`, so `.oh/skills/firstmate/SKILL.md` appeared under all three
  the moment it was written (verified with `ls -l` on each). Never hand-mirror a
  skill into a provider dir.
- **The new skill self-registered into this session's own skill list** the moment
  the file was written (a `<system-reminder>` announced `firstmate` as available).
  Useful signal that frontmatter parsed; also means a malformed `description:`
  would surface immediately rather than at the next session.
- **US-008's remaining AGENTS.md edit must not re-litigate the Skills table.**
  The `/firstmate` row is now at line 190, between `/spec` and `/teach`. US-008
  edits the same file for the runner-ladder mention — keep the two edits in
  different sections so neither reverts the other.
- **`.oh/skills/` has 43 skill dirs but AGENTS.md's table lists ~25.** The table
  is curated, not exhaustive (`herdr`, `fanout`, `weigh`, `rlm`, `blog`, `sync`,
  `approve`, `critique`, `benchmark` are all absent). Do not treat a missing row
  as evidence a skill does not exist.

---

## US-005 — 2026-08-12 11:02 UTC

**Title**: `/ship-spec` Stage 10 opt-in arm
**Files changed**: `.oh/skills/ship-spec/SKILL.md` (= `.claude/skills/ship-spec/SKILL.md`)
**Commit**: 1475ba1a
**Result**: PASS

### What I did

Made `--executor=firstmate` a real `/ship-spec` flag, additively. Stage 1 gained
the `*--executor=firstmate*) SHIP_SPEC_EXECUTOR=firstmate` case arm and
`firstmate` in the `ralph|delegate-advisor)` validation list; Stage 10 gained an
"Opt-in (`firstmate`)" subsection carrying the step-(2) substitution for the
Advisor `/goal` prompt, the `.oh/scripts/firstmate.sh` invocations, a per-mode
session-handle/log/watch-command table, the `FIRSTMATE_TIMEOUT_MS` bound, and the
`FIRSTMATE-INCOMPLETE` → stays-draft recovery mirroring `RALPH-INCOMPLETE`.
Verified mechanically: `grep -Fx` on the byte-identical default line, the
136-line Stage 11–13 tail diffed byte-for-byte against `HEAD`, `ralph.sh` and
`.oh/prompts/` zero-diff, `readlink CLAUDE.md` = `AGENTS.md`. 14 probes green
(incl. `autopilot-executor-toggle`, `ship-spec-ready-finalization`,
`autopilot-upstream-default`, `autopilot-pi-agent`), 564/564 vitest across 41
files, `pnpm run typecheck` clean.

### Learnings for future iterations

- **A case arm's ACs are satisfiable while the flag still fails.** The AC pair
  (new arm + validation list) exists precisely because line 68's
  `case "$SHIP_SPEC_EXECUTOR" in ralph|delegate-advisor)` hard-exits on anything
  else. Adding the arm alone yields `ERROR: invalid SHIP_SPEC_EXECUTOR=firstmate`.
  US-006 has the same two-part shape in autopilot (~L204) — do not land one half.
  I executed the extracted snippet across all five `ARGUMENTS` values plus two
  env-var shapes rather than eyeballing it; `--executor=bogus` silently resolves
  to `ralph` (pre-existing, unchanged — only the *env var* path hard-errors).
- **`/ship-spec`'s own argument-hint is NOT probe-pinned; `/autopilot`'s IS.**
  Only `.oh/evals/probes/autopilot-executor-toggle.sh:32` pins a hint bracket,
  and it targets `$SKILL` (the autopilot skill), never `$SHIP`. So extending
  ship-spec's hint is free, while US-006's identical-looking edit REQUIRES the
  lockstep probe update in the same commit. Do not generalize from this story.
- **The two `$SHIP` pins are the whole ship-spec surface of that probe** (L69
  the default line, L70 the delegate-advisor arm). Everything else in ship-spec
  is unpinned by it — but `ship-spec-ready-finalization.sh` separately pins five
  tokens (`ready-for-review PR`, ``Stage 13 — `gh pr ready` ``,
  `Finalization contract`, `/eval`, `/ci-status`) and greps for *forbidden*
  draft-only phrasings, so new Stage-10 prose must not reintroduce
  "manual: `gh pr ready`"-style wording.
- **"Stages 11–13 unmodified" is cheap to prove, so prove it.**
  `git show HEAD:<file> | awk '/^### Stage 11 —/{f=1} f'` vs. the same awk on
  the worktree copy, then `diff`. Hunk-header inspection (`git diff -U0 | grep
  '^@@'`) plus the stage line numbers is the corroborating check — an insertion
  *at* the boundary would shift Stage 11 without appearing to touch it.
- **`.claude/skills/ship-spec/` and `.oh/skills/ship-spec/` are the same file.**
  `.claude/skills`, `.pi/skills`, `.codex/skills` are all blanket symlinks to
  `../.oh/skills`, so the AC's `.claude/...` path and git's `.oh/...` path name
  one inode. Stage the `.oh/` path; editing through either works, but only the
  real path is what git records.
- **CHANGELOG is still US-008's single entry for the whole feature** (US-001
  through US-005 have all deferred it). US-008 must therefore cover the
  ship-spec/autopilot opt-in arms too, not just its own `sandbox-processes.md`
  change.

---


## US-006 — 2026-08-12 11:06 UTC

**Title**: `/autopilot` pass-through flag
**Files changed**: `.oh/skills/autopilot/SKILL.md` (= `.claude/skills/autopilot/SKILL.md`), `.oh/evals/probes/autopilot-executor-toggle.sh`
**Commit**: ddbec416
**Result**: PASS

### What I did

Landed the `firstmate` arm in `/autopilot` as pure deferral, both halves of the
two-part edit together: the additive `*--executor=firstmate*) EXECUTOR=firstmate`
case arm AND `firstmate` in the `case "$EXECUTOR" in ship-spec|delegate-advisor|ralph)`
validation list (~L204). Added a §5 `#### firstmate` subsection mirroring
`delegate-advisor` (passes `--executor=firstmate` to `/ship-spec`, no autopilot-side
build mechanics, reuses the `DELEGATE-FAIL` compensation) with the
`FIRSTMATE_TIMEOUT_MS` cap stated where the arm is described, plus the
argument-hint bracket, the toggle prose, the design-notes executor bullet, the
`DELEGATE-FAIL` token row, and two Reference rows. The probe's pinned bracket
literal was updated in the SAME commit per the lockstep AC, and I added a new
probe assertion for the validation list so the two halves can never land apart.
Verified: both required probes green, the full suite 94 PASS / 0 FAIL, 564/564
vitest, typecheck clean, `ralph.sh` + `.oh/prompts/` zero-diff, §5 ralph fallback
byte-identical, and the three pinned case arms at exactly one occurrence each.

### Learnings for future iterations

- **Execute the extracted snippet; don't eyeball a two-part case edit.** I ran
  the live `EXECUTOR=` block against 7 `ARGUMENTS` values and 5
  `AUTOPILOT_EXECUTOR` values. `--executor=firstmate` → `firstmate` (exit 0),
  `AUTOPILOT_EXECUTOR=bogus` → exit 1. Note the asymmetry US-005 also found:
  `--executor=bogus` silently resolves to the default (the `case` just doesn't
  match), while only the *env-var* path hard-errors. Pre-existing, unchanged.
- **When you break a pinned literal by design, ADD an assertion for the thing
  that was un-pinned.** The bracket edit was the AC's sanctioned probe break;
  the validation list at ~L204 was pinned by nothing, which is exactly why the
  critique flagged it as a silent-failure hazard. I pinned it
  (`grep -Fq 'case "$EXECUTOR" in ship-spec|delegate-advisor|ralph|firstmate)'`)
  so the arm and the list are now mechanically coupled.
- **`shellcheck` on `.oh/evals/probes/` is info-noisy at baseline and NOT in
  CI.** `autopilot-executor-toggle.sh` has 24 pre-existing SC2016 infos (the
  correct single-quoted `grep -F` idiom); mine made 25. CI runs
  `shellcheck -S warning` over `.oh/scripts/*.sh` and friends — probes are not
  in the glob at all. Compare against `git show HEAD:<file> | shellcheck -`
  before calling an info-level hit a regression, and gate on `--severity=warning`.
- **A third probe SKIPs in this environment: `next-dev-prod.sh`** ("no
  mifunedev/website process or app-website tmux session"). It is
  environment-conditional and unrelated to this branch — it skips at HEAD too.
  US-009's AC pins the SKIPPED baseline at 2 (`autopilot-preflight-gate`,
  `debugmcp-availability`); expect 3 here and attribute the third to the absent
  website process, not to a regression.
- **The deferral must set no budget sentinel — verify with a grep for the
  ASSIGNMENT, not the mention.** `grep -nE 'FIRSTMATE_TIMEOUT_MS[[:space:]]*=|export FIRSTMATE_TIMEOUT_MS'`
  returns nothing in the autopilot skill while 5 read-only mentions document the
  cap. Enforcement stays in US-001's `resolve_timeout_ms`.
- **CHANGELOG still unwritten for this whole feature.** US-001..US-006 have all
  deferred it (no US-006 AC names it). US-008 owns the single `## [Unreleased]`
  entry and must now cover the ship-spec Stage 10 arm AND this autopilot arm,
  not just `sandbox-processes.md`.

---

## US-007 — 2026-08-12 11:10 UTC

**Title**: `/spec execute` names the third executor
**Files changed**: `.oh/skills/spec/references/execute.md` (= `.claude/skills/spec/references/execute.md`)
**Commit**: 142f098f
**Result**: PASS

### What I did

Added exactly one sentence to §1 "Commitment + build" of
`.oh/skills/spec/references/execute.md`, immediately after the paragraph that
already enumerates `--executor=ralph` (default) and `--executor=delegate-advisor`
(legacy): it names `--executor=firstmate` as the third, opt-in executor, states
that it launches ONE long-lived First-Mate session over the whole
`.oh/tasks/<slug>/` task graph and reaches the same `STATUS: COMPLETE` terminal
interface, and defers its mechanics to `/ship-spec` Stage 10's
"Opt-in (`firstmate`)" subsection — so `/spec execute` composes rather than forks
the build literals, per the file's own Core-principle paragraph. Verified: the
diff is a PURE INSERTION (4 added lines, zero modified/deleted — `grep -c '^-'`
returns 1, the `--- a/...` header), the §2 AUDIT-FAIL loop-back text is
content-identical to HEAD, `spec-family-contract.sh` PASSes, typecheck clean,
564/564 vitest, and `autopilot-executor-toggle` / `ralph-fallback-order` /
`ship-spec-ready-finalization` / `first-mate-charter` are all still green.

### Learnings for future iterations

- **A one-sentence insertion into hard-wrapped prose is only a PURE insertion if
  you append at a paragraph boundary.** My first attempt spliced the sentence
  mid-paragraph before the closing "Use that skill's stage text / as the
  authority..." line, which re-flowed that line and produced a *modified* line
  (`grep -c '^-'` = 2). Appending after the paragraph's final line instead gives
  4 pure `+` lines. US-008's `sandbox-processes.md` AC makes exactly this a
  machine-checked invariant (`grep -c '^-'` must return 1) — build the edit that
  way from the start rather than discovering it at verification time.
- **"Text X is unchanged" ACs mean CONTENT, not line numbers.** Diffing
  `grep -n -A2 'AUDIT-FAIL'` against HEAD reports a false failure the moment any
  insertion above it shifts the line numbers. Drop `-n` (or diff the extracted
  block) to check what the AC actually asserts.
- **`.claude/skills` is a blanket symlink to `../.oh/skills`, so `.claude/...`
  and `.oh/...` are the same file** — confirmed here by `ls -i` returning one
  inode for both `execute.md` paths. Edit and stage the `.oh/` path (that is what
  `git ls-files` tracks); the `.claude/` view updates for free. Same relationship
  already noted for `ship-spec` in US-005's entry.
- **`spec-family-contract.sh` pins the two phrases every `/spec` reference doc
  must keep**: `.oh/tasks/<slug>/` (the folder interface) and
  `AGENTS.md § The Workflow` (the authority), plus a ban on a `^## Handoff`
  heading. My sentence happens to reuse the `.oh/tasks/<slug>/` literal, but any
  future edit to these four reference docs must not remove either phrase or add
  a `## Handoff` section.
- **CHANGELOG is STILL unwritten for this whole feature.** US-001..US-007 have
  now all deferred it (no US-007 AC names it either). US-008 owns the single
  `## [Unreleased]` entry and must cover the ship-spec Stage 10 arm, the
  autopilot arm, AND this `/spec execute` sentence — not just its own
  `sandbox-processes.md` change.
- **Three stories remain and they are the heavy ones**: US-008 (docs + wiki entry
  + raw snapshot + README regen), US-009 (two new probes), US-010 (live per-mode
  smoke + evidence.md + the `PROVISIONAL PENDING US-010` wiki confirmation pass).
  US-010 greps the wiki entry US-008 writes, so the literal marker string must be
  spelled exactly.

---
