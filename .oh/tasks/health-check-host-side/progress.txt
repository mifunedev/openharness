PROGRESS — health-check-host-side (issue #762)
branch task/762-health-check-host-side · base development @ c77ec642

Terminal interface: the whole line `STATUS: COMPLETE` marks the task graph done.

## Task graph

US-001 scope-preflight.sh            deps: —                 status: completed  passes: true
US-002 SKILL.md branch + host-only   deps: US-001            status: completed  passes: true
US-003 behavioural probe             deps: US-001, US-002    status: completed  passes: true
US-004 scope claims aligned          deps: US-002            status: completed  passes: true

Serial chain US-001 → US-002 → {US-003, US-004}. US-003 and US-004 are
independent of each other once US-002 lands.

## Log

### Plan — 2026-08-12
- Observed the real failure in this container: no /var/run/docker.sock,
  `docker ps` and `docker system df` both fail with the connection error,
  `command -v docker` returns /usr/bin/docker. The CLI is present; only the
  socket is gone. Detection must test the socket.
- Counted the blast radius: nine Docker calls across steps 2, 5 and 6 — nine
  copies of the same error for one invocation.
- Verified the provider surfaces are symlinks (`readlink .claude/skills` ->
  ../.oh/skills, same for .pi and .codex), so one file edit serves every
  surface. No per-surface copies to keep in sync.
- Found three existing probes that constrain the rewrite: docker-stats string,
  locked-append line, dispatcher kept-list. Recorded in prd.md.
- Found /docker-disk-cleanup at SKILL.md:72 is a dangling reference — the skill
  does not exist anywhere in the repo.
- Decision recorded in prd.md: Option 2's mechanism carrying Option 1's
  authority. Literal Option 1 is unreachable — there is no host-side agent
  runtime, and #756 exists to keep the container from reaching host state.
- Wrote prd.md and prd.json.

### Critique — 2026-08-12
- Two adversarial critics (implementer lens, operator lens) ran in parallel.
  Both APPROVE-WITH-CHANGES; 14 findings. 13 accepted, 1 amended, 1 rejected.
  Full disposition in critique.md.
- Re-verified every load-bearing claim myself rather than taking the reports at
  face value. All five checked out, including two that corrected me:
  - memory-log-locked-append.sh asserts TWO things about this skill
    (AUDIT_RUN_ID at line 16-18 AND the locked-append line at 19). My
    regression table named one.
  - My rationale for rejecting Option 1 was WRONG. CLAUDE.md -> AGENTS.md is a
    symlink, and root AGENTS.md defines the orchestrator: an agent at the host
    project root with docker/compose permissions that already lists this skill.
    There IS a host-side agent role. Design improved: the relocated procedure
    now has a named addressee that already has the repo checked out.
- Accepted the two strongest findings:
  - `[ -S path ]` alone cannot tell a live daemon from an OOM-killed one or a
    chmod-000 socket. Either would be called `available` and reproduce the
    nine-failure wall behind a passing preflight. `available` now requires a
    completed round-trip.
  - Labelling metric scope does NOT stop the skill printing a green Disk row
    beside a `docker compose build` target. A label explains a number; it does
    not withdraw a conclusion. The verdict now renders N/A and refuses.
- Dropped the `unverified` state: nothing forced it to ever be resolved.
- Rejected `deferred` for the audit-full slice (a rerun cannot recover it) in
  favour of `partial`. Rejected the prose-only alternative.

### Build — 2026-08-12
- US-001 `.oh/skills/health-check/scripts/scope-preflight.sh`. Verified live in
  this socket-less container: DOCKER_TRIAGE=host-only, exactly one notice,
  rc=0, endpoint resolved through `docker context inspect` (which works with no
  daemon — verified, rc=0). Six branches exercised with a docker shim.
- US-002 SKILL.md rewritten: step 0 + three-way branch, KEY=VALUE-is-not-the-
  report rule, host-only markers on steps 2/5/7 + the ladder, the "Host-side
  Docker triage" section addressed to the orchestrator with the round trip
  specified in both directions and an UNKNOWN fallback, the withholding rule on
  the Disk row, scope-labelled step 1, the corrected root-overlay claim, and the
  dangling /docker-disk-cleanup pointer removed.
- US-003 `.oh/evals/probes/health-check-socket-degrade.sh`, 9 assertions across
  6 arms. Two defects found in my own TEST method before trusting it: the shim
  logged `-H`'s value instead of the subcommand (so an assertion would have
  passed for the wrong reason), and the absent-CLI arm broke its own PATH so it
  silently skipped. Both fixed.
- Verified by rejection with attribution: 11 mutations, each shown to fire its
  OWN named assertion, not merely to turn the probe red. Baseline green before
  and after; both files restored byte-identical.
- US-004 AGENTS.md (verified live through the CLAUDE.md symlink),
  .oh/templates/AGENTS.md, frontmatter description AND TRIGGER,
  .oh/skills/audit/references/full.md (partial, not deferred), CHANGELOG.md,
  and `.oh/scripts/link-providers.sh` required_execs so boot validation catches
  a missing or non-executable preflight. `link-providers.sh --check` rc=0.
- Full suite: 103 probes, zero REGRESSION/ERROR. Diff vs the pre-change
  scoreboard is exactly one added row. The 3 SKIPPED are pre-existing.

### Audit + PR — 2026-08-13
- PR #764 opened against upstream development (branches live on upstream, not the
  fork — verified from the head-repo of the 5 most recent PRs).
- /audit pr run 1 (audit-20260813T055733Z-263366): PR-AUDIT-BLOCKED, and correctly
  so — #760 had landed on development after my merge, conflicting on exactly the
  two files the briefing predicted. CHANGELOG auto-merged with both entries intact;
  RESULTS.md REGENERATED via /eval against the merged base, never hand-resolved.
  #760's own probe confirmed still green here.
- /audit pr run 2 (audit-20260813T060219Z-296946): PR-AUDIT-PROMOTABLE.
  promotable=true, evidenceComplete=true, MERGEABLE/CLEAN, CI 4/4 pass.
- title-convention flag is non-gating and pre-existing: pr-classify.sh:72 tests
  ^FROM .+ TO .+$, which no PR in this repo matches. promotable=($rfr or $rtm) and
  never reads flags. Left alone deliberately — worth its own ticket.
- evidence.md written per audit/references/reviewer-evidence-doc.md: observed
  commands and real output only, correlated to the run id and verbatim verdict,
  with the diff-correctness gap stated rather than papered over.

STATUS: COMPLETE
