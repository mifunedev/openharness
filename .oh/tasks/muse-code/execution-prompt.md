Implement https://github.com/mifunedev/openharness/issues/952 with the operator's clarification: the ticket is outdated. Add first-class support to `oh harness install muse-code` through the current install-only lifecycle. `oh init` is retired. This clarification supersedes all retired lifecycle requirements below: do not restore install.museCode, persisted intent, boot provisioning, optional catalog kinds, or oh init. Preserve binaries and auth through the existing home volume; fresh homes require the install verb. Create the standard skills surface through the canonical linker used by sandbox bootstrap.

Read cumulative AGENTS.md, applicable canonical skills, and directory READMEs. Preserve unrelated work and use an isolated feature worktree. Follow the spec workflow; the operator has authorized generating and executing this prompt within issue scope. Keep one implementation owner. First delegate the issue's three independent read-only explorations: official Muse upstream behavior, harness/config/provision lifecycle, and provider/context/init surfaces. Reconcile all findings yourself.

Verify official current installer source and docs: exact command, prefix and profile controls, platform requirements, binary/version, auth/config locations, login, META_API_KEY, AGENTS.md semantics, and Agent Skills discovery. Do not invent upstream flags or substitute mocked tests for real upstream evidence.

Before code changes, score the five approaches in the issue against all ten criteria, record the architecture decision and rejected alternatives, and write a plan mapping every acceptance item to files and validation. Mark every required repository surface applied or not applicable. Prefer optional muse-code plus install.museCode and .agents/skills when evidence supports it.

Implement through the existing catalog, config schema/env mapping, generic install/provision machinery and persistent sandbox home. Install as sandbox, verify muse --version, preserve idempotence and stopped-sandbox intent, and control profile mutations. Create/repair .agents/skills -> ../.oh/skills through both link-providers.sh and oh init. Preserve canonical AGENTS.md and .oh/skills ownership. Respect all issue non-goals; justify any necessary deviation with upstream evidence.

Add meaningful coverage for catalog classification, config round trips, live/already-installed/stopped install intent, enabled/disabled fresh-home provisioning, and both provider initialization paths. Document install, verified auth modes and existing secret handling, context/skills, verification, persistence/recreation, disable/uninstall, and upstream limitations. Update appropriate overview/configuration/installation/quickstart docs and changelog.

Run relevant tests and required eval/CI gates, repair regressions, and record commands and observed outputs. Exercise the real installer in a disposable home without changing operator credentials or profiles. Prove the installation prefix and profile behavior, repeat-install safety, and generic restoration. Exercise real context/skill discovery where practical. Use isolated facilities for recreation; do not destroy or restart the active workspace. Distinguish real recreation, filesystem persistence checks, mocks, and unavailable authenticated verification.

Review the final diff against every acceptance checkbox. Commit evidence covering benefit, requested and actual behavior, divergence, and unverified items. Open/update the issue-linked PR against the correct remote/base and verify CI for its exact head. Mark Ready only when every applicable acceptance item and required gate is supported; otherwise leave it draft with concrete blockers. Do not merge. Return this prompt's location, PR URL/status, validation results, and remaining gaps. Continue authorized work without redundant confirmation.
