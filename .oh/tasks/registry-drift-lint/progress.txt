# progress — registry-drift-lint (issue #758)
# Branch: task/758-registry-drift-lint   Base: development (upstream = mifunedev/openharness)
# Advisor: First Mate (.oh/prompts/advisor/pr.yml). A delegate is not DONE until it has
# written its entry here AND its user story is validated against acceptanceCriteria.
# Registry under test: mifunedev/skills @ master, read-only clone at
#   .oh/worktrees/project/mifunedev/skills   (outside this worktree, never in the diff)

## Dependency graph

  US-001 linter ──┬── US-002 contract+allow ──┬── US-003 tests ── US-006 historical proof ──┐
                  │                            └── US-005 sweep ────────────────────────────┤
                  └── US-004 probe ────────────────────────────────────────────────────────┴── gates → PR

  wave 1: US-001
  wave 2: US-002, US-004      (both depend only on US-001)
  wave 3: US-003, US-005      (depend on US-001 + US-002)
  wave 4: US-006              (advisor runs directly; verification gate)
  wave 5: /eval, /audit pr, evidence.md, /retro, ready PR

## Entries

[advisor] plan — explored the problem against the real registry, not the issue text alone.
  Confirmed 18 skill folders on master. Confirmed the merged `ste` copy still carries 7 `.oh/`
  references, so a zero-tolerance rule is red on day one and needs a reasoned exception model.
  Confirmed registry commit 036a53f still carries all three historical defects, which makes it
  a real regression fixture rather than a synthetic one. Wrote prd.md + prd.json (6 stories).

[advisor] architect memo — solution shape reviewed. Adopted: the ALLOW/KNOWN two-class
  exception split, stale-entry detection, and the DANGLING-REF rule. Rejected its proposed
  home (a fourth `/sync` subcommand): `.oh/skills/sync/scripts/` is in neither the vitest
  include glob nor the CI shellcheck argument list, so that placement would have traded away
  every automated gate. `.oh/scripts/` is in both. The memo named the same objection as its own
  leading risk. Rejected its `_`-for-`-` manifest key encoding as unnecessary: the measured
  suppression rules mean no exception entry needs to name a retired-vocabulary folder.

[advisor] graph — dependency graph above recorded as the tracked todo list, delegates assigned
  per story (`delegate` + `complexity` + `thinking` fields in prd.json).

[advisor] critique — 2 adversarial critics dispatched (implementer lens, user/reviewer lens).

[advisor] oracle — built an independent expectation set before any delegate ran, so delegate
  output is validated against observed ground truth rather than against a delegate's own report.
  Both trees scanned: 18 skill folders, 31 files.

  registry @ master        -> 13 raw findings across 4 skills
  registry @ 036a53f       -> 15 raw findings; includes all three historical defects at
                              SKILL.md:102 (/caveman), references/rules.md:420 (/caveman),
                              SKILL.md:285 (.oh/skills/retro/references/memory-protocol.md)

  Load-bearing check confirmed: every skill with a finding on master is named
  ste, reflect, ship-spec, or harness-context. None is a retired-vocabulary token, so no
  exception entry and no sweep line needs to name one. The architect's `_`-encoding workaround
  is therefore unnecessary and is not adopted.

[advisor] classification decided (First Mate owns this call; delegates implement it):

  ALLOW  ste/references/rules.md   .oh/scripts/ralph.sh          example prose, teaches path style (141, 371)
  ALLOW  ste/references/rules.md   .oh/crons/autopilot.md        example prose, teaches path style (517)
  ALLOW  ste/SKILL.md              .oh/scripts/oh-path           inside `if [ -x ... ]` guard (260, 261)
  ALLOW  ste/SKILL.md              .oh/scripts/locked-append.sh  inside `if [ -x ... ]` guard (260, 263)
  ALLOW  harness-context/SKILL.md  scripts/README.md             names the harness repo layout, not a sibling file (23)
  KNOWN  ste/SKILL.md              /retro                        registry ships reflect, not retro (280)
  KNOWN  ste/SKILL.md              .oh/skills/retro/...          bare cross-repo pointer, no fallback (282)
  KNOWN  reflect/SKILL.md          /update-config                skill absent from the registry (148)
  KNOWN  ship-spec/SKILL.md        scripts/ralph.sh              harness script, absent from the published folder (340)

  5 ALLOW + 4 KNOWN. master must exit 0 and print KNOWN: 4.

[advisor] plan defect found by the advisor, ahead of the critics — US-006 runs the linter
  against 036a53f while the exception list is written against master. The KNOWN entry keying on
  `.oh/skills/retro/references/memory-protocol.md` matches that text at 036a53f line 285 too, so
  the production list would suppress historical defect 3 and silently weaken the proof.
  Fix: US-006 runs with `--allow` pointed at a fixture holding an empty allow block. No
  fail-open switch is added.
  SUPERSEDED below — measurement produced a better fix than the workaround all three of us
  proposed.

[advisor] critique returned — both critics reported. 19 findings adjudicated in critique.md.
  4 blocking, all accepted and folded in:
    B-HIGH1  KNOWN suppressed the exit code, so the check was green while a live defect stood.
             KNOWN no longer touches the exit code; only ALLOW suppresses.
    A-HIGH1  Token-keyed exceptions re-admitted historical defect 3 (the human repair kept the
             .oh/ path and only made the sentence conditional).
    A-HIGH3  Zero scanned files exited 0 — an unclosed fail-open, plus two Bash mechanics that
             produce the same silence (process substitution under set -e; array in a subshell).
    B-HIGH3  The check had no invocation site at all. New story US-007 wires it into
             .oh/skills/builder/references/skill.md.
  2 partial rejections, recorded with reasons: DANGLING-REF kept (re-measurement showed 4 true
  positives, not 1); the registry-side pointer declined for scope, with the cost recorded.

[advisor] better fix found for A-HIGH1 / B-HIGH2 than the workaround. Keying an exception on a
  hash of the WHOLE source line separates the repair from the defect outright:
      master  skills/ste/SKILL.md:282 -> 01ede414c12b
      036a53f skills/ste/SKILL.md:285 -> 27bf235ad29e
  So the SHIPPED exception list still reports all three historical defects at 036a53f. US-006
  now proves rejection against the real production configuration instead of a weakened one.
  The empty-allow-block fixture is no longer needed; the linter still accepts one, as a
  distinct criterion. Hash invariant verified identical in bash and Python before briefing.

[advisor] packaging defect caught by critic A — .oh/manifest.json ships scripts/** and excludes
  docs/**. A fail-closed script whose exception file lives in .oh/docs/ would be a permanent
  hard error for every installed harness. Contract moved to .oh/scripts/registry-portability.md,
  indexed from .oh/docs/README.md.

[advisor] day-one state measured, not predicted. Registry master 1d11ab6: 18 folders, 31 files,
  14 findings -> 8 ALLOW, 5 KNOWN, 1 line carrying two tokens under one hash. The check exits 1
  on live master. That is correct: the registry does carry portability defects today, criterion 5
  forbids repairing them here, and a green check would be a lie.

[advisor] wave 1 dispatched — 4 workers, non-overlapping files, each briefed with the ground
  truth it must reproduce rather than left to invent it:
    US-001 .oh/scripts/registry-portability.sh          general-purpose, high
    US-002 .oh/scripts/registry-portability.md + docs index   general-purpose, medium
    US-004 .oh/evals/probes/registry-portability.sh     general-purpose, medium
    US-007 .oh/skills/builder/references/skill.md       general-purpose, low

[US-004] probe returned. Advisor re-verified independently rather than accepting the report:
  file present and executable, the three required headers in order, `bash -n` clean,
  shellcheck -S warning clean, no retired-vocabulary token, and an unarmed run prints SKIPPED
  and exits 2. Worker chose tier A with a reason the advisor accepts — this suite has no B tier
  (.oh/evals/README.md defines `A | ablation`), and opt-in-ness is carried by the exit-2 SKIPPED
  state, which is how cc-safety-net-wiring and oh-standalone-lifecycle already do it.
  Worker also proved, by echoing the stub's argv, that neither --strict-exceptions nor --allow
  is passed, and that an unexpected exit code maps to 2 and never to 0.
  10 of 11 acceptanceCriteria verified. The 11th (`bash .oh/skills/eval/run.sh` still exits 0)
  is a suite-level gate, deferred to the final gate run so it is measured once, after every
  worker has landed. passes stays false until then.

[US-007] publishing gate returned. Advisor re-verified: the diff is 15 insertions and 0
  deletions across two files, so nothing existing was removed or contradicted; both referenced
  paths are correct; no retired-vocabulary token. The worker reported that its first attempt
  renumbered an existing table-of-contents line, which git recorded as a deletion, and that it
  re-placed the section to keep the diff purely additive. That is the right call and it is the
  kind of detail a worker usually hides. 7 of 7 acceptanceCriteria verified.

[US-002] contract returned. Advisor re-verified against its own oracle rather than the report:
  exactly one fenced `allow` block; the extracted block is byte-identical to the advisor's
  authoritative entry list (`diff` clean); 8 ALLOW and 5 KNOWN; every entry has 5 fields and a
  well-formed 12-hex hash; no retired-vocabulary token in either touched file. The index entry
  in .oh/docs/README.md resolves.
  The worker reported that its own validation script first printed 13 false `BAD hash` lines
  because awk lacks interval quantifiers, then rewrote the check and cross-proved the block with
  a diff. Self-caught false alarm, disclosed rather than buried — the correction is sound.
  18 of 19 acceptanceCriteria verified. The 19th (the guarding probe exits 0) is repo-wide and
  is deferred to the final gate run, after the artifacts are force-added.
