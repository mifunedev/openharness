# progress
US-009 (2026-06-27): name-collision check PASS — `ls .mifune/skills/ | grep -E '^(weigh|rlm)$'` returns only the two new skills (no pre-existing dir of either name); neither `weigh` nor `rlm` shadows a Claude Code built-in slash command (help/clear/config/model/mcp/compact/init/review/run/loop/verify/...) nor a Pi package (pi-loop, pi-tasks, pi-recap, pi-fff, pi-messenger-bridge). No collision.
