# OpenHarness — secrets & runtime (never commit).
# harness.yaml was removed in 0.4.0; these stay one more release so a stale
# local artifact is never accidentally committed.
/harness.yaml
/harness.yml
/harness.yaml.migrated
/.env
!/.env.example
.claude/.env.claude
.hermes/.env
.hermes/auth.json
.oh/cli/dist/
.oh/cli/node_modules/
# Worktree and project-clone roots — only the directory guide is tracked.
.worktrees/*
!.worktrees/AGENTS.md
!.worktrees/CLAUDE.md
projects/*
!projects/AGENTS.md
!projects/CLAUDE.md
