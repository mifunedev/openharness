# Changelog

All notable changes to this project are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versions use CalVer (`YYYY.M.D` with `-N` suffix for same-day releases) and match git tags.

Update policy and release automation live in [`/git`](.claude/skills/git/SKILL.md) § Changelog.

## [Unreleased]

### Fixed
- **Equipped-project sandbox no longer crash-loops on boot.** `link-providers.sh` hard-required a git checkout (`git rev-parse --show-toplevel`), so an `oh init`-equipped project that wasn't a git repo failed the entrypoint's provider-linking step (`entrypoint.sh` `exit 1`) and the container restart-looped under `restart: unless-stopped`. It now falls back to `${OH_PROJECT_ROOT:-$PWD}` when not in a git checkout, guarded by a `.oh/skills` existence check; behavior inside a real git checkout is unchanged.

### Added
- **`.oh/scripts/get-oh.sh` — a curl|bash installer for the standalone `oh` CLI.** Closes the gap where `oh init` needs the `oh` binary but there is no host-side way to get it (the CLI is a private, unpublished npm package; `install.sh` only builds `oh` inside the container). Installs the single self-contained `oh` binary to `~/.local/bin/oh` (`OH_BIN_DIR`-overridable) — **no repo clone**, and it never touches an existing `~/.openharness` sandbox. Prefers a prebuilt bundle (`oh.mifune.dev/oh.js`, published by the openharness-web Pages build) and falls back to building from source in a temp dir (`OH_JS_URL`/`OH_GITHUB_REPO`/`OH_GITHUB_REF` overridable). When Node.js ≥ 20 is missing it offers to install nvm + Node 22 and sources it so `oh` works in the same shell (`--yes`/`--no`, `OH_NVM_VERSION`). `oh init` fetches its scaffold payload on demand (a temp clone it deletes). Served at `oh.mifune.dev/get-oh.sh` (openharness-web GitHub Pages sync); documented in README, `.oh/docs/quickstart.md`, and `.oh/docs/installation.md` (each curl|bash paired with a review-first alternative).
- Add ADR-0001 documenting #532's standards-scope disposition: the lightweight RFC/ADR convention is sufficient for now, while document taxonomy, registries, lifecycle, and conformance machinery stay deferred until a concrete future need appears ([#532](https://github.com/mifunedev/openharness/issues/532)).
- Add a descriptive, example-only `.oh/harness.yml` manifest reference that points at today's `.oh/agents/`, `.oh/skills/`, `.oh/hooks/`, `.oh/crons/`, `.oh/scripts/ralph.sh`, and `.oh/tasks/` surfaces without defining a schema, registry, or conformance profile ([#532](https://github.com/mifunedev/openharness/issues/532)).
- Add `.oh/docs/rfcs/rfc-selfimprove-roadmap.md`, a curated #525 self-improving-harness roadmap that decomposes the broad epic into dependency-ordered proposed child issues for human filing; linked from the docs and RFC indexes ([#525](https://github.com/mifunedev/openharness/issues/525)).
- Add `.oh/docs/rfcs/rfc-trace-ledger.md`, a #525 foundational trace/event ledger RFC documenting the append-only normalized event model, proposed `.oh/traces/` and `.oh/sessions/` storage, redaction rules, replay/diagnosis/scoring event set, and JSONL example ([#525](https://github.com/mifunedev/openharness/issues/525)).
- Wiki entry `oh-cli-portable-lifecycle` documenting the standalone `oh` CLI lifecycle — payload precedence (`--from` > `--from-remote` > bundled > auto-fallback), remote-fetch limits, and the `oh sandbox`/`oh shell`/`oh gateway` verbs — plus its corpus README index row; `.oh/docs/installation.md` gains a "Standalone CLI (`oh`)" section stating the Node ≥18 + git + docker host prerequisites for this path (with a quickstart cross-link), without changing the clone-and-own no-Node pitch ([#564](https://github.com/mifunedev/openharness/issues/564)).
- `oh-standalone-lifecycle` eval probe guarding the lifecycle-verb registrations (`sandbox`/`shell`/`gateway` + `--from-remote`), the no-stale-`#531`-marker invariant under `.oh/cli/src/`, and the `rewriteComposeForTarget` → `/home/sandbox/project` consumer mapping; plus a unit assertion that the generated consumer `.devcontainer/docker-compose.yml` contains `/home/sandbox/project` and no `/home/sandbox/harness` path ([#564](https://github.com/mifunedev/openharness/issues/564)).
- **`oh sandbox`, `oh shell [container]`, and `oh gateway <args…>` lifecycle verbs** — thin argv-array wrappers over the vendored `.oh/scripts/` (`docker-compose.sh --repo-dir <root> up -d --build`; `docker exec -it -u sandbox <name> zsh` with `sandbox.name` read via `harness-config.sh get` and an explicit `<root>/harness.yaml` path; `gateway.sh` with `OH_PROJECT_ROOT` set), sharing a new equipped-project-root resolver (`lib/project.ts`) that works from any subdirectory; `oh sandbox` defensively seeds `harness.yaml` from `harness.yaml.example` (parity with `make harness-config`) ([#564](https://github.com/mifunedev/openharness/issues/564)).
- Add `.oh/docs/security-considerations.md` documenting the security boundaries the harness already enforces today — each path-cited to its real mechanism (gitignored env secrets + template allowlist, the secret-exposure command/path hooks + the `-F <file>` safe pattern, devcontainer sandbox isolation + the Docker-socket caveat, the human merge / no-auto-merge gate, the autopilot owned-surface guard, and the untrusted-model-output posture) and labelled ENFORCED vs RECOMMENDED; linked from the docs index ([#568](https://github.com/mifunedev/openharness/issues/568)).
- Add a lightweight RFC/ADR index and convention at [`.oh/docs/rfcs/README.md`](.oh/docs/rfcs/README.md), linked from the docs index: a proposal is a `RFC:`/`ADR:`-titled GitHub issue moving through a minimal Draft → Accepted → Superseded lifecycle, seeded with #531/#525/#532 as first entries. Framed as a convention, not a standards body; the full taxonomy/registries/conformance scope stays deferred to [#532](https://github.com/mifunedev/openharness/issues/532) ([#567](https://github.com/mifunedev/openharness/issues/567)).
- Add an agent-browser X.com login snippet for dashboard-observed human login, persistent profiles, 2FA/CAPTCHA handling, Google OAuth fallback, and credential safety.
- Add a Railway one-click hosted smoke deploy path from the README, with config-as-code, deploy assets, docs, and an eval drift guard ([#553](https://github.com/mifunedev/openharness/issues/553)).
- **`OH_PROJECT_ROOT` project-root seam** (RFC #531 Phase 1) — single source of truth for the container workspace path (default `/home/sandbox/harness`); `HARNESS` is kept as a back-compat alias. `evals/probes/project-root-seam.sh` guards the seam contract against regression ([#531](https://github.com/mifunedev/openharness/issues/531)).
- **`oh init [dir]` scaffolds a fresh harness checkout** from a new `.oh/templates/` payload source (`harness.yaml`, `AGENTS.md`, `gitignore`, and a `.devcontainer/devcontainer.json` whose `workspaceFolder` is pinned to `/home/sandbox/project`), with a `--templates <dir>` escape hatch to point at an alternate source. `runInit` is exported from `.oh/cli/src/commands/init.ts` and dispatched from `cli.ts` (with help text). Guarded by the new `oh-init-scaffold` eval probe ([#531](https://github.com/mifunedev/openharness/issues/531)).
- Add `oh update` CLI command (RFC #531 Phase 3): upgrades only the `.oh/` control plane of an OpenHarness-equipped repo from a `--from <checkout>` source, version-gated and path-escape-guarded so project source stays untouched; remote-fetch deferred ([#531](https://github.com/mifunedev/openharness/issues/531)).
- Add `.oh/manifest.json` payload manifest honored by `oh update` (RFC #531 follow-on): `oh update` now overlays a declared allowlist of `.oh/` content and no longer vendors `.oh/docs/` or `.oh/patches/` into equipped repos; sources with no manifest fall back to legacy overlay-all. Plus a guard probe keeping shipped `.oh` scripts' upstream-repo references overridable ([#531](https://github.com/mifunedev/openharness/issues/531)).
- **Recursive Language Models (RLM) integrated as two harness-owned skills** — `/weigh` (weighted-trajectory selection over the Workflow tool, backed by a pure, version-controlled, deterministic-first scorer at `.oh/skills/weigh/scripts/score-trajectories.mjs` with frozen `DEFAULT_WEIGHTS` and a hard eligibility floor that never makes a silent least-bad pick) and `/rlm` (context-as-environment decomposition that addresses large artifacts via `query-context.mjs` without ingesting them, then recurses sub-agents under a bounded depth/children/step budget, reusing `.oh/scripts/ralph.sh` + `.worktrees/` by reference). Adds an opt-in, default-off self-consistency weighting seam to `/critique`, and the `weigh-scorer-contract` and `rlm-context-budget` eval probes ([#533](https://github.com/mifunedev/openharness/issues/533)).
- **`oh init`/`oh update` learn `--from-remote [--ref <ref>]`** — materialize the OpenHarness payload from a shallow `git clone` of the public repo (temp checkout under `os.tmpdir()`, always removed in a `try/finally` wrapping the whole run) instead of requiring a local built checkout. `--from-remote` supplies BOTH the vendor payload and the scaffold templates from the fetched checkout (unlike `--from`, which sets only the payload source); `oh init` with no source flags auto-falls back to the remote fetch when the CLI's bundled payload is absent (one-line notice naming the URL + ref), every remote fetch prints `fetched payload v<X> (installed CLI v<Y>)` for version-skew visibility, and the fetched checkout flows through the existing manifest + path-guarded vendor walk. Retires the stale `#531` deferral markers in CLI help/error text (installed-binary payload bundling stays gated on publishing) ([#564](https://github.com/mifunedev/openharness/issues/564)).
- Add `.oh/docs/oh-directory-layout.md`, a descriptive map of the `.oh/` control plane — every real top-level entry with its purpose and canonical consumer, `config.json`/`patches/` flagged as not-in-a-fresh-clone, and the OH-RFC-0003 normalized dirs labelled "proposed, not present"; linked from the docs index and reconciled with (not duplicating) `.oh/README.md` ([#566](https://github.com/mifunedev/openharness/issues/566)).
- Add a descriptive [`.oh/docs/glossary.md`](.oh/docs/glossary.md) defining Open Harness's core vocabulary (harness, agent, runtime, loop, policy, tool, capability, eval, session, trace, checkpoint, artifact, terminal state, skill, primitive, orchestrator, sandbox, worktree) as the repo uses each term, with a canonical source pointer per entry; linked from the docs index ([#565](https://github.com/mifunedev/openharness/issues/565)).
### Changed
- Improve `/health-check` performance by caching baseline snapshots, gating verbose Docker/process probes, and reusing one-pass diagnostics for ranked reclaim reports ([#576](https://github.com/mifunedev/openharness/issues/576)).
- Resolve the #532 RFC-index disposition as accepted-lightweight, cross-linking ADR-0001 and keeping the heavier standards-body scope deferred until a concrete future issue needs it ([#532](https://github.com/mifunedev/openharness/issues/532)).
- Clarify the glossary's model / agent CLI / harness / loop / policy / trace layer separation without introducing conformance machinery ([#532](https://github.com/mifunedev/openharness/issues/532)).
- Clarify the #525 docs/RFC indexes so the roadmap curation and trace/event ledger RFC are discoverable as the draft epic's human-filed child-issue map and foundation spec ([#525](https://github.com/mifunedev/openharness/issues/525)).
- `oh init` full-scaffold "Next steps" now recommends `oh sandbox` (build/start) and `oh shell` (connect), keeping the raw `docker compose` command only as a parenthetical fallback; retired the two stale `#531` devcontainer-templating comment lines from `.devcontainer/Dockerfile` (comment-only diff — path values unchanged) ([#564](https://github.com/mifunedev/openharness/issues/564)).
- **Replace the `/advisor` skill with an `advisor` agent.** The advisor→executor delegation-briefing pattern (formerly `.oh/skills/advisor/SKILL.md` + `references/recursive-delegation.md`) is now a single read-only sub-agent at `.oh/agents/advisor.md` (`model: opus`, `tools: Read, Glob, Grep, Bash`). Because sub-agents cannot nest, the agent's contract is to synthesize the tight 5-field briefing (and, when the task decomposes, a bounded recursive-decomposition plan) and **return** it — the caller performs the `Agent`/ralph handoff. Repointed every reference to the deleted skill in lockstep: `.oh/scripts/link-providers.sh` + `.claude/protected-paths.txt` (vendored/protected pack lists), the `advisor-monitored-loop` + `skills-vendored` eval probes and the `context-audit` recursion probe, the `/pr-audit` · `/rlm` · `/delegate` · `/harness-context` · `/ship-spec` · `/retro` skill docs, and `AGENTS.md`.
- Treat root `harness.yaml` as local gitignored state generated from tracked `harness.yaml.example`; `make harness-config` creates it on demand and `make sandbox` runs that guard before building.
- **Consolidate the harness's own devcontainer back into the conventional root `.devcontainer/`**, superseding the earlier `.oh/devcontainer/` relocation in this same unreleased cycle. All six build/bootstrap assets (`Dockerfile`, `docker-compose.yml` + the hermes-dashboard overlay, `entrypoint.sh`, `client-slack-supervise.sh`, `seed-msg-bridge.sh`) now sit alongside a hand-maintained `devcontainer.json` (its `dockerComposeFile` points at the same-dir compose) in `.devcontainer/` — one conventional location, no split, no compat shim. Retires the `sync-devcontainer.sh` compat generator, simplifies the `oh init` devcontainer copy (source is the sibling `.devcontainer/`; only the consumer `/home/sandbox/harness` → `/home/sandbox/project` workspace rewrite remains), drops the whole-dir `.devcontainer/` `.dockerignore` exclusion so the entrypoint `COPY` resolves (env secrets still excluded via `**/.env*`), and repoints the CI boot-lint/hadolint globs, the `.oh/scripts` lifecycle wrappers, the `project-root-seam` / `boot-lint-glob` / `cron-watchdog` / `sandbox-boot-guard-ci` probes, and the docs. The `oh-devcontainer-restructure` eval probe is inverted to guard the consolidated layout.
- **Relocate the Ralph/spec task workdirs from repo-root `tasks/` to `.oh/tasks/`** (RFC #531 follow-on) — reclassified from repo-root "live identity/state" to `.oh/` machinery (ephemeral build scratch). No back-compat root symlink is kept — the **git-mutating** consumers (the `cleanup-tasks` cron, `.oh/scripts/ralph.sh`, the eval probes) are repointed to the real `.oh/tasks/` path because git index operations cannot traverse a symlink, and nothing reads the bare `tasks/` path. The `.oh/README.md` and `docs/roadmap.md` namespace-taxonomy docs are amended to reclassify `tasks/` accordingly. Paired with a `ryaneggz/mifune` submodule PR repointing the skill/agent references, with the `.mifune` gitlink bumped to match ([#531](https://github.com/mifunedev/openharness/issues/531)).
- **Relocate the Ralph/spec cron definitions from `crons/` to `.oh/crons/`** so the scheduled-agent surface joins the `.oh/` machinery namespace, with **no** back-compat root symlink — every consumer was repointed to the real `.oh/crons/` path: `cron-runtime.ts` reads, the `locked-append.sh` liveness logs, the cron bodies, the eval probes, and the `CRONS_DIR` default itself (`.oh/crons`) in `docker-compose.yml`/`entrypoint.sh`/`cron-runtime.ts`; `.gitignore`/`protected-paths.txt` entries moved with the dir, and the governing principle (`.oh/README.md`, `docs/roadmap.md`) reclassifies `crons/` as machinery. Paired with a `.mifune` submodule PR repointing cron-path references in the skills.
- **Relocate the fitness-function eval suite from `evals/` to `.oh/evals/`** so the eval probes, capability benchmark, datasets, and `RESULTS.md` join the `.oh/` machinery namespace, with **no** back-compat root symlink (consumers repointed directly). Because the move is one directory level deeper, the depth-coupled resolvers were repointed in lockstep: the eval runner `run.sh` walk-up, every probe's `ROOT="$(… /../../..)"` (68 probes), and the `repo-orientation-benchmark-score.mjs` scorer's `repoRoot`. Also scoped `oh-shipped-repo-overridable` to exclude the (non-shipped) eval probes, trimmed `context/REPO_MAP.md` back under its 12288-byte budget, and reclassified `evals/` as machinery in the governing principle (`.oh/README.md`, `docs/roadmap.md`). Historical dataset oracles and CHANGELOG entries were left intact. Paired with a `.mifune` submodule PR repointing eval-path references in the skills.
- **Relocate the harness's long-term memory + session logs from `memory/` to `.oh/memory/`** so the memory surface joins the `.oh/` machinery namespace, with **no** back-compat root symlink (consumers repointed directly). The tracked `MEMORY.md`/`README.md` move with the dir; the gitignored dated `[0-9]*/log.md` logs stay ignored at the new path (`.gitignore` rule moved). Like `crons/`, memory has no git-mutating consumers — `/retro`, the crons, and `autopilot-caps.sh` append via `locked-append.sh` — so references were repointed for consistency (incl. `autopilot-caps.sh`'s `mkdir`/log paths + its test). Prose `memory/<word>` separators ("memory/disk", "memory/identity", "memory/liveness", "memory/doc") were left intact; only real paths were repointed. Governing principle (`.oh/README.md`, `docs/roadmap.md`) reclassifies `memory/` as machinery. Paired with a `.mifune` submodule PR repointing memory-path references in the skills.
- **Relocate the always-on identity core from `context/` to `.oh/context/`** so the session-start identity files (`SOUL.md`, `IDENTITY.md`, `TOOLS.md`, `USER.md`, `REPO_MAP.md`) and the collapsed `context/rules/` provider pointers join the `.oh/` machinery namespace, with **no** back-compat root symlink; `AGENTS.md`/`CLAUDE.md` session-start reads and other references were repointed to the real `.oh/context/` path, with `protected-paths.txt`, the `repo-orientation` benchmark `startupContext.path`, the `repo-map-contract` probe (`REPO_MAP`/`CONTEXT_README`/`cd`), and the `single-line-footer` test updated in lockstep. Prose `context/<word>` (the `harness-context` skill name, "context/model" footer segments) was left intact; the genuine `context/rules/` pointer subdir moved with the dir. Governing principle (`.oh/README.md`, `docs/roadmap.md`) reclassifies `context/` as machinery. Paired with a `.mifune` submodule PR repointing context-path references in the skills.
- Routed the hardcoded `/home/sandbox/harness` container-path literals in the devcontainer layer and `.oh/scripts` through `${OH_PROJECT_ROOT:-/home/sandbox/harness}`, with `HARNESS` kept as a back-compat alias. Zero behavior change — only the indirection is added (Phase 1 of #531 toward `oh init`) ([#531](https://github.com/mifunedev/openharness/issues/531)).
- **Relocate the harness's own devcontainer build assets from `.devcontainer/` to `.oh/devcontainer/`** so the boot machinery is part of the portable `.oh/` control plane. The six build/bootstrap assets (`Dockerfile`, `docker-compose.yml` + the hermes-dashboard overlay, `entrypoint.sh`, `client-slack-supervise.sh`, `seed-msg-bridge.sh`) now live at `.oh/devcontainer/`; root `.devcontainer/` is reduced to a thin generated compat layer — the VS Code `devcontainer.json` (regenerated by `.oh/scripts/sync-devcontainer.sh`) plus the user-owned `.env`. No-behavior-change relocation validated by CI's sandbox boot-guard and a new `oh-devcontainer-restructure` eval probe ([#531](https://github.com/mifunedev/openharness/issues/531)).
- **Absorb the shared primitive pack into `.oh/`, retiring the `.mifune` submodule.** The skills, agents, and hooks (+ `skills.lock`) are now vendored directly under `.oh/skills`, `.oh/agents`, `.oh/hooks` and shipped through the `.oh/manifest.json` payload, so `oh init`/`oh update` lay them down in one shot with **no** submodule fetch — eliminating the split-source footgun where the CLI-vendored `.oh/` and the remote-pinned `.mifune` could diverge (a fresh `oh init` could materialize un-swept skills until the submodule was pushed). Removes `.gitmodules` + the gitlink, retargets the provider symlinks (`.claude`/`.codex`/`.pi` `/skills → ../.oh/skills`, `.claude/agents`, `.claude/hooks`, and the Hermes link), renames `.oh/scripts/ensure-mifune.sh → link-providers.sh` (provider-symlink wiring + vendored-pack validation, no git), repoints `oh init` (`init.ts` drops the submodule-materialize phase), the CI/release path filters and eval invocation, and replaces the `mifune-checkout` probe with `skills-vendored`. Supersedes the #539 submodule-mount approach.
- Move the rendered Docusaurus docs site, assets, and blog archive to `mifunedev/openharness-web`; the core repo now points readers to `docs/README.md`, DeepWiki, and the external site source ([#536](https://github.com/mifunedev/openharness/issues/536)).
- Dedupe Docusaurus-site leftovers after the `openharness-web` move ([#536](https://github.com/mifunedev/openharness/issues/536) follow-up): strip the now-dead Docusaurus-only frontmatter (`sidebar_position`, `slug`, `id`; `title` kept) from the canonical GitHub-readable `docs/**/*.md`, and drop the stale in-repo docs-site references (the `app-docs` tmux session in `docs/intro.md`, the docs-site mentions in `docs/connecting.md`). The probe-guarded `packages/oh/**` boot-guard filter and the labeled legacy/future `.gitignore` package globs are preserved.
- Relocate the GitHub-readable markdown docs from `docs/` to `.oh/docs/`, grouping them with the portable `.oh/` control plane (the rendered Docusaurus site and `blog/` already live in `mifunedev/openharness-web` after #536). The `.oh/docs/` path — formerly the now-removed Docusaurus site builder — is reclaimed for markdown only: `evals/probes/docs-build-fast-path.sh` now forbids Docusaurus *build machinery* under it (a `.oh/docs/package.json` / `docusaurus.config.*` / `sidebars.*`) rather than the directory's existence. Live pointers (the README docs index, `AGENTS.md`, install/devcontainer/codex references, the first-boot banner), the `repo-orientation` capability eval, and `.oh/README.md`'s namespace taxonomy are repointed; the `oh update` payload manifest continues to exclude `.oh/docs/**`, so the docs are not vendored into equipped repos ([#536](https://github.com/mifunedev/openharness/issues/536) follow-up).
- **Default the Advisor-managed build executor to ralph.** The `/spec` / `/ship-spec` Stage 10 build now defaults to the **Monitored async loop** — the Advisor launches `.oh/scripts/ralph.sh` directly and owns the `STATUS: COMPLETE` watch (one loop per task, runnable in parallel across independent tasks) — and `/delegate` becomes an **optional within-iteration** fan-out tool that never replaces the loop. The legacy `/delegate --plan tasks/<slug>/prd.json` worker fan-out is preserved behind `--executor=delegate-advisor` (`/ship-spec`, default `ralph`) and `AUTOPILOT_EXECUTOR=delegate-advisor` / `--executor=delegate-advisor` (`/autopilot`, whose default mode is now `ship-spec`; the inline `ralph` bypass is unchanged). Updated `/ship-spec` Stage 10 + the `--executor` flag, `/spec execute`, `/autopilot`, the `/advisor` Monitored-async-loop variant, the ralph `prompt.md` template (an "Execution model" note), `AGENTS.md`, and the `autopilot-executor-toggle` eval probe in lockstep (ported from [ryaneggz/openharness#338](https://github.com/ryaneggz/openharness/pull/338)).
### Fixed
- Make Hermes gateway startup repeatable by pinning `HERMES_HOME`/`terminal.cwd` to the harness checkout and self-healing Microsoft Teams webhook dependencies.
- Refresh the devcontainer root `pnpm install` gate when package manifest inputs change, preserving fast boot when dependencies are current ([#521](https://github.com/mifunedev/openharness/issues/521)).
- Cron runtime entries now store canonical absolute source file paths, so body hot-reload keeps working after cwd changes instead of logging `BODY_RELOAD_ERR` and falling back to stale cached prompts ([#517](https://github.com/mifunedev/openharness/issues/517)).
- Release publishing now smoke-tests the locally built sandbox image before pushing GHCR tags, ensuring the published release image boots and passes the sandbox healthcheck first ([#515](https://github.com/mifunedev/openharness/issues/515)).
- Remove obsolete top-level `wiki-ingest`, `wiki-query`, and `wiki-lint` skill directories left after the consolidated `/wiki` dispatcher moved their procedures under `.oh/skills/wiki/references/`, preventing Pi startup from reporting `description is required` on reference-only `SKILL.md` files.
### Removed
- Remove the in-repo `.oh/docs` Docusaurus package, docs deploy workflow, docs build scripts, and docs-only pnpm dependency graph from Open Harness core ([#536](https://github.com/mifunedev/openharness/issues/536)).
### Deprecated
### Security

## [2026.6.25-3] - 2026-06-25

### Added
### Changed
### Fixed
### Removed
### Deprecated
### Security
- Patch Dependabot security alerts for `dompurify`, `http-proxy-middleware`, `undici`, and `webpack-dev-server` via pnpm overrides and lockfile refresh ([#527](https://github.com/mifunedev/openharness/issues/527)).

## [2026.6.25-2] - 2026-06-25

### Added
### Changed
### Fixed
- Restore the GitHub Pages docs workflow after the docs app relocation by pointing the build and artifact paths at `.oh/docs` and guarding the path in `docs-build-fast-path`.
### Removed
### Deprecated
### Security

## [2026.6.25] - 2026-06-25

### Added
- `/sync` dispatcher skill codifies `publish`, `catchup`, and `status` for gated origin↔upstream synchronization, including topology docs and the `sync-skill-contract` eval probe ([#526](https://github.com/mifunedev/openharness/pull/526)).
- Forward sync `ryaneggz/openharness:development` → `mifunedev/openharness:development` (2026-06-24): brings in the `auditor` agent, `gateway.sh` client-session launcher, DebugMCP MCP registration, `.oh/` machinery grouping, consolidated `/spec` dispatcher, `/wiki` skill consolidation, `sandbox-boot-guard` CI workflow, `context/REPO_MAP.md`, `repo-map-contract` probe, `spec-family-contract` probe, `sandbox-boot-guard-ci` probe, and `crons/prompt-miner.md`. Cron timezone: `heartbeat` and `cleanup-tasks` normalised to `America/Los_Angeles` (public baseline); `autopilot` stays `America/Denver` (both sides agree). Stale upstream branch `release-prep/sync-ryaneggz-development` should be deleted post-merge ([ryaneggz#327](https://github.com/ryaneggz/openharness/issues/327), and others).
- `/t3` skill starts, inspects, and stops T3 Code (`npx t3`) in a sandbox tmux session, including pairing-URL discovery and log/status helpers ([#509](https://github.com/mifunedev/openharness/issues/509)).
- `/prompt-miner` skill + deterministic `mine-traces.mjs` engine — a cross-session, data-driven cousin of `/retro` that scores Claude+Pi session traces by a friction+ground-truth outcome proxy, ranks the initiating prompts, and mines falsifiable prompt **markers** behind a propose-then-confirm gate; guarded by the `prompt-miner-schema-compat` probe. Ships a **disabled-by-default** (`enabled: false`), cap-gated daily cron ([#503](https://github.com/mifunedev/openharness/issues/503)).
- Default Pi `fff` file-search support: `.pi/settings.json` pins `npm:@ff-labs/pi-fff@0.9.5` (adds the `ffgrep`/`fffind` tools and frecency-ranked `@`-mention autocomplete, backed by the prebuilt-binary `@ff-labs/fff-node` binding — no Rust toolchain needed), `.pi/APPEND_SYSTEM.md` gains a soft "prefer fff when available" hint that keeps native `grep`/`find` as the fallback (not `override` mode), and `docs/integrations/pi-fff.md` + `wiki/pi-fff.md` cover modes, verify steps, the disable path, and troubleshooting ([#499](https://github.com/mifunedev/openharness/issues/499)).
- `autopilot-merged-pr-reference-dedupe` eval probe guards that `/autopilot` skips completed-but-still-open tickets whose development PRs already merged ([#468](https://github.com/mifunedev/openharness/issues/468)).
- `AGENTS.md § The Workflow` codifies the canonical operative path (`select → spec-plan ⇄ spec-critique → spec-execute → merge → reset|clean`) with a single designated runner and the `/ship-spec` all-in-one caveat, guarded by the `workflow-boundaries` eval probe ([#493](https://github.com/mifunedev/openharness/issues/493)).
- The `spec-*` skill family (`/spec-plan`, `/spec-critique`, `/spec-execute`, `/spec-retro`) decomposes `/ship-spec` into independently runnable, folder-pointed workflow nodes, guarded by the `spec-family-contract` eval probe ([#493](https://github.com/mifunedev/openharness/issues/493)).
- The advisor-monitored ralph-loop variant is codified in `context/rules/advisor-model.md` (own-watch + surfaces-blocks + promotable-gate finalize), guarded by the `advisor-monitored-loop` eval probe ([#493](https://github.com/mifunedev/openharness/issues/493)).
- `context/REPO_MAP.md` gives session-start agents a Git-tracked source-map command plus default keep/disregard paths for context loading, guarded by `repo-map-contract` plus the CB-004 repo-orientation A/B benchmark manifest/scorer ([#464](https://github.com/mifunedev/openharness/issues/464)).
- `pi-dynamic-workflows` is now a default project-local Pi package pinned to the upstream `v1.0.1` commit, with docs for the `workflow` tool's deterministic JavaScript fan-out model and package-pin test coverage ([#451](https://github.com/mifunedev/openharness/issues/451)).
- `heartbeat-logging-contract` eval probe guards that heartbeat runs keep structured memory logs and locked liveness appends ([#447](https://github.com/mifunedev/openharness/issues/447)).
- `sandbox-boot-guard` CI workflow validates the sandbox compose configuration and locally builds the devcontainer image for boot-path changes, guarded by the `sandbox-boot-guard-ci` eval probe ([#449](https://github.com/mifunedev/openharness/issues/449)).
- `retro-deterministic-contract` eval probe guards that `/retro` keeps schema-backed output, self-contained helper scripts, and synchronized `.pi`/`.claude` skill copies ([#443](https://github.com/mifunedev/openharness/issues/443)).
### Changed
- Clarify oh.mifune.dev copy — "harness" now consistently means Open Harness (the repo); the CLI you pick is the "agent". The landing page, site tagline/meta, `docs/intro.md`, `docs/installation.md`, and `docs/harnesses/overview.md` now state one-repo-per-sandbox, that the repo tracks/versions state, and sandbox isolation (agents never run straight on your host) ([#466](https://github.com/mifunedev/openharness/pull/466)).
- Relocate the shared skill source of truth from `.claude/skills/` to `.mifune/skills/` (a single tracked primitive). `.claude/skills`, `.codex/skills`, and `.pi/skills` are now symlinks to it, and the Hermes runtime gets it via a `.hermes/skills/openharness` symlink seeded by `.devcontainer/entrypoint.sh` (replacing the old `skills.external_dirs` config-injection). CI/release path filters and the eval-runner invocation now point at `.mifune/skills/**`; `.mifune/README.md` + `skills.lock` and the Hermes/installation docs are updated ([#501](https://github.com/mifunedev/openharness/issues/501)).
- Bypass the in-tree Pi path-guard risky-bash confirmation in interactive TUI sessions (`ctx.mode === "tui"`, where the operator is already approving each step) while keeping sensitive-path write/edit prompts and all non-TUI (headless/cron/autopilot) confirmations intact ([#495](https://github.com/mifunedev/openharness/issues/495)).
- Replace the hand-built in-tree Slack Pi extension (`.pi/extensions/slack/`) with the community [`pi-messenger-bridge`](https://github.com/tintinweb/pi-messenger-bridge) npm package, installed via npm into a gitignored `.pi/bridge/` dir and loaded only in the dedicated `client-slack` tmux session via `--extension` (not globally pinned), so no other pi session competes for the Slack connection. Slack tokens move from `SLACK_APP_TOKEN`/`SLACK_BOT_TOKEN` to `PI_SLACK_APP_TOKEN`/`PI_SLACK_BOT_TOKEN`; access control changes from a static `SLACK_ALLOW_*` allowlist to the package's challenge-based `trustedUsers`; runtime config lives in `~/.pi/msg-bridge.json` (`autoConnect:true`). Updates the `oh config slack` wizard, `.devcontainer/entrypoint.sh`, healthcheck, docker-compose/`harness.yaml` plumbing, all Slack docs, and adds a `pi-messenger-bridge` wiki entry ([#481](https://github.com/mifunedev/openharness/issues/481)).
- Docs builds now run automatically only from the docs workflow on `main`/`master` pushes; root build, Harness CI, release validation, and `/eval` stay on the fast non-docs path ([#455](https://github.com/mifunedev/openharness/issues/455)).
- Docs copy clarifies the product framing — Open Harness is the harness, the agent is your call (one developer, one project, one agent) — across the intro, installation, harnesses overview, and landing pages ([#493](https://github.com/mifunedev/openharness/issues/493)).
- `/retro` now uses a report schema plus skill-local helper scripts for deterministic hypothesis validation, duplicate-memory checks, and log rendering ([#443](https://github.com/mifunedev/openharness/issues/443)).
### Fixed
- `scripts/locked-append.sh` now hashes canonical absolute append targets for flock files, so relative and absolute spellings of the same memory or cron log serialize through one lock ([#513](https://github.com/mifunedev/openharness/issues/513)).
- Non-Slack Pi turns that hit the Codex Responses `previous_response_not_found` provider-state error are now re-injected once by an auto-loaded `.pi/extensions/codex-stale-response-retry.ts` extension, recovering loop/interactive prompts after installed `@earendil-works/pi-ai` clears its stale WebSocket `previous_response_id` continuation while leaving Slack-prefixed turns to the dedicated bridge recovery extension ([#506](https://github.com/mifunedev/openharness/issues/506)).
- Re-enabled the default `@narumitw/pi-codex-usage` Pi package at `0.6.2`, which includes the upstream stale-`ExtensionContext` statusline timer fix and restores `/codex-status` without the replacement-context crash ([#419](https://github.com/mifunedev/openharness/issues/419)).
- Weekly cleanup now preserves dirty, untracked, unpushed, or suspicious stale worktrees instead of deleting them by age alone, with eval coverage for the preservation gate ([#478](https://github.com/mifunedev/openharness/issues/478)).
- Cron runtime and weekly cron prompt liveness writes now route `.cron.log` records through `scripts/locked-append.sh`, extending the locked shared-log contract beyond autopilot and heartbeat paths ([#474](https://github.com/mifunedev/openharness/issues/474)).
- Cron fire-time metadata reload now preserves path-qualified cron file paths, preventing body hot-reload from falling back to stale cached prompts with `BODY_RELOAD_ERR` after `reloadEntryForFire` ([#472](https://github.com/mifunedev/openharness/issues/472)).
- `/autopilot` issue selection now skips open tickets that already have merged PR references, avoiding duplicate rebuilds when `Closes #N` landed on `development` but GitHub left the issue open ([#468](https://github.com/mifunedev/openharness/issues/468)).
- Compose helper diagnostics now use temporary `harness.yaml` env files so `--print-argv` and `config` do not create or overwrite `.devcontainer/.harness.yaml.env` ([#470](https://github.com/mifunedev/openharness/issues/470)).
- `/context-audit` and `/health-check` now route Memory Protocol examples through `scripts/locked-append.sh`, guarded by `memory-log-locked-append` ([#476](https://github.com/mifunedev/openharness/issues/476)).
- `/prd` now consistently writes PRDs to `tasks/<feature-name>/prd.md`, guarded by the `prd-output-path-contract` eval probe ([#483](https://github.com/mifunedev/openharness/issues/483)).
- Sandbox boot guard now boots the built sandbox image and verifies the in-container healthcheck before passing, catching entrypoint regressions in CI ([#485](https://github.com/mifunedev/openharness/issues/485)).
- Slack bot replies now post in a thread anchored to the triggering channel message (`thread_ts ?? ts`, gated to group chats so DMs stay flat) instead of as flat top-level messages. The harness temporarily pins the bridge to a fork branch carrying the unreleased patch (`github:ryaneggz/pi-messenger-bridge#feat/slack-thread-replies`, whose `prepare` script builds `dist/` on install); an upstream PR to [`tintinweb/pi-messenger-bridge`](https://github.com/tintinweb/pi-messenger-bridge) re-pins to a published release once merged ([#481](https://github.com/mifunedev/openharness/issues/481)).
- The `client-slack` Slack bridge now runs under a self-healing supervisor (`.devcontainer/client-slack-supervise.sh`) that restarts pi on the `extension ctx is stale` failure (and on crashes), clearing the single-instance lock so it reconnects — fixing the bridge silently going dead after a pi session replacement ([#481](https://github.com/mifunedev/openharness/issues/481)).
- The `client-slack` bridge now runs pi attached to the tmux pane's TTY (interactive mode) instead of `--mode rpc … | tee`, eliminating the `extension_ui_request` JSON flood that the loaded UI extensions (prompt-suggester/pi-recap) produced while keeping the REPL alive at idle; logs are captured out-of-band via pi stderr plus a `tmux pipe-pane` mirror ([#481](https://github.com/mifunedev/openharness/issues/481)).
- A standalone in-tree `.pi/bridge-recovery/` extension, co-loaded beside the bridge, re-injects a failed Slack-originated turn once on the Codex `previous_response_not_found` error — restoring replies the npm bridge silently dropped with `willRetry:false` ([#481](https://github.com/mifunedev/openharness/issues/481)).
- Cron worktree pruning now preserves a live early-run tmux session whose name still matches its cron worktree basename, preventing active autopilot checkouts from disappearing before branch rename ([#445](https://github.com/mifunedev/openharness/issues/445)).
- Heartbeat instructions now compute memory-log timestamps explicitly and route both memory/liveness writes through `scripts/locked-append.sh` instead of raw shared-log appends ([#447](https://github.com/mifunedev/openharness/issues/447)).
- Devcontainer boot now stops stale legacy `system-cron` tmux sessions before starting `cron-watchdog`/`cron-system`, avoiding unhealthy migrated sandboxes ([#453](https://github.com/mifunedev/openharness/issues/453)).
- The pi-messenger-bridge seed no longer clobbers runtime trust grants on every container boot: `.devcontainer/seed-msg-bridge.sh` seeds `~/.pi/msg-bridge.json` only when absent, otherwise jq-merges the seed structure while preserving the operator's `auth.trustedUsers`/`auth.channels`, and leaves the runtime file untouched on malformed input or jq failure ([#493](https://github.com/mifunedev/openharness/issues/493)).
### Removed
- Remove stale Pi authentication/port-forward docs now that Pi supports device auth ([#528](https://github.com/mifunedev/openharness/issues/528)).
- Removed the deprecated executable-loop framework — `context/rules/loop.md`, the `/orchestrate` runner skill, and the four loop-coupling tier-A probes (`loop-handoff-consistency`, `loop-benchmark-gate`, `loop-repeat-gate`, `orchestrate-contract`). The canonical workflow is now solely `AGENTS.md` § The Workflow; the former loop-node skills (`/critique`, `/approve`, `/audit`, `/benchmark`, `/delegate`, `/retro`, `/strategic-proposal`, `/imagine`, `/wiki-ingest`, `/context-audit`) remain as standalone / spec-*-composed skills with their loop-routing `## Handoff` sections removed. Also retired the `CB-002-walk-the-loop` capability task and the gitignored `autopilot-process-map` spec ([#497](https://github.com/mifunedev/openharness/issues/497)).
### Deprecated
### Security
- `/post-bridge` now defaults post creation to draft/dry-run previews and requires `POST BRIDGE LIVE CONFIRMED` before non-draft live or scheduled publishing, guarded by the `post-bridge-publish-confirmation` eval probe ([#523](https://github.com/mifunedev/openharness/issues/523)).
- `client-slack` restore now keeps Slack token values out of the tmux command string by sourcing a permission-restricted runtime env file before launching `pi` ([#461](https://github.com/mifunedev/openharness/issues/461)).

## [2026.6.18] - 2026-06-18

### Added
- `pr-audit-duplicate-issue-refs` eval probe guards that `/pr-audit` keeps duplicate open-PR issue references visible as a read-only triage flag ([#439](https://github.com/mifunedev/openharness/issues/439)).
- `harness-audit` wiki entry and Context Snapshot memory load-status diagnostics preserve the shared-memory model salvaged from duplicate #432 PRs ([#441](https://github.com/mifunedev/openharness/issues/441)).
- `autopilot-open-pr-reference-dedupe` eval probe guards that issue selection checks open PR metadata, branch names, titles, and bodies before launching duplicate autopilot work ([#437](https://github.com/mifunedev/openharness/issues/437)).
- `harness-audit-shared-memory` eval probe guards that `/harness-audit` reads durable long-term memory from `AUDIT_LOG_ROOT` in cron worktrees while preserving source inspection via `AUDIT_ROOT` ([#432](https://github.com/mifunedev/openharness/issues/432)).
### Changed
- `/pr-audit` now documents a duplicate issue-reference flag so multiple open PRs that close/reference the same issue are grouped for human canonical-PR selection instead of appearing as unrelated conflicts ([#439](https://github.com/mifunedev/openharness/issues/439)).
### Fixed
- Autopilot issue selection now dedupes against open PR references in linked metadata, branch names, titles, and bodies before starting work, preventing repeated PRs for the same issue when GitHub linked-PR metadata is empty ([#437](https://github.com/mifunedev/openharness/issues/437)).
### Removed
### Deprecated
### Security

## [2026.6.17] - 2026-06-17

### Added
- `retro-deterministic-contract` eval probe guards that `/retro` keeps schema-backed output, self-contained helper scripts, and synchronized `.pi`/`.claude` skill copies ([#443](https://github.com/mifunedev/openharness/issues/443)).
### Changed
- Sync `ryaneggz/openharness:development` changes after `e863676` into canonical development, including Pi autoresearch package support, cron/watchdog hardening, issue-template generalization, Slack startup fixes, and fork branch-catch-up guidance ([#430](https://github.com/mifunedev/openharness/issues/430)).
### Fixed
### Removed
### Deprecated
### Security

## [2026.6.16] - 2026-06-16

### Added
- `/git` skill promotes the former `context/rules/git.md` conventions into a provider-portable command skill, leaving the rules file as a pointer for providers that still load rules ([#422](https://github.com/mifunedev/openharness/issues/422), [#424](https://github.com/mifunedev/openharness/issues/424)).
- `/watchdog` skill replaces the autopilot-specific watchdog intent with a generic stuck/stale automation watchdog; its first guarded action completes stale autopilot draft PRs and removes draft only after verification, rather than merely nudging for a merge.
- `autopilot-upstream-default` eval probe — guards that future `/autopilot` and `/ship-spec` runs default GitHub/git operations to upstream `mifunedev/openharness` via `upstream/development`, not the personal fork checkout.
- `/teach` skill — post-implementation communication pass that revises/proposes the relevant wiki model, then teaches the operator the mental model, shipped changes, verification evidence, caveats, and understanding checks ([#214](https://github.com/ryaneggz/openharness/issues/214)).
- Add the tokenmaxxing Open Harness blog post explaining frontier-token spend as a harness-building pattern, not a productivity leaderboard ([#212](https://github.com/ryaneggz/openharness/issues/212)).
- `wiki-ingest` GitHub-repo-study (`references/github-repo-research-wiki.md`) and social-image (`references/social-image-wiki-ingest.md`) research patterns — `SKILL.md` gains §4b-i (attached image/screenshot ingest) and §4c (GitHub-repo study) plus a linked-primary-artifact fetch step, seeded with the `goldman-humanoid-robot-supply-chain` social-image worked example (cited raw `.md` snapshot force-added for provenance); also routes the `cleanup-tasks`/`eval-weekly`/`heartbeat` crons to `agent: pi` and adds `/ci-status` eval-probe-gate and `/pr-audit` green-verification + draft-cap-saturation triage guidance ([#208](https://github.com/ryaneggz/openharness/pull/208)).
- Locked append helper for critical autopilot/caps shared runtime logs — `scripts/locked-append.sh` serializes whole-record writes with `flock`, autopilot/caps logging now routes memory/liveness appends through it, and the `locked-append-critical-path` eval probe plus script tests guard the contract ([#204](https://github.com/ryaneggz/openharness/issues/204)).
- Deterministic autopilot caps preflight gate — the hourly `autopilot` cron now declares `preflight: scripts/autopilot-caps.sh`, a generic new `preflight:` field in `scripts/cron-runtime.ts` that runs a gate script **before** any worktree/tmux/agent is created. On a capped hour (≥10 open autopilot PRs or ≥6 created today) the gate writes the byte-faithful `SKIPPED-CAP-*` memory + liveness logs and the runtime logs `SKIPPED_PREFLIGHT` and spawns **nothing** — no Pi session, no model query, no worktree — replacing an hourly full gpt-5.5 session that only re-derived "the cap is hit". The caps are configurable defaults in `harness.yaml` (`autopilot.total_cap` / `autopilot.daily_cap`, read live each fire; `AUTOPILOT_TOTAL_CAP`/`AUTOPILOT_DAILY_CAP` env vars still override), the gate fails open (`PREFLIGHT_ERROR` → proceed) so a transient `gh` failure never wedges the loop, and `/autopilot` §1 keeps a thin in-session recheck deferring to the same canonical script. Guarded by the new `autopilot-preflight-gate` tier-A probe ([#194](https://github.com/ryaneggz/openharness/issues/194)).
- `.github/dependabot.yml` — `ignore` `js-yaml` for Dependabot security updates (its fix `>=4.2.0` is unreachable through `@docusaurus/* → gray-matter → js-yaml`, so the auto security-update job failed every retry with `security_update_not_possible`); version-update PRs stay disabled (`open-pull-requests-limit: 0`). Mirrors the pnpm-side accept of `GHSA-h67p-54hq-rp68` ([#200](https://github.com/ryaneggz/openharness/issues/200)).
- `evals/datasets/` verifiable trajectory corpus (Repo2RLEnv-inspired) — the eval system's third leg (a catalogue of real issue→shipped-PR trajectories, each a self-contained folder with `manifest.json` provenance, a `prompt.md` input, an `oracle/`, and a dual-mode `verify.sh`), seeded with DS-001/002/010/020 across ship-spec/autopilot/retro types, guarded by the `datasets-schema` tier-A probe and cross-referenced from the capability benchmark ([#196](https://github.com/ryaneggz/openharness/issues/196)).
- Wire the executable-loop `benchmark` node — the last `☐` in `context/rules/loop.md` § 7, which **closes the cycle**: every node is now `☑`, so the forward walk runs `ideate → … → compress → benchmark → repeat → ideate`. New `/benchmark` skill (`.claude/skills/benchmark/SKILL.md`) is the node driver — a thin **verdict-owner** (modeled on `/audit`) that composes `/eval` (regression floor) + the capability-benchmark ceiling delta vs. counterfactual (`evals/capability/`), emitting `STATUS: BENEFICIAL` → `repeat` or `NOT-BENEFICIAL` → `repeat` (after revert); machinery added with no benchmark movement is `NOT-BENEFICIAL` by definition (loop.md § 1), and it surfaces the § 1 human-redirect signal. Guarded by the new `loop-benchmark-gate` tier-A probe. `/eval-lint` instrument-grooming remains a named follow-on ([#179](https://github.com/ryaneggz/openharness/issues/179)).
- Capability benchmark instrument (`evals/capability/`) — the harness's progress *ceiling* (success · cost-time · unattended on representative end-to-end tasks), distinct from the `evals/probes/` regression floor: a README spec, 3 seed task specs (CB-001..003), a baseline scoreboard, and the `capability-benchmark-schema` guard probe. Stands up the instrument + baseline; gate/selection wiring is a follow-on ([#167](https://github.com/ryaneggz/openharness/issues/167)).
- Wire the executable-loop `repeat` node — the **cycle-closing edge** (`context/rules/loop.md` § 2 `repeat → ideate`), previously the one forward edge drawn with no `STATUS:` token, gains its `CYCLE-CONTINUE` token and § 7 marks it ☑. `repeat` is the sole **runner-applied** node (no work-skill / no `## Handoff`, like `/orchestrate` itself): `/orchestrate` applies its freshness gate mechanically — the `--max-iters` bound (standalone) plus autopilot caps (cron mode, invariant 4) — emitting `CYCLE-CONTINUE` → `ideate` to close the cycle, or honest-halting when the bound/caps are hit. Guarded by the new `loop-repeat-gate` tier-A probe. Forward walk still halts at the unwired `benchmark`; `repeat` is reachable via `/orchestrate --start repeat`. The runner skill is named `/orchestrate` (not `/loop`, which is reserved) ([#173](https://github.com/ryaneggz/openharness/issues/173), [#175](https://github.com/ryaneggz/openharness/issues/175)).
- `/orchestrate` skill — standalone walker of the executable-loop decision tree (`context/rules/loop.md` § 2): reads § 2 as the route table, STATUS-tail routing, `--dry-run`/`--start`/`--max-iters`, honest halt at unwired nodes ([#160](https://github.com/ryaneggz/openharness/issues/160)).
- Wire the `compress` loop node: `/context-audit` emits `STATUS: COMPRESS-DONE` → `benchmark` ([#160](https://github.com/ryaneggz/openharness/issues/160)).
- `orchestrate-contract` tier-A eval probe guarding the `/orchestrate` contract literals ([#160](https://github.com/ryaneggz/openharness/issues/160)).
- `implement` node wired in the executable loop — `/delegate` (`.claude/skills/delegate/SKILL.md`), the canonical implement driver, gains a `## Handoff` that emits `STATUS: IMPL-COMPLETE` → `audit` (every task completed *and* `tasks/<slug>/prd.json` fully conformant) or `IMPL-INCOMPLETE` → `implement` (resume), derived from its existing Delegation Report + task-graph conformance; the `ralph` serial fallback path maps its `STATUS: COMPLETE` done-sentinel to the same outcome. Completes the loop's decision spine (`ideate → … → critique → approve → implement → audit → retro → compound`) as routable end-to-end ([#156](https://github.com/ryaneggz/openharness/issues/156)).
- `/critique` skill (`.claude/skills/critique/SKILL.md`) — the **critique node** of the executable loop (`context/rules/loop.md` § 2), the evidence half of the `critique → approve` pair. Runs two adversarial `critic` agents in parallel (implementer lens + user lens, with a `.claude/protected-paths.txt` cross-check) against `tasks/<slug>/prd.md`, writes their SEVERITY-tagged findings to `tasks/<slug>/critique.md` in the exact shape `/approve` parses, and emits `STATUS: CRITIQUE-DONE` → `approve`. Produces evidence, does not decide; touches local artifacts only (invariant 6). Formalizes `/ship-spec` Stage 3 as a reusable node ([#156](https://github.com/ryaneggz/openharness/issues/156)).
- `/approve` skill (`.claude/skills/approve/SKILL.md`) — the **approve decision gate** (the loop's `approve|deny` decision point) of the executable loop (`context/rules/loop.md` § 2). Reads a spec's `tasks/<slug>/critique.md` and emits `STATUS: APPROVED` → `implement` or `DENIED` → `plan`, auto-gating on critic SEVERITY (any unmitigated high, any `[PROTECTED-PATH]` finding, or a `HALT` recommendation → deny) with an optional `AskUserQuestion` human override for borderline highs (skipped under `--auto`). Embodies invariant 6 (critic-before-commitment): it runs while only local artifacts exist, **before** any GitHub-side issue/branch/PR, so a `DENIED` is fully reversible. Formalizes `/ship-spec` Stage 4's inline HALT gate as a reusable node; fails closed (absent/unparseable critique never yields approval) ([#156](https://github.com/ryaneggz/openharness/issues/156)).
- `/audit` skill (`.claude/skills/audit/SKILL.md`) — the **per-unit verdict gate** of the executable loop (`context/rules/loop.md` § 2 `audit` node). Decides whether ONE implementation satisfies its task graph and is promotable by composing (never forking) existing checks in fail-fast order: `prd.json` task-graph conformance → `/eval` regression floor → `/pr-audit` promotable classification (subsumes `/ci-status`; falls back to `/ci-status` pre-PR) → `/agent-browser` for browser-verification stories. Read-only — it emits `STATUS: AUDIT-PASS` → `retro` or `AUDIT-FAIL` → `implement`, and never mutates PR state or fixes (a no-run CI is FAIL, not pass). Distinct from the audit-family survey skills (`/pr-audit`, `/context-audit`, `/harness-audit`) ([#156](https://github.com/ryaneggz/openharness/issues/156)).
- `loop-handoff-consistency` eval probe (`evals/probes/loop-handoff-consistency.sh`) — tier-A guard that every skill's `## Handoff` section emits a `STATUS:` token known to `context/rules/loop.md` § 2 and routes only to real loop nodes, so a renamed token or an off-tree route target flips it REGRESSION (the § 4 "declared routes MUST match § 2" guard).
- `context/rules/loop.md` — single source of truth for the **executable harness loop**: a decision-tree of skills where each node emits a terminal `STATUS:` token and hands off to the next, anchored to a *capability-as-outcomes* objective (a held-out capability benchmark as the progress ceiling vs. the `evals/probes` suite as the regression floor). Defines the `STATUS:` terminal-status convention, the per-skill `## Handoff` convention (distributed declaration, Advisor-centralized execution), and the six load-bearing invariants every handoff must preserve. Layer 0 of the executable-loop build; node wiring is incremental (see its § 7 build state).
- Harness CI now triggers on `Makefile` and `harness.yaml` changes, guarded by the `harness-ci-core-paths` eval probe so core sandbox config path-filter coverage cannot silently drift ([#165](https://github.com/ryaneggz/openharness/issues/165)).
- Sandbox Docker healthcheck for tmux-managed runtime services: `scripts/sandbox-healthcheck.sh` verifies `cron-watchdog`/`cron-system`, flags legacy `system-cron`, conditionally checks configured Slack/Hermes dashboard sessions, and `.devcontainer/docker-compose.yml` reports failures through container health status ([#150](https://github.com/ryaneggz/openharness/issues/150)).

### Changed
- `/retro` now uses a report schema plus skill-local helper scripts for deterministic hypothesis validation, duplicate-memory checks, and log rendering ([#443](https://github.com/mifunedev/openharness/issues/443)).
### Fixed
### Removed
### Deprecated
### Security

## [2026.6.14] - 2026-06-14

### Added
- `/ship-spec` post-draft pipeline now compacts, hands off to an expert `/worktrees` Advisor launched in an `agent-ship-<slug>` tmux session via a `/goal`-prefixed prompt (the Advisor isolates work in a worktree and drives `/delegate` workers that each run `scripts/ralph.sh`, monitoring to `STATUS: COMPLETE`), then undrafts the PR through a `/pr-audit` **promotable** gate (`gh pr ready` only on CI-green + mergeable + clean; otherwise it stays draft with a comment). Two `/compact` checkpoints bracket the implement phase — before implement (Stage 7.5, after the PRD artifacts) and after implement (Stage 11.5, before the `/pr-audit` audit) — so the implementer and the auditor each start with a clean context ([#140](https://github.com/ryaneggz/openharness/issues/140)).
- `ship-spec-ready-finalization` eval probe (`evals/probes/ship-spec-ready-finalization.sh`) — guards that `/ship-spec` treats draft PR creation as an observability checkpoint and ready-for-review as the successful terminal state ([#134](https://github.com/ryaneggz/openharness/issues/134)).
- `autopilot-pi-agent` eval probe (`evals/probes/autopilot-pi-agent.sh`) — guards the executable contract that the `autopilot` cron sets `agent: pi` and `cron-runtime` honors per-cron agent overrides when launching/resuming tmux sessions ([#116](https://github.com/ryaneggz/openharness/issues/116)).
- `autopilot-executor-toggle` eval probe (`evals/probes/autopilot-executor-toggle.sh`) — guards the `/autopilot` default delegate-advisor executor, Ralph fallback toggle, exact `/goal` Advisor phrase, mandatory `/compact` before `prd.json` delegation, `autopilot-<branch>` tmux naming, duplicate guard, active-marker cleanup, and non-mutating research dry-runs.
- Default Pi recap package support: `.pi/settings.json` pins `npm:@tifan/pi-recap@0.4.2`, adding `/recap` plus automatic idle/resume session recaps for long-running Pi sessions ([#138](https://github.com/ryaneggz/openharness/issues/138)).
- Default Pi Codex usage package support: `.pi/settings.json` pins `npm:@narumitw/pi-codex-usage@0.4.2`, adding `/codex-status` plus a compact `openai-codex` statusline for 5-hour session usage and weekly usage ([#108](https://github.com/ryaneggz/openharness/issues/108)).
- Default Pi task-tracking package support: `.pi/settings.json` pins `npm:@tintinweb/pi-tasks@0.7.0`, adds task tools (`/tasks` menu and persistent widget), ignores `.pi/tasks/` and `.pi/tasks-config.json` runtime state, and documents usage in docs/wiki ([#105](https://github.com/ryaneggz/openharness/issues/105)).
- Gate the eval probe suite in CI — a new independent `eval-probes` job in `.github/workflows/ci-harness.yml` runs `bash .claude/skills/eval/run.sh` on every PR and push to `development`/`main`, failing the pipeline on a new green→red probe regression while staying git read-only (no `RESULTS.md` commit); probes are first made hermetic (PASS or SKIPPED, never ERROR) in a cold runner, `evals/**` becomes a CI path trigger, and a self-guard `eval-ci-gate` probe asserts the gate cannot be silently removed ([#103](https://github.com/ryaneggz/openharness/issues/103)).
- `drift-check-cron-staleness-glob` eval probe (`evals/probes/drift-check-cron-staleness-glob.sh`) — Tier-A behavioral probe that EXTRACTS the live Step C-2 bash block from `.claude/skills/drift-check/SKILL.md` and RUNS it against fixtures (README-style with no line-1 `---`, a valid scheduled cron, disabled `enabled: false` cron, invalid schedule, and empty schedule) with `RUNTIME_START` before their mtimes; PASS only when the inert set includes the valid cron and excludes the README-style, disabled, invalid, and empty-schedule fixtures, so a revert of the schedulable-cron predicate to the raw `for f in crons/*.md` glob flips it to REGRESSION (a bare text-presence grep is explicitly rejected) ([#98](https://github.com/ryaneggz/openharness/issues/98)).
- Default Pi goal mode package: project-local `.pi/settings.json` now pins `npm:@narumitw/pi-goal@0.4.2`, adding the `/goal` command and `goal_complete` tool to Pi sessions by default; `docs/harnesses/pi.md` documents the package and a regression test guards the Pi package list ([#97](https://github.com/ryaneggz/openharness/issues/97)).
- SIGHUP cron-schedule reload — `scripts/cron-runtime.ts` installs a `SIGHUP` handler that stops the live croner jobs, re-reads every `crons/*.md`, and re-arms the schedules without restarting the `cron-system` tmux session, closing the schedule hot-reload gap left by #47 (body edits already hot-reloaded; schedule/frontmatter changes and added/removed cron files previously required a full restart). The reschedule is fault-isolated (a malformed file is dropped as `SCHED_INVALID`, the rest stay scheduled), re-entrancy-guarded, and logs a `RELOAD` liveness line; reload from the host with `docker exec -u sandbox openharness kill -HUP "$(cat crons/.pid)"`. SIGTERM/SIGINT shutdown is unchanged ([#88](https://github.com/ryaneggz/openharness/issues/88)).
- `/pr-audit` skill (`.claude/skills/pr-audit/`) — triages the whole open-PR queue from one bulk `gh pr list --json` query (no per-PR API loop): checks `isDraft` first and splits draft PRs into a separate work-in-progress class (promotable/WIP/limbo) so an unfinished draft never pollutes the merge-readiness signal, then assigns every ready-for-review PR a single actionable primary state (CI-failing/conflicting/changes-requested/needs-review/ready/pending) via the `crons/heartbeat.md` ready-selector and the `ci-status` `statusCheckRollup` field, layers orthogonal stale/convention flags (`FROM … TO …` title + base-`development` per `context/rules/git.md`), and is read-only by default. `--deep` fans out capped parallel diff reviewers for flagged PRs, `--proof` writes an idempotent (marker-keyed) per-PR verdict comment so human reviewers see the evidence on the PR itself, and `--label-apply`/`--close-stale` mutate only after confirmation ([#76](https://github.com/ryaneggz/openharness/issues/76)).
- Hermes web dashboard opt-in: set `hermes.dashboard: true` in `harness.yaml` (or legacy `HERMES_DASHBOARD=true`), with Hermes installed, to auto-launch the dashboard in the `app-hermes-dashboard` tmux session — published loopback-only on `HERMES_DASHBOARD_PORT` (default 9119) via a compose overlay ([#376](https://github.com/mifunedev/openharness/issues/376)).
- `ERR_JOB` cron-runtime status line — `scripts/cron-runtime.ts` replaces croner's silent `catch: true` with an injectable `onJobError(id, err)` handler that records a `<id>\tERR_JOB\t<error-string>` line to `crons/.cron.log`, so a synchronous job-callback throw is now distinguishable from an idle cron while preserving croner's crash-resistance ([#49](https://github.com/ryaneggz/openharness/issues/49)).
- Context fitness-function eval corpus (`evals/`) — deterministic probes with a 3-state exit oracle (PASS/REGRESSION/SKIPPED) and a `RESULTS.md` benchmark scoreboard ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- `/eval` runner skill (`.claude/skills/eval/`) — runs the probe suite against real state, writes the benchmark, surfaces green→red regressions naming the lesson each closes ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- `warn-devtcp` PreToolUse hook — non-blocking warning when a Bash command uses `/dev/tcp` (suggests `ss`/`curl`/`nc`, since zsh lacks the pseudo-device) ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- Seed probes `next-dev-prod`, `devtcp-hook`, `health-check-docker-stats` ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- Shared ablation mechanics (`scripts/ablate.sh`) + `/eval --ablate <file> --probe <id>` — the prune-half, reporting `LOAD-BEARING`/`PRUNABLE` ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- Weekly `eval-weekly` cron that runs the suite and logs regressions ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- `eval-gate` probe (`evals/probes/eval-gate.sh`) — pins the corrected autopilot §6 rule so it cannot silently regress: asserts the gate keys on the green→red delta + runner exit code and that the bare "Any `REGRESSION`" gate phrasing is gone ([#22](https://github.com/ryaneggz/openharness/issues/22)).
- Autopilot loop: hourly cron + `/autopilot` skill that selects the next harness-infra item, builds it via `/ship-spec` + `/delegate`, and finalizes a ready-for-review PR ([#4](https://github.com/ryaneggz/openharness/issues/4)).
- `/drift-check` skill (`.claude/skills/drift-check/`) — read-only detection of three drift classes: framework drift (`origin/development` behind `upstream/development`), branch-behind/append-file drift (HEAD behind `origin`, dirty tree, or an unexpected branch), and cron-staleness drift (`crons/*.md` newer than the running `cron-system` runtime); prints a recommended remediation per finding and is surfaced automatically each heartbeat pulse ([#7](https://github.com/ryaneggz/openharness/issues/7)).
- `boot-lint` CI job + `.devcontainer/**` / `install/**` path filters in `ci-harness.yml` — runs shellcheck (`-S warning`) on the boot shell scripts and hadolint (curated `.hadolint.yaml` ignore list) on the `Dockerfile`, so a broken boot path can no longer merge with a green CI badge ([#26](https://github.com/ryaneggz/openharness/issues/26)).
- Heartbeat autopilot-health nudge (`crons/heartbeat.md` step 2.8) — each pulse kills `cron-autopilot-*` tmux sessions frozen at a usage/session-limit or `Resume from summary` prompt (a detached run stuck there blocks every later `:05` fire under `overlap: false`), sweeps orphaned keep-markers, and surfaces long-lived sessions + green/mergeable open `autopilot` PRs for operator action. Killing a confirmed-stuck session is the only autonomous act; it never merges and never kills on age alone ([#41](https://github.com/ryaneggz/openharness/issues/41)).
- `clean-restore` probe (`evals/probes/clean-restore.sh`) — pins the autopilot branch-restore so it cannot silently regress: asserts the canonical **scoped** restore (`git checkout development -- $OWNED_PATHS` then `git checkout development`, with an owned-tree-clean + HEAD-on-`development` assertion) at every restore site, and that exactly one forced tree-wide `git checkout -f development` remains (the §1 self-heal) ([#45](https://github.com/ryaneggz/openharness/issues/45), rescoped by [#63](https://github.com/ryaneggz/openharness/issues/63)).
- Typecheck gate in harness CI — a `Typecheck` step now runs `tsc --noEmit` across both TypeScript packages (`packages/docs` via the pnpm workspace, `packages/oh` via its own `npm ci`), with a matching root `pnpm typecheck` script for local-dev parity, so type regressions can no longer ship silently ([#51](https://github.com/ryaneggz/openharness/issues/51)).
- `wiki/repo2rlenv.md` — bounded synthesis of Hugging Face Repo2RLEnv as a repository-grounded RL/evaluation-environment synthesis layer that fits Open Harness alongside `evals/` rather than as another agent CLI, with `wiki/README.md` regenerated to index it ([#75](https://github.com/ryaneggz/openharness/pull/75)).
- `/wiki-ingest` hardening — a new step 7 directs the orchestrator to regenerate the `wiki/README.md` index (via `/wiki-lint` or its atomic fallback) whenever a tracked entry ships as a deliverable, plus a new `references/concurrent-ingest-worktrees.md` documenting the isolated-worktree pattern for concurrent ingests / preserving unrelated branch state ([#75](https://github.com/ryaneggz/openharness/pull/75)).

### Changed
- `/autopilot` now **defers the whole build to `/ship-spec`** (DRY): since `/ship-spec` owns the compacts, the worktree Advisor, `/delegate` + ralph, `/eval`, and the `/pr-audit` promotable undraft, autopilot's `delegate-advisor` default no longer re-runs implement/eval/finalize — it runs `/ship-spec --issue` and reconciles the terminal outcome, keeping selection, caps, selection-rationale, session lifecycle, and branch restore. The `/goal` handoff phrase is updated to match, ralph stays as a legacy inline fallback (now finalizing via `/pr-audit`), and the `autopilot-executor-toggle` eval probe + `crons/autopilot.md` move in lockstep ([#140](https://github.com/ryaneggz/openharness/issues/140)).
- Align `/ship-spec` guidance with the ready-for-review finalization contract: draft PR creation remains an observability checkpoint, but successful runs continue through implementation, `/eval`, `/ci-status`, and `gh pr ready` ([#134](https://github.com/ryaneggz/openharness/issues/134)).
- Mirror CI's boot-path lint and eval-probe regression gates in `release.yml`, so tagged releases now require `validate`, `boot-lint`, and `eval-probes` to pass before publishing GHCR images or GitHub Releases ([#111](https://github.com/ryaneggz/openharness/issues/111)).
- Sandbox onboarding banner status markers now prefer emoji (`✅` / `❌` / `⬜`) and fall back to the legacy bracket markers (`[✓]` / `[✗]` / `[ ]`) outside UTF-8 locales or when `OH_BANNER_STATUS_STYLE=legacy` is set.
- Cron definitions can set `agent: <binary>` in frontmatter to override `CRON_AGENT_BIN` for one job; `crons/autopilot.md` now sets `agent: pi` so the documented Pi tmux Advisor session is the actual attach/resume agent while other crons keep the global default ([#116](https://github.com/ryaneggz/openharness/issues/116)). Pi tmux cron runs now use the attachable positional TUI invocation (`pi "$(cat prompt)"`) instead of the headless piped `pi -p ... | tee ...` shape, matching the known-good `tmux new -s <name> pi "<prompt>"` attach behavior ([#118](https://github.com/ryaneggz/openharness/issues/118)).
- `/autopilot` now defaults to the `delegate-advisor` executor: the cron-created Pi tmux session constructs the PM/advisor plan, runs the exact `/goal` handoff, uses `/ship-spec`, compacts before executing `tasks/<slug>/prd.json`, then orchestrates implementation via `/delegate --plan`; sessions are renamed to sanitized `autopilot-<branch>` names and left alive for manual attach/continue/reap. `AUTOPILOT_EXECUTOR=ralph` or `--executor=ralph` preserves the existing `scripts/ralph.sh "$SLUG"` fallback.
- Replace Open Harness' local Pi plan-mode extension with the upstream package: `.pi/settings.json` now pins `npm:@narumitw/pi-plan-mode@0.4.2`, `.pi/extensions/plan-mode/` is removed, and docs/wiki/tests describe plan mode as package-provided ([#107](https://github.com/ryaneggz/openharness/issues/107)).
- Cron tmux sessions now use the category-prefix convention: the runtime session is `cron-system` (formerly `system-cron`), and detached cron fires are `cron-<id>-<MMDD>-<HHMM>` (for example, `cron-heartbeat`/`cron-autopilot-0610-1805` style names). The migration path is guarded so entrypoint will not start `cron-system` while legacy `system-cron` is still running, and heartbeat scans/sweeps both new `cron-autopilot-*` and legacy `autopilot-*` sessions/markers during the transition ([#95](https://github.com/ryaneggz/openharness/issues/95)).
- Cron agent runs now write explicit `AGENT_START`, `AGENT_FALLBACK`, and `AGENT_DONE` liveness lines to `crons/.cron.log`, so the log shows which agent started a task and which agent completed it after a Claude→Codex fallback ([#94](https://github.com/ryaneggz/openharness/pull/94)).
- A non-zero cron child exit now enriches its `EXIT_<code>` liveness line in `crons/.cron.log` with a bounded tail (≤200 chars) of the failing job's tee'd log, so a failure is diagnosable from the cron log alone instead of requiring the (often-gone) `/tmp/<session>.log`; the `OK` success path and the detached-tmux `SPAWNED` path are unchanged (the tmux path cannot observe the agent's exit) ([#73](https://github.com/ryaneggz/openharness/issues/73)).
- Autopilot clean-state + branch-restore are now scoped to an explicit `OWNED_PATHS` harness-infra surface (`.claude/ context/ docs/ scripts/ crons/ wiki/ evals/ memory/ tasks/ CHANGELOG.md`): the §1 main check and every §5/§6/§7 restore site operate on bare `-- $OWNED_PATHS` (unquoted, so it word-splits into multiple pathspecs), so a stray edit *outside* the owned surface no longer blocks a run or gets clobbered by the restore, and a dirty *owned* path now skips non-destructively with a distinct `BLOCKED-OWNED-WIP` liveness token (not a bare `FAIL`) so the heartbeat reads a real owned-WIP stall as a stall rather than idleness. Exactly one forced tree-wide `git checkout -f development` remains — the §1 self-heal, which fires only when the whole tree is already clean and so can never destroy foreign WIP ([#63](https://github.com/ryaneggz/openharness/issues/63)).
- Default cron agent execution now falls back from Claude to Codex on usage/session-limit output for every cron that uses the default agent path, and Ralph's default fallback order is now `claude→codex` with `Submitted-by:` commit trailers required in generated task prompts.
- Autopilot §5 now implements tickets with the **resumable Ralph runner** (`scripts/ralph.sh`) instead of an in-process `/delegate` wave: `/ship-spec` already scaffolds the four-file Ralph contract, so §5 launches the loop and cheap bash-polls `progress.txt` for `STATUS: COMPLETE`. The loop persists per-story state (`prd.json` `passes` + `progress.txt`) and falls back `claude→codex`, so a mid-run Claude usage-limit resumes from the last passing story instead of leaving a spec-only draft PR (the in-process failure mode that produced #50). New `RALPH-INCOMPLETE` status leaves the PR draft + resumable on timeout/exhaustion; `/delegate` is retained as the documented fallback for parallelizable multi-story tasks. `overlap: false` preserved (the foreground blocks on the poll) ([#55](https://github.com/ryaneggz/openharness/issues/55)).
- Upgrade Docusaurus `3.7.0` → `3.10.1` (`@docusaurus/core`, `preset-classic`, `plugin-client-redirects`, `theme-mermaid`, `tsconfig`, `types`) and add the `@mermaid-js/layout-elk` peer the new `theme-mermaid` requires. This clears the remaining Dependabot alerts that the 3.7 bundler stack pinned: `webpack` → 5.107.2 (lifts the old exact `5.98.0` override; 3.10's `webpackbar` build no longer trips webpack's `ProgressPlugin` schema) and `webpack-dev-server` → 5.2.4 (Docusaurus 3.10 uses `wds@5` natively). Validated with a full `docs:build` ([#37](https://github.com/ryaneggz/openharness/issues/37)).
- Replace the placeholder GitHub-template `SECURITY.md` with a real security policy — CalVer support line, private vulnerability reporting (Security tab / maintainer email) with a triage SLA, the automated-hardening stack (Dependabot + `pnpm.overrides`, GitGuardian, boot-lint), and the sandbox trust boundary ([#33](https://github.com/ryaneggz/openharness/issues/33)).
- Autopilot §6 eval gate now keys on the green→red regression **delta** + the `/eval` runner exit code rather than the bare presence of a `REGRESSION` row — a probe already red on the base (`unchanged` delta) is pre-existing and proceeds (posted on the PR for honesty); only a NEW green→red regression or a non-zero runner exit keeps the PR draft (`PR-DRAFT-EVAL-RED`) ([#22](https://github.com/ryaneggz/openharness/issues/22)).
- Make `/ci-status` repo-agnostic — derive the target repo at runtime and add a PR-first status path ([#20](https://github.com/ryaneggz/openharness/issues/20)).
- A kept per-run cron tmux session now resumes the run's own conversation as a live, attachable agent (`claude --continue`, falling back to a shell) instead of a bare `bash` — so you can `tmux attach` and proceed if a run needs a judgment call. The autonomous run stays headless (`claude -p`); the resumed agent is idle until a human drives it ([#18](https://github.com/ryaneggz/openharness/issues/18)).
- Autopilot v2: issue-queue-first selection (build the oldest open `autopilot` issue; when the queue is empty, run first-principles `/harness-audit` research and file an `autopilot` ticket before building it), per-run tmux sessions (opt-in `tmux: true` cron frontmatter with a keep-marker/session-name contract), an `/eval` regression gate before a PR is marked ready, a total-open ceiling of 10 autopilot PRs alongside the 6/day cap, every autopilot PR documents its selection rationale in the description, and a new `/ship-spec --issue <N>` flag to scaffold against an existing ticket ([#14](https://github.com/ryaneggz/openharness/issues/14)).
- Autopilot guardrail: the cap of 2 open `autopilot` PRs becomes a daily cap of 6 concurrently-open PRs created per UTC day — a same-day close or merge frees the slot so the next hourly fire can build another; the `/harness-audit` fallback throttle tightens from 6h to 4h and is tracked via an explicit `AUDIT-RUN` liveness marker in `crons/.cron.log` ([#9](https://github.com/ryaneggz/openharness/issues/9)).
- `/health-check` gains an in-container RAM-reclaim step (`docker stats` per-container; RAM ≠ `docker ps` Size) ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- `/retro` gains a correction-surface triage tag (harden/proceduralize/eval) routing each promotable lesson to its cheapest closing artifact ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- `/context-audit` Tier-2 ablation now sources the shared `scripts/ablate.sh` mechanics (observable behavior unchanged) ([#1](https://github.com/ryaneggz/openharness/issues/1)).
- Release pipeline now validates (lint, format check, build, test, root tests) before building/pushing the Docker image or creating the GitHub Release ([#24](https://github.com/ryaneggz/openharness/issues/24)).
- Extend the typecheck gate to `release.yml`'s validate job — it now runs `Typecheck (workspace packages)` (`pnpm --filter './packages/**' ... typecheck`) and `Typecheck (oh CLI — npm standalone)` (`npm --prefix packages/oh ci && ... typecheck`) before building the image, mirroring `ci-harness.yml` so the release pipeline is a superset of the PR pipeline's checks ([#53](https://github.com/ryaneggz/openharness/issues/53)).
- Warn in `/delegate` that the `implementer`/`pm`/`critic` sub-agent types are read-only (`tools: Read, Glob, Grep, Bash`) and silently make zero file changes — at both the Worker-configuration block and the §Reference Key Resources table — and recommend `subagent_type: general-purpose` (or `claude`) for any worker that must `Write`/`Edit` files; a new `rl-delegation-write-worker` `/eval` probe guards the warning against regression ([#57](https://github.com/ryaneggz/openharness/issues/57)).
### Fixed
- Fail sandbox boot when the required root `pnpm install` step fails during devcontainer entrypoint startup, preventing a false-healthy container with missing cron/Pi/Slack dependencies ([#136](https://github.com/ryaneggz/openharness/issues/136)).
- Regenerate the generated wiki index and add a Tier-A `wiki-readme-index` eval probe so `wiki/README.md` cannot silently list missing entries or omit tracked `wiki/*.md` pages again ([#132](https://github.com/ryaneggz/openharness/issues/132)).
- Add a `cron-watchdog` tmux supervisor in the devcontainer entrypoint so `cron-system` restarts automatically if the cron runtime session dies after boot, with docs/tests/eval coverage for the supervision contract ([#130](https://github.com/ryaneggz/openharness/issues/130)).
- Correct the devcontainer first-boot and interactive shell banners to reference the installed `oh` CLI (`oh config slack`) instead of nonexistent/stale Open Harness command names ([#124](https://github.com/ryaneggz/openharness/issues/124)).
- Validate cron ids before scheduling: `scripts/cron-runtime.ts` now skips unsafe ids, unsafe filename basenames, and explicit id/filename mismatches with `ID_INVALID`/`ID_MISMATCH` liveness lines, preventing malformed cron frontmatter from reaching shell-wrapper or `/tmp` path interpolation ([#128](https://github.com/ryaneggz/openharness/issues/128)).
- Release the autopilot cron overlap lock on terminal kept Pi sessions: the runtime now exports `$CRON_OVERLAP_PIDFILE`, `/autopilot` clears it before restoring `development` after `PR-READY`/terminal draft states, and the `autopilot-pi-agent` probe guards against a retained review pane causing hourly `SKIPPED_OVERLAP` stalls ([#126](https://github.com/ryaneggz/openharness/issues/126)).
- Remove the stale `workspace/startup.sh` auto-start hook: the devcontainer entrypoint no longer runs a leftover app/docs bootstrap script, the workspace template docs no longer advertise it, CI/release boot-lint shellcheck globs cover only active boot scripts, and `/harness-audit` now checks for stale workspace auto-start hooks instead ([#120](https://github.com/ryaneggz/openharness/issues/120)).
- Correct the false daily-log persistence claim in `context/rules/memory.md` — the § Layout block no longer states `log.md` is "tracked inside" the gitignored `memory/YYYY-MM-DD/` directory (the `memory/[0-9]*/` rule makes daily logs a local-only working journal; only `MEMORY.md`, `README.md`, and topic notes persist in git, with the two pre-ignore logs `memory/2026-05-03`/`memory/2026-05-04` remaining as a tracked historical vestige), and a new Tier-A `memory-gitignore-claim` probe (`evals/probes/memory-gitignore-claim.sh`) guards the correction so reintroducing the false claim or dropping the gitignore rule flips it to REGRESSION ([#101](https://github.com/ryaneggz/openharness/issues/101)).
- `/drift-check` Class-C cron-staleness now qualifies each `crons/*.md` as a *schedulable* cron before the mtime check — mirroring `scripts/cron-runtime.ts` `parseCronFile` + `loadCrons` (line-1 `---`, a closing `---`, a non-empty Croner-valid `schedule:` key, and not `enabled: false`) — so non-cron docs like `crons/README.md` and malformed schedules the runtime would skip as `SCHED_INVALID` are skipped silently instead of being false-flagged "inert until runtime restart" on every heartbeat pulse ([#98](https://github.com/ryaneggz/openharness/issues/98)).
- Scope the weekly `cleanup-tasks` cron pre-flight to `tasks/` (excluding `tasks/archive/`) and isolate its archive branch + commit work in a crash-safe `.worktrees/archive/<date>` worktree, so unrelated foreign WIP can no longer abort the sweep and a crashed run can no longer leak dirty state into the archive commit or strand a worktree that blocks future sweeps; a dirty `tasks/` now skips with a distinct `BLOCKED-TASKS-WIP` liveness token, guarded by `evals/probes/cleanup-tasks-scoped-guard.sh` ([#85](https://github.com/ryaneggz/openharness/issues/85)).
- `/eval` runner now writes `evals/RESULTS.md` atomically — it builds the full scoreboard into a temp sibling file (`RESULTS.md.tmp.$$`) and replaces the live file in a single `mv -f`, and reads carry-forward rows for probes not run this invocation from a pre-write snapshot instead of the live file — so a crash or concurrent run can no longer leave a partially-written scoreboard and a filtered run (`--probe <id>`) no longer erases untouched rows. A new Tier-A `eval-results-atomic` probe locks the invariant in ([#83](https://github.com/ryaneggz/openharness/issues/83)).
- Autopilot `OWNED_PATHS` guard now word-splits correctly under zsh: the §1 owned-surface check and every §5/§6/§7 scoped restore expand the path list as a native shell array `"${OWNED_PATHS[@]}"` instead of the bare `$OWNED_PATHS`, which collapsed to a single pathspec under zsh (no `SH_WORD_SPLIT`) and left the dirty-owned-tree guard vacuously satisfied; the two coupled eval probes (`owned-surface-guard.sh`, `clean-restore.sh`) are updated in lockstep to assert the array form and reject any bare-form reintroduction ([#81](https://github.com/ryaneggz/openharness/issues/81)).
- Ralph runner now exits promptly on completion: `scripts/ralph.sh` detects the `STATUS: COMPLETE` whole-line sentinel in the agent's iteration **output** (self-healing `progress.txt` if the file write was missed), not just in `progress.txt`, and **self-closes its own tmux session** on a clean completion instead of dropping into a persistent `exec bash` — so a finished loop no longer spins to `MAX_ITER` or leaves a zombie session that piles up. The `ship-spec` Ralph prompt template now directs the agent to print the marker as its final line when terminal ([#79](https://github.com/ryaneggz/openharness/issues/79)).
- Autopilot one-PR stall: shipped backlog items are now checked off after finalize (with a §2 self-heal for merged-but-unchecked items), dedupe hits advance to the next candidate instead of ending the run, dedupe also matches merged PRs so shipped work is never rebuilt, and the §1 pre-check fast-forwards `development` to origin before selecting ([#9](https://github.com/ryaneggz/openharness/issues/9)).
- Removed the dangling `.claude/ICP.md` entry from `.claude/protected-paths.txt` — the file was deleted in `9241da6` ("sweep README/ICP refs") but its protection entry was left behind, so critic gates flagged a protected path that no longer exists.
- De-referenced the deleted `.claude/ICP.md` from its two remaining live consumers: `/ship-spec` critic-B now reads `context/USER.md` for the single-developer / single-project framing (the lens that lived in ICP.md), and `context/USER.md` drops the dangling `per .claude/ICP.md` citation while keeping the fact.
- Add `pull_request` trigger and expanded `paths:` filter (`.claude/skills/**`, `crons/**`, `context/**`) to `ci-harness.yml` so autopilot PRs touching skills, crons, or context get a real CI status check ([#12](https://github.com/ryaneggz/openharness/issues/12)).
- `/eval` runner now exits non-zero (exit 1) when any green→red regression is detected, so CI and autopilot gates receive an unambiguous failure signal ([#29](https://github.com/ryaneggz/openharness/issues/29)).
- Autopilot loop starvation: a single open in-flight `autopilot` PR no longer idles the whole loop. When no actionable ticket exists — queue empty OR every open ticket already has a PR — the run now falls through to `/harness-audit` research (cap-bounded by the existing 6/day + 10-total guards) instead of exiting on the retired `IN-FLIGHT` token. A run idling on another run's PR is barred from un-drafting or finalizing it; the heartbeat watches for stuck-green drafts instead ([#41](https://github.com/ryaneggz/openharness/issues/41)).
- Correct stale `docs/wiki/` → `wiki/` and `workspace/heartbeats/` → `crons/` path references in the `/harness-audit` and `/skill-lint` skills (audit sub-agents were inspecting directories removed in the wiki/cron restructure), and add an `evals/probes/skill-paths.sh` guard probe so the drift cannot silently return ([#43](https://github.com/ryaneggz/openharness/issues/43)).
- Autopilot dirty-tree deadlock: the branch-restore at §5/§6/§7 now uses a canonical forced clean restore (`git checkout -f development` + a post-restore assertion mirroring the §1 guard) instead of a bare `git checkout development`, so a crashed `/delegate` wave that leaves uncommitted tracked changes can no longer strand the loop on the feature branch and FAIL every hourly fire. §1 also self-heals a *clean*-but-stranded branch; a *dirty* tree at §1 still hard-FAILs to protect any human work-in-progress ([#45](https://github.com/ryaneggz/openharness/issues/45)).
- Cron definitions now hot-reload their body at fire time: `reloadBody` re-reads and re-parses `crons/*.md` before each fire so body edits apply without a runtime restart; an unreadable file falls back to the cached body and logs `BODY_RELOAD_ERR` ([#47](https://github.com/ryaneggz/openharness/issues/47)).
- Cron scheduling is now fault-isolating: a malformed `schedule:` string in any `crons/*.md` is skipped and logged `SCHED_INVALID` (in `crons/.cron.log`) instead of throwing from `new Cron()` and crashing the whole runtime — so one bad cron file no longer silently stops every other cron. Boot now logs a `BOOT` summary `"<N> scheduled, <M> skipped"`, and each construction is individually guarded as defense-in-depth ([#67](https://github.com/ryaneggz/openharness/issues/67)).
- Correct stale `apps/` and `src/data/roadmap` path references missed by the `apps/→packages/` monorepo rename across four skills — `/harness-audit` (docs-app context gather + Implementer test/package-health checks), `/strategic-proposal` (roadmap write target reconciled to `docs/roadmap.md`, `apps/docs/` → `packages/docs/`), `/ci-status` (`docs.yml` trigger doc) and `/harness-context` (`apps/README.md` → `packages/README.md`) — and extend the `evals/probes/skill-paths.sh` guard so the rename tokens (`apps/docs`, `apps/README`, `apps/*`, `src/data/roadmap`) cannot silently reappear ([#69](https://github.com/ryaneggz/openharness/issues/69)).
- Extend the `boot-lint` CI shellcheck coverage to active boot scripts — the `shellcheck -S warning` glob now lints `scripts/*.sh` alongside `.devcontainer/*.sh` and `install/*.sh`, `workspace/**` remains in the `on.push`/`on.pull_request` `paths:` triggers so workspace template changes still fire the job, a latent `SC2088` in `scripts/install.sh` is silenced with a scoped lint-disable (preserving the user-facing `~/.openharness` literal), and `evals/probes/boot-lint-glob.sh` guards the active boot-script glob against drift ([#90](https://github.com/ryaneggz/openharness/issues/90)).
- Pin the devcontainer's corepack pnpm to the declared `packageManager` version (`pnpm@10.33.0`) and add a CI drift guard so the Dockerfile and `package.json` can't silently diverge ([#114](https://github.com/ryaneggz/openharness/issues/114)).
### Removed
- Curated autopilot backlog (`crons/autopilot-backlog.md`) and the 4h `/harness-audit` throttle (`AUDIT-RUN` marker) — autopilot is now steered via GitHub `autopilot` issues, so the in-repo backlog file is gone ([#14](https://github.com/ryaneggz/openharness/issues/14)).
### Deprecated
### Security
- Resolve all open critical & high Dependabot alerts in the root `pnpm-lock.yaml` via `pnpm.overrides`: `shell-quote` →1.8.4 (GHSA-w7jw-789q-3m8p), `@babel/plugin-transform-modules-systemjs` →7.29.4 (GHSA-fv7c-fp4j-7gwp), `fast-uri` →3.1.2 (GHSA-v39h-62p7-jpjc, GHSA-q3j6-qgpj-74h6), `serialize-javascript` →7.0.3 (GHSA-5c6j-r48x-rmvq); `vitest` →3.2.6 already landed in [#28](https://github.com/ryaneggz/openharness/pull/28) ([#30](https://github.com/ryaneggz/openharness/issues/30)).
- Resolve moderate & low Dependabot alerts whose patched version is compatible with the Docusaurus 3.7 bundler: root `pnpm-lock.yaml` via `pnpm.overrides` — `qs` →6.15.2, `uuid` →11.1.1, `ws` →8.20.1 (scoped to the 8.x line so the `ws@7` consumer is untouched), `mermaid` →11.15.0, `postcss` →8.5.10; and `packages/oh` `esbuild` devDependency →0.25.0. The `webpack` (low) and `webpack-dev-server` (moderate) alerts are deferred to a Docusaurus 3.7→3.10 upgrade — webpack 5.107's stricter `ProgressPlugin` schema breaks the Docusaurus 3.7 `docs:build`, so the `webpack` pin stays at `5.98.0` here ([#35](https://github.com/ryaneggz/openharness/issues/35)).
- Resolve the `joi` Dependabot alert (uncaught `RangeError` via recursive `link()` schemas, GHSA-q7cg-457f-vx79) by overriding `joi` →18.2.1 in the root `pnpm-lock.yaml`; `joi` is Docusaurus's config-validation transitive, and the 17→18 major builds clean against Docusaurus 3.10 (validated via `docs:build`) ([#39](https://github.com/ryaneggz/openharness/issues/39)).

## [2026.6.13] - 2026-06-13

### Added
- Add an `Eval Probe Regression Gate` CI job, new eval self-guard probes, and atomic `/eval` scoreboard writes so green→red probe regressions block public harness changes ([#415](https://github.com/mifunedev/openharness/issues/415)).
- Load `@tintinweb/pi-goal` and `@tintinweb/pi-tasks` by default in Pi, document their usage, and ignore Pi task runtime state ([#415](https://github.com/mifunedev/openharness/issues/415)).
- Add the public-safe `/pr-audit` skill for one-shot bulk PR triage; read-only by default with optional deep review, proof comments, label application, and stale close actions ([#415](https://github.com/mifunedev/openharness/issues/415)).

### Changed

### Fixed
- Tighten `/drift-check` cron-staleness detection to compare only schedulable cron files and skip README, disabled, unscheduled, empty, and invalid cron docs ([#415](https://github.com/mifunedev/openharness/issues/415)).
- Correct `context/rules/memory.md` so daily `memory/YYYY-MM-DD/log.md` files are documented as local/gitignored journals unless force-added ([#415](https://github.com/mifunedev/openharness/issues/415)).
- Expand boot-path shellcheck coverage to `scripts/*.sh` and `workspace/*.sh` while preserving the intentional literal `~/.openharness` installer message ([#415](https://github.com/mifunedev/openharness/issues/415)).

### Removed
### Deprecated
### Security

## [2026.6.12] - 2026-06-12

### Added
- `/autopilot` skill and hourly `crons/autopilot.md` loop: issue-queue-first harness-infra selection, `/ship-spec --issue`, Ralph implementation, `/eval` gate, conservative PR caps, and no auto-merge behavior ([#412](https://github.com/mifunedev/openharness/issues/412)).
- Heartbeat autopilot nudge logic surfaces green/mergeable autopilot PRs, long-lived sessions, and kills only sessions frozen at an unambiguous interactive/usage prompt ([#412](https://github.com/mifunedev/openharness/issues/412)).
- Autopilot-specific eval probes guard the forced clean branch restore and the corrected eval-gate decision rule ([#412](https://github.com/mifunedev/openharness/issues/412)).
- `/eval` skill and `evals/` probe corpus: deterministic shell probes with a 3-state oracle, an overwrite-row `evals/RESULTS.md` benchmark, and an aggregate non-zero exit only for new green→red regressions ([#410](https://github.com/mifunedev/openharness/issues/410)).
- `scripts/ablate.sh` provides shared swap/restore/trap mechanics for evaluating whether a context file is load-bearing under a deterministic probe ([#410](https://github.com/mifunedev/openharness/issues/410)).
- `/drift-check` detects framework drift, branch/append-file drift, and cron-staleness drift without mutating local state; the heartbeat now surfaces its findings when present ([#410](https://github.com/mifunedev/openharness/issues/410)).
- `CI: Harness` now runs on pull requests and harness infrastructure paths, adds workspace and standalone `packages/oh` typecheck gates, cancels superseded runs, and adds boot-path linting with `shellcheck`/`hadolint` ([#408](https://github.com/mifunedev/openharness/issues/408)).
- `release.yml` now validates lint, format, typecheck, build, package tests, and root script tests before publishing release artifacts ([#408](https://github.com/mifunedev/openharness/issues/408)).
- `SECURITY.md` documents supported versions, private vulnerability reporting, automated hardening, and the sandbox trust boundary ([#408](https://github.com/mifunedev/openharness/issues/408)).
- Hermes web dashboard opt-in: set `hermes.dashboard: true` in `harness.yaml` (or legacy `HERMES_DASHBOARD=true`), with Hermes installed, to auto-launch the dashboard in the `app-hermes-dashboard` tmux session — published loopback-only on `HERMES_DASHBOARD_PORT` (default 9119) via a compose overlay ([#376](https://github.com/mifunedev/openharness/issues/376)).

### Changed

### Fixed
- Cron runtime execution now reloads cron bodies at fire time, records synchronous job callback failures as `ERR_JOB`, supports opt-in tmux-backed cron runs, and has focused tests for the new behavior ([#408](https://github.com/mifunedev/openharness/issues/408)).
- `Docs: build & deploy` workflow no longer fails on forks: the Pages `Configure`/`Upload`/`Deploy` steps are gated to the canonical repo (`github.repository == 'mifunedev/openharness'`), so forks build-validate docs without attempting a Pages deploy. Also removed a dangling `docs/harnesses/t3code.md` link to the deleted `integrations/cloudflare.md` ([#404](https://github.com/mifunedev/openharness/issues/404)).
- `/release` skill now resolves the canonical remote (prefers `upstream`, else `origin`) for `REPO` and all release pushes, so a release can't accidentally land on a private fork; GHCR verification tries the org package path before the user path ([#402](https://github.com/mifunedev/openharness/issues/402)).
- `/ship-spec` Stage 7 now clones a self-contained `.claude/skills/ship-spec/templates/prompt.md` instead of the deleted `tasks/archive/openharness-v07-convergence/prompt.md`; repointed the two other dead references to that task ([#402](https://github.com/mifunedev/openharness/issues/402)).

### Removed

### Deprecated

### Security
- Dependency manifests and lockfiles are refreshed with Docusaurus/Vitest upgrades and scoped `pnpm.overrides` for current transitive advisory remediation ([#408](https://github.com/mifunedev/openharness/issues/408)).

## [2026.6.10] - 2026-06-10

### Changed
- Separate the maintainer's personal/instance state from the public template so it ships clean and fork-ready: `memory/MEMORY.md` reset to an empty stub, the `context/IDENTITY.md` lessons list emptied, `context/USER.md` "Working context" genericized to placeholders, the social account-ID line dropped from `context/TOOLS.md`, and `crons/heartbeat.md` timezone set to `America/Los_Angeles`. The private fork (`ryaneggz/openharness`) retains the full content ([#400](https://github.com/mifunedev/openharness/issues/400)).
- Default sandbox timezone changed from `America/Denver` to `America/Los_Angeles` across the image, compose, install script, and cron docs/config — overridable via `harness.yaml`/`.devcontainer/.env` ([#400](https://github.com/mifunedev/openharness/issues/400)).

### Fixed
- `.gitignore` now ignores `tasks/archive/` — the prior `tasks/*/archive/` pattern never matched the real path ([#400](https://github.com/mifunedev/openharness/issues/400)).

### Removed
- Maintainer's research wiki (11 `wiki/*.md` entries plus tracked `wiki/raw/` snapshots) and accumulated task history (`tasks/archive/**`, in-flight `tasks/make-shell-service-arg/`) from the public template — moved to the private fork ([#400](https://github.com/mifunedev/openharness/issues/400)).
- Personal agent-fleet docs (`docs/agents/`) and their matching `agent/*` workspace branches from the public template — moved to the private fork ([#400](https://github.com/mifunedev/openharness/issues/400)).

## [2026.6.9] - 2026-06-09

### Added
- `cron` preinstalled in the sandbox base image so scheduled jobs work without a manual `apt-get install` ([#312](https://github.com/mifunedev/openharness/issues/312)).
- Documented "compound engineering" (the Every / Kieran Klaassen AI-native methodology) and how Open Harness embodies it: a `wiki/compound-engineering.md` entry mapping it onto the harness's append-only memory, critic gate, protected-paths accumulation, and recursive delegation; a public blog post (`blog/2026-06-04-compound-engineering.md`); and light pointers added to `docs/intro.md` and `docs/resources.md`. Deliberately no new `context/rules/` file — the concept only names mechanisms the harness already enforces ([#378](https://github.com/mifunedev/openharness/issues/378)).
- `/health-check` skill: triage host memory/swap/disk/CPU and Docker usage before starting or building a stack, rank reclaim levers by safety×yield (auto-prune regenerable build cache, confirm before destructive container/volume removal), and report a RAG verdict naming the binding constraint ([#380](https://github.com/mifunedev/openharness/issues/380)).
- `wiki/rubricrefine-agent-reliability.md` — RubricRefine entity page: training-free pre-execution refinement for improving tool-use agent reliability ([#391](https://github.com/mifunedev/openharness/issues/391)).
- `wiki/turbovec-turboquant-rag.md` — turbovec / TurboQuant entity page: compressed-vector RAG retrieval ([#391](https://github.com/mifunedev/openharness/issues/391)).
- Public blog post (`blog/2026-06-07-containers-microvms-vms.md`) comparing container, microVM, and full-VM isolation for AI agent execution — frames the choice as trust × tenancy, explains why Open Harness's container is optimal for the single-developer terminal case and why a Firecracker microVM tier suits untrusted/multi-tenant hosting; stems from the Firecracker/microVM isolation research ([#384](https://github.com/mifunedev/openharness/issues/384)).
- Reference docs for three skills, each a new `references/*.md` plus a pointer from its `SKILL.md`: `harness-audit` (external-proposal implementation-audit pattern), `strategic-proposal` (V2MOM / operating-model → wiki-plan variant), and `wiki-ingest` (official-docs research pattern + consent-gated write-recovery anti-pattern) ([#393](https://github.com/mifunedev/openharness/issues/393)).
- Blog post `blog/2026-05-19-four-words-multi-agent.md` — mental-model essay defining the four-role vocabulary (Operator / Sandbox / Orchestrator / Harness) for agent systems, with a one-recursion-level mermaid diagram and a worked Open Harness example. Renders via the existing `@docusaurus/theme-mermaid` wiring ([#320](https://github.com/mifunedev/openharness/issues/320)).
- `harness.yaml` master config for non-secret settings: `sandbox.name`, `sandbox.timezone`, `install.*` flags, `slack.allow_users`/`slack.allow_channels`, `crons.*`, and `compose.overrides`; `scripts/harness-config.sh` converts it to `.devcontainer/.harness.yaml.env` passed via `--env-file`; precedence: `harness.yaml` > `.devcontainer/.env` > compose defaults; no `yq` required ([#395](https://github.com/mifunedev/openharness/issues/395)).

### Changed
- Moved the canonical repo to `mifunedev/openharness` (transferred from `ryaneggz/open-harness` and renamed). Release images now publish to `ghcr.io/mifunedev/openharness`; `release.yml`, docs, `README.md`, `scripts/install.sh` default clone target, and the `ci-status`/`strategic-proposal` skills point at the new home. Old URLs redirect; historical CHANGELOG image paths are preserved ([#397](https://github.com/mifunedev/openharness/issues/397)).
- `.gitignore` now exempts `/.hermes/hooks/` from the `/.hermes/*` ignore so Hermes hook scripts are trackable ([#393](https://github.com/mifunedev/openharness/issues/393)).
- Release process now promotes `development` → `main` (fast-forward) before cutting the release branch and tag, keeping `main` as the authoritative release line; `/release` skill and `.claude/rules/git.md` § Releases document the `development → main → release → tag → sync` flow and abort if `main` has diverged.
- Renamed the top-level `apps/` directory to `packages/` and updated workspace, docs, Dockerfile, CI, and test discovery paths to match.
- `/wiki-ingest` now requires an explicit `--slug` override for social/share URLs (`linkedin.com`, `x.com`, etc.), segments with a ≥10-digit share/activity ID, or slugs over 60 chars — closing a `context/rules/wiki.md` §3 gap that let raw LinkedIn share slugs through ([#373](https://github.com/mifunedev/openharness/issues/373)).
- Renamed and rewrote the `/reflect` skill as `/retro` (`.claude/skills/retro/SKILL.md`) — a scientific-method session-closing pass that frames session observations as falsifiable hypotheses with cited evidence, verdicts, and confidence, and assesses six learning/knowledge subsystems (continual learning, context compression, reinforcement learning, wiki, docs, memory scaffolding); preserves the propose-then-confirm gate, append-only writes, and mandatory log entry.
- `.devcontainer/.env` reduced to secrets-only surface (`GH_TOKEN`, `SLACK_APP_TOKEN`, `SLACK_BOT_TOKEN`); all non-secret env vars migrate to `harness.yaml` ([#395](https://github.com/mifunedev/openharness/issues/395)).
### Fixed
- Autopilot caps now target the canonical `mifunedev/openharness` repo instead of the checkout's implicit GitHub remote: `crons/autopilot.md` declares `repo:`, the cron runtime validates and exports `AUTOPILOT_REPO`/`AUTOPILOT_REMOTE` at fire time, cap queries use `gh pr list --repo`, and `/autopilot` plus `/ship-spec` resolve the matching local git remote for fetch/push.
- `harness.yaml` ships fully commented again (`sandbox.name`/`sandbox.timezone` re-commented) so a fresh clone no longer forces `SANDBOX_NAME`/`TZ` — restoring the "defaults stay authoritative until a key is uncommented" invariant and the `harness-config` test that asserts it ([#398](https://github.com/mifunedev/openharness/pull/398)).
- Renamed three wiki entries ingested from LinkedIn share URLs to short semantic slugs (`inspectable-agent-harness`, `latmd-knowledge-graph`, `codegraph-mcp`), fixed their cross-links, and regenerated the stale `wiki/README.md` index ([#373](https://github.com/mifunedev/openharness/issues/373)).
- `CRONS_DIR`, `CRON_AGENT_BIN`, `SLACK_ALLOW_USERS`, and `SLACK_ALLOW_CHANNELS` are now passed into the container via compose `environment:` and `harness.yaml`; previously documented but never actually reached the container ([#395](https://github.com/mifunedev/openharness/issues/395)).
### Removed
### Deprecated
### Security
- Bump root test tooling and the `@openharness/oh` CLI build dependency to clear the remaining `esbuild <0.28.1` Dependabot alerts across `pnpm-lock.yaml` and `packages/oh/package-lock.json` ([#92](https://github.com/ryaneggz/openharness/issues/92)).

## [2026.5.29] - 2026-05-29

### Added
- Optional Hermes agent CLI image integration (`INSTALL_HERMES=true`) with project-local runtime state under `~/harness/.hermes`, auth isolated at `~/.hermes`, and default `skills.external_dirs` pointing at `/home/sandbox/harness/.claude/skills`; OpenCode and DeepAgents are now documented as optional image-level installs (`INSTALL_OPENCODE=true`, `INSTALL_DEEPAGENTS=true`) while the default agent set remains Claude Code, Codex, and Pi; optional harness docs/wiki entries include install steps and upstream docs ([#364](https://github.com/mifunedev/openharness/issues/364)).
- `wiki/sequoia-capital-the-next-1t-company.md` captures the Sequoia/AI Opportunities thesis that AI-native winners may sell completed services work rather than software tools ([#362](https://github.com/mifunedev/openharness/issues/362)).
- `wiki/learn-harness-engineering.md` captures the Walking Labs *Learn Harness Engineering* course as an entity page; `docs/resources.md` + `docs/intro.md` link the external course as the human-facing entry point ([#341](https://github.com/mifunedev/openharness/issues/341)).
- `fast-check` ^4.8.0 added to root devDependencies and vitest include glob extended to cover `apps/**/__tests__/**/*.test.ts` — enables property-based tests across all TypeScript surfaces ([#354](https://github.com/mifunedev/openharness/issues/354)).
- `/imagine` skill (`.claude/skills/imagine/SKILL.md`) — one-shot draft PRD sketch generator. Takes a free-text `<scenario>`, derives a slug via `/prd`'s rule, and writes a 7-section sketch (Scenario / Intent / Sketch / mermaid Diagram / Rough goals / Story seeds / Open questions for /prd) to `.claude/specs/<slug>/spec.md`. Output is gitignored scratch (`.gitignore:51`); purpose-built as input for `/ship-spec --plan <path>`. No clarifying questions — ambiguities go into the spec body so the user can edit before formalizing.
- `scripts/__tests__/cron-runtime.property.test.ts` — never-throw regression-prevention property test for `parseCronFile` using fast-check; 100 arbitrary string pairs, zero source changes ([#354](https://github.com/mifunedev/openharness/issues/354)).
- `scripts/__tests__/cron-runtime.property.test.ts` — forward-compatibility regression-prevention property test for `parseCronFile`: asserts that valid frontmatter with 1–5 arbitrary unknown keys is parsed correctly with `result.schedule === "* * * * *"`; constrained arbitraries prevent structural-YAML-character false failures ([#354](https://github.com/mifunedev/openharness/issues/354)).
- `scripts/__tests__/cron-runtime.property.test.ts` — ordering-stability regression-prevention property test for `loadCrons`: generates 2–8 unique `.md` filenames, writes them in REVERSE-alphabetical order, and asserts `loadCrons(tmpDir)` returns entries in ascending alphabetical order; exercises the `.sort()` at `cron-runtime.ts:50` so accidental removal is caught ([#354](https://github.com/mifunedev/openharness/issues/354)).
- `.pi/extensions/__tests__/path-guard.property.test.ts` — no-false-positives property test for `isSensitivePath`: generates arbitrary strings guaranteed not to match any `SENSITIVE_PATHS` regex and asserts `isSensitivePath(s) === false`; imports both `isSensitivePath` and `SENSITIVE_PATHS` from `path-guard` (no re-declaration drift) ([#354](https://github.com/mifunedev/openharness/issues/354)).
- `apps/oh/src/__tests__/cli.property.test.ts` — determinism and no-throw property tests for `isHelpFlag` and `isVersionFlag`: two determinism assertions (same input → same result) and one no-throw assertion covering all `string | undefined` inputs; uses `vi.mock` hoisting to suppress the top-level `main()` call in `cli.ts` during import ([#354](https://github.com/mifunedev/openharness/issues/354)).
- Property-based testing convention documented in `docs/property-testing.md` with sections on Convention, Arbitrary patterns, numRuns defaults, Decision tree, and Surface ledger linking to the five v1 properties across three target surfaces ([#354](https://github.com/mifunedev/openharness/issues/354)).
### Changed
- `.pi/extensions/path-guard.ts` refactored to expose `SENSITIVE_PATHS` as a named export and extract the sensitive-path check into a new `isSensitivePath(p: string): boolean` named export; default-export behavior unchanged ([#354](https://github.com/mifunedev/openharness/issues/354)).
- Export `isHelpFlag` and `isVersionFlag` from `apps/oh/src/cli.ts` for testability (#354).
- `make shell [container]` now accepts an optional running container name and runs `docker exec`; defaults to `$(SANDBOX_NAME)`. Pair with `SHELL_USER=<user>` for containers without a `sandbox` user.
- Hermes now ships the Slack messaging extra (`hermes-agent[slack]`) by default when `INSTALL_HERMES=true`, so the gateway's Slack adapter works out of the box without a manual `uv pip install`. Requires an image rebuild ([#364](https://github.com/mifunedev/openharness/issues/364)).
### Fixed
- Fresh sandbox images no longer leak root-owned uv tool paths into the default `sandbox` user's runtime. DeepAgents still installs into image-level paths at build time, but runtime `UV_TOOL_DIR`/`UV_TOOL_BIN_DIR` now point at sandbox-owned home paths, and the optional Hermes install directory is chowned to `sandbox` so `uv pip install --python /usr/local/lib/hermes-agent/venv/bin/python ...` works without sudo.
- Ad-hoc `uv pip install` into the Hermes venv no longer fails with `Permission denied` after a UID sync. The Dockerfile chowns `/usr/local/lib/hermes-agent` to the build-time sandbox UID, but the entrypoint remaps `sandbox` to the host UID on boot and its re-chown only covered `/home/sandbox`, leaving the venv (a system path) orphaned. The entrypoint now re-chowns `/usr/local/lib/hermes-agent` and `/opt/uv` to the current sandbox UID on every boot ([#364](https://github.com/mifunedev/openharness/issues/364)).
- Hermes auth writes no longer fail with a cross-device-link error (`OSError: [Errno 18] EXDEV`). The entrypoint previously symlinked `~/harness/.hermes/auth.json` into the home-scoped `~/.hermes` named volume, but that volume is a different filesystem from the bind-mounted checkout, so Hermes' `atomic_replace()` (write-temp-then-`os.replace`) failed on every credential save. `auth.json` now lives directly in `HERMES_HOME` (gitignored); the entrypoint heals leftover legacy symlinks on boot and the unused `hermes-auth` volume was removed. Requires an image rebuild ([#364](https://github.com/mifunedev/openharness/issues/364)).
- Secret-exposure hooks (`.claude/hooks/{deny-env-dump,deny-secret-paths}.sh`) no longer deny operations on tracked template env files (`.example.env`, `.env.example`, `.env.sample`, `.env.template`); real `.env`, `.env.local`, `.env.production` continue to deny. Path-branch was split out of monolithic `DENY` so allowlist applies only to `READ_CMD`-anchored secret-path matches; `\benv\b` patterns retightened to `(^|[^A-Za-z0-9._/-])env` (command-position) to fix a pre-existing false-positive on any path ending in `.env`. Codex wrapper inherits the fix ([#356](https://github.com/mifunedev/openharness/issues/356)).
### Removed
### Deprecated
### Security

## [2026.5.24] - 2026-05-24

### Added
- `wiki/` directory + `context/rules/wiki.md` schema for Karpathy LLM Wiki pattern ([#351](https://github.com/mifunedev/openharness/pull/351)).
- `/wiki-ingest` skill (`.claude/skills/wiki-ingest/SKILL.md`) — captures `WebFetch` snapshots and authors/updates wiki entries; supports URL/path ingest + `--from-draft` promotion; orchestrator-only writes; body-merge per `context/rules/wiki.md` § 7 ([#351](https://github.com/mifunedev/openharness/pull/351)).
- `/wiki-query` skill (`.claude/skills/wiki-query/SKILL.md`) — frontmatter-only grep against `wiki/*.md` (title, slug, tags) using the canonical extraction from `context/rules/wiki.md` § 6; multi-word OR semantics; sub-cap reads all when < 3, top 3 by `updated:` descending otherwise ([#351](https://github.com/mifunedev/openharness/pull/351)).
- `/wiki-lint` skill (`.claude/skills/wiki-lint/SKILL.md`) — manual health check for the wiki corpus: five finding types (stale-90d, deprecated, orphan, broken outbound link, contradiction-stub-descoped); atomic `wiki/README.md` regeneration with tmp-then-rename + validation; `--dry-run` flag for read-only audits; reports `confidence: deprecated` entries but never autonomously sets the flag (per `context/rules/wiki.md` § 5 lifecycle) ([#351](https://github.com/mifunedev/openharness/pull/351)).
- `/reflect` skill (`.claude/skills/reflect/SKILL.md`) — deliberate whole-session "Improve" pass that operationalizes the Memory Improvement Protocol (`context/rules/memory.md`): scans the current conversation for durable behavioral patterns, applies the qualify filter, classifies survivors by tier, and proposes `memory/MEMORY.md` / `context/IDENTITY.md` additions for explicit confirmation before writing (log-tier entry always appended). Manual-trigger, current-session scope, propose-then-confirm. Authored via the skill-builder agent for `github:mifunedev/skills` ([#337](https://github.com/mifunedev/openharness/issues/337)).
- `/caveman` token-compression skill + subcommands (`.claude/skills/caveman{,-commit,-review,-compress,-stats}/`) — output-style mode that compresses agent prose ~65–75% via telegraphic phrasing while preserving code, API names, error strings, security warnings, and irreversible-action confirmations verbatim. Levels: `lite`/`full`/`ultra`/`wenyan`. Vendored adaptation of [JuliusBrussee/caveman](https://github.com/JuliusBrussee/caveman) (MIT, commit `655b7d9`) authored natively to harness skill conventions; upstream `curl | bash` installer intentionally not run. Pinned in `.mifune/skills.lock` as manual/vendored entries ([#335](https://github.com/mifunedev/openharness/issues/335)).
- `/context-audit` skill (`.claude/skills/context-audit/`) — data-backed, repeatable eval for the default-loaded context budget. Tier-1 scores each file on 4 dimensions (footprint, load-bearing, integrity, redundancy) and emits KEEP/TRIM/DEMOTE/CUT verdicts. Tier-2 ablation harness (`runner.sh` + 7 probe files) removes a target file, runs a fixed task suite via `claude -p`, and measures behavior degradation — the only provably safe gate for cutting load-bearing content ([#333](https://github.com/mifunedev/openharness/issues/333)).
- `/render-html` skill (`.claude/skills/render-html/SKILL.md`) — orchestrator skill for rendering an artifact as a bespoke, self-contained HTML file under `memory/<UTC-date>/<slug>.html` for one-shot human review (audit synthesis, council output, lint matrix, weekly digest). Output is gitignored via existing `memory/[0-9]*/` rule ([#314](https://github.com/mifunedev/openharness/issues/314)).
- `/interview` skill (`.claude/skills/interview/SKILL.md`) — adaptive pre-work clarifier that batches 2–4 task-specific questions through `AskUserQuestion`, echoes a 2–3 sentence scope brief, and proceeds. Skips trivial tasks with an announcement. Serves as the in-tree reference pattern for `AskUserQuestion`-based elicitation in future skills ([#316](https://github.com/mifunedev/openharness/issues/316)).
- Blog post `blog/2026-05-19-statusline-context-rot.md` — frames `/statusline` as the fix for the context-rot problem Ralph-loop users already grok; includes a copy-pasteable shell-script statusline that surfaces `context_window.used_percentage` and `rate_limits.five_hour.used_percentage` from the Claude Code statusline JSON ([#318](https://github.com/mifunedev/openharness/issues/318)).

### Changed
- Register `/wiki-ingest`, `/wiki-query`, `/wiki-lint` in `AGENTS.md` skills table ([#351](https://github.com/mifunedev/openharness/pull/351)).
- All 15 skills in `.claude/skills/` now sourced from and version-pinned to `github.com/mifunedev/skills` (commit `c559c100`) via `.mifune/skills.lock`; `interview` and `render-html` contributed upstream and added to the lock ([#327](https://github.com/mifunedev/openharness/issues/327)).
- `README.md`, `docs/installation.md`, and `docs/quickstart.md` restructured to surface fork-and-clone and clone-and-own as first-class install paths alongside the upstream one-liner; `scripts/install.sh` local-run detection is now the documented bootstrap for self-hosters ([#331](https://github.com/mifunedev/openharness/issues/331)).
- Skill `LICENSE` files updated to `Copyright (c) 2026 Mifune Dev (mifune.dev)` to match upstream correction.
- Re-synced `context-audit` to the portable upstream form ([mifunedev/skills#6](https://github.com/mifunedev/skills/pull/6)): removed the top-level `argument-hint` frontmatter key, repinned `.mifune/skills.lock` (`commit` `f95011fe`, checksum `02dd6324`), and added the previously-missing `LICENSE` for folder parity ([#339](https://github.com/mifunedev/openharness/issues/339)).
- Renamed the Slack-bridge tmux session from `agent-pi` to `client-slack` and introduced a `client-` prefix in `context/rules/sandbox-processes.md` for external-surface clients (Slack, Discord, web UIs) that bridge an in-sandbox agent. `.devcontainer/entrypoint.sh` now restores the `client-slack` session on container start whenever `SLACK_APP_TOKEN` and `SLACK_BOT_TOKEN` are present in `.devcontainer/.env` and `pi` is installed, so the Slack bridge survives `docker compose restart` without manual relaunch. Rename propagated through `apps/oh/src/config/slack.ts` (`oh config slack` wizard), `.pi/extensions/slack/README.md`, `docs/integrations/slack.md`, `docs/connecting.md`, `docs/intro.md`, and `docs/harnesses/overview.md` (the latter folding in the half-finished `harness-pi` rename from #302).
### Fixed
- Sandbox image installs `pi-coding-agent` under the sandbox user's npm prefix (`/home/sandbox/.local`) and places that bin directory before `/usr/bin`, allowing `pi update` to self-update without sudo on fresh builds.
### Removed
- `config.example.json` and its install-time seeding step (`scripts/install.sh`); the base ships zero compose overlays and all readers tolerate a missing `config.json`, so the example template was dead weight. Packs that need overlays create their own `config.json` ([#323](https://github.com/mifunedev/openharness/issues/323)).
### Deprecated
### Security

## [2026.5.18] - 2026-05-18

### Added
- `OH_GITHUB_REPO` and `OH_GITHUB_REF` env vars for fork-friendly installs ([#309](https://github.com/mifunedev/openharness/pull/309)).
- `/worktrees` skill (`.claude/skills/worktrees/SKILL.md`) — orchestrator skill for managing `.worktrees/` lifecycle: create, list, remove, clean, stale audit, project clones.

### Changed
- README and `docs/installation.md` add a "For forks / self-host" section; canonical upstream `oh.mifune.dev` URL unchanged ([#309](https://github.com/mifunedev/openharness/pull/309)).
- `/release` skill now pushes the source branch back to origin immediately after the CHANGELOG promotion commit, before cutting the release branch. Without this, the promotion commit lived only on `release/*`, so the next round of work on `development` re-carried already-released `[Unreleased]` entries and the next `/release` would double-promote them ([#297](https://github.com/mifunedev/openharness/issues/297)).

### Fixed
- Repaired `CHANGELOG.md` drift between `development` and `main`: prior to the `/release` fix above, three releases (`2026.5.14`, `2026.5.15`, `2026.5.16`) had promoted versioned sections on `main` but never landed those promotions back on `development`, leaving stale `[Unreleased]` entries that duplicated already-shipped content. This commit restores the versioned sections to `development` from `main` and trims `[Unreleased]` to the genuinely new (post-`2026.5.16`) entries ([#297](https://github.com/mifunedev/openharness/issues/297)).

### Removed
### Deprecated
### Security

## [2026.5.16] - 2026-05-16

### Added
- `docs/connecting.md` — new runbook documenting the VSCode Attach + port-forwarding workflow for remote dev servers. Covers the "machine unreachable" debugging pattern: VS Code Remote SSH's automatic port-forwarding, manual fallback for direct-terminal users, and the `localhost:` prefix convention for accessing services on the Docker host. Referenced from `intro.md`, `quickstart.md`, `harnesses/overview.md`, `harnesses/t3code.md`, and `integrations/slack.md`. ([#300](https://github.com/mifunedev/openharness/issues/300))
- **Agents catalog**: new `docs/agents/` section (renamed from `docs/agents/` → `docs/harnesses/` after initial creation to match sidebar label). Includes `_category_.json` organizing the section, `overview.md` as the hub page, and five per-branch playbook pages (`portfolio-mgr.md`, `blog-writer.md`, `linkedin-ghostwriter.md`, `dc-designer.md`, `uat-tester.md`), each documenting use-case, harness selection, auth flow, and tmux launch pattern. ([#300](https://github.com/mifunedev/openharness/issues/300))

### Changed
- Renamed `docs/agents/` folder to `docs/harnesses/` so the folder name matches the sidebar label; added `@docusaurus/plugin-client-redirects` to `apps/docs/` with seven redirects from `/docs/agents/*` → `/docs/harnesses/*` preserving URLs from the initial agents-catalog PR. ([#300](https://github.com/mifunedev/openharness/issues/300))
- Cross-linked `docs/connecting.md` from `intro.md`, `quickstart.md`, `harnesses/overview.md`, `harnesses/t3code.md`, and `integrations/slack.md` so the VSCode attach pattern is discoverable from each major entry point. ([#300](https://github.com/mifunedev/openharness/issues/300))
- Fixed `apps/README.md` script-name drift: `pnpm --filter @openharness/docs dev` → `pnpm --filter @openharness/docs start` to match the actual `start` script in `apps/docs/package.json`. ([#300](https://github.com/mifunedev/openharness/issues/300))
- Docs frame the orchestrator-via-VS-Code-attach as the canonical workflow. `docs/quickstart.md` "Enter the sandbox" now leads with the VS Code Dev Containers attach flow (Command Palette → `Dev Containers: Attach to Running Container...` → `openharness`), since it works identically on localhost and over Remote-SSH; `make shell` is demoted to a terminal fallback. `docs/intro.md` "How it works" names the **orchestrator** (the agent session attached at the project root that owns git, lifecycle, and edits, drives other containers over the Docker socket, and only needs host-shell intervention for things the running container can't do for itself — typically a new bind-mounted volume), frames a second sandbox as opt-in for isolation only, and closes with a Mermaid architecture diagram (orchestrator at the center of the sandbox subgraph, tmux session row, `docker.sock` → optional second sandbox, external GitHub / LLM-provider edges, host bind-mount). `docs/harnesses/overview.md` replaces the redundant "Running an agent in tmux" section with "Default surfaces" (Pi+Slack, T3 Code, docs app on `:3000`), each as a named tmux session, with explicit guidance to open the two browser surfaces in VS Code's Simple Browser (`Ctrl+Shift+P` → `Simple Browser: Show`; `Cmd+Shift+P` on macOS) so the live UI sits next to the code being edited. Documented tmux sessions renamed `agent-pi` → `harness-pi` and `agent-t3code` → `harness-t3code` in `docs/harnesses/overview.md` only — the `oh config slack` wizard at `apps/oh/src/config/slack.ts` still creates `agent-pi`, and `context/rules/sandbox-processes.md` still defines the `agent-` prefix; full rename across wizard code, rule, and sibling docs (`docs/intro.md`, `docs/integrations/slack.md`, `docs/harnesses/t3code.md`, `.pi/extensions/slack/README.md`) will follow. ([#302](https://github.com/mifunedev/openharness/issues/302))
- `.worktrees/README.md` now reserves `.worktrees/project/<owner>/<repo>/` for durable standalone repo clones and narrows `.worktrees/agent/` to harness agent branches / legacy agent-owned clones, using generic examples so the layout applies cleanly to new users. ([#295](https://github.com/mifunedev/openharness/issues/295), [#298](https://github.com/mifunedev/openharness/issues/298))

### Fixed
- Dark-mode inline `code` pills in the docs site were nearly invisible against pure-black surfaces. `apps/docs/src/css/custom.css` background bumped from `rgba(255, 255, 255, 0.06)` → `0.12`, border `0.1` → `0.22`, and text raised to pure white. Light mode untouched. ([#302](https://github.com/mifunedev/openharness/issues/302))

### Removed
### Deprecated
### Security

## [2026.5.15] - 2026-05-15

### Added
- `retro-deterministic-contract` eval probe guards that `/retro` keeps schema-backed output, self-contained helper scripts, and synchronized `.pi`/`.claude` skill copies ([#443](https://github.com/mifunedev/openharness/issues/443)).
### Changed
- `oh config slack` wizard UX overhaul. Token prompts now echo `●` per keystroke (was: silent — looked frozen) via a TTY-aware raw-mode `askSecret` that handles backspace, Ctrl-U, and Ctrl-C; piped/non-TTY stdin still uses the legacy readline fallback as `askSecretPiped` so `echo "" | oh config slack` keeps working. Flow is reframed into four numbered steps `[1/4] … [4/4]` with inline "Where:" hints and clickable URLs (OSC 8 hyperlinks; fall back to `label (url)` on legacy terminals). Each input shows `✓ valid prefix` / `✓ N user(s) accepted` after validation. A final summary screen lists the keys (tokens redacted, IDs visible) and gates the env-file write behind a `Proceed? [Y/n]` confirmation. New helpers exported from `apps/oh/src/lib/prompt.ts`: `step()`, `link()`, `bold()`. Zero new runtime dependencies. ([#290](https://github.com/mifunedev/openharness/issues/290))
### Fixed
### Removed
### Deprecated
### Security

## [2026.5.14] - 2026-05-14

### Added
- `.devcontainer/docker-compose.yml` publishes container port 1455 to the host loopback (`127.0.0.1:1455:1455`) for the **Pi harness** login flow, whose OAuth callback binds `http://localhost:1455`. With the port on the host loopback, VS Code Remote SSH's automatic port-forwarding carries the redirect back to the laptop and Pi login completes out of the box; direct-terminal (plain `ssh`) users still need to forward the port manually with `ssh -L 1455:localhost:1455 user@host`. The Codex CLI is unaffected — it has its own headless path via `codex login --device-auth`. ([#287](https://github.com/mifunedev/openharness/issues/287))

### Removed
- In-tree docker-compose overlays under `.devcontainer/` except `docker-compose.postgres.yml`: the five host-auth overlays (`claude-host.yml`, `codex-host.yml`, `opencode-host.yml`, `pi-host.yml`, `deepagents-host.yml`), the three SSH overlays (`ssh.yml`, `ssh-generate.yml`, `sshd.yml`), `git.yml`, and `cloudflared.yml`. The host-auth overlays existed to share host agent credentials when the host UID matched the sandbox's UID 1000; `.devcontainer/entrypoint.sh` now syncs the sandbox user's UID/GID to the bind-mounted repo's owner on boot (replacing the host-group-membership workaround), so files written in either direction stay readable without per-agent overlays. Agent auth still persists in named volumes (`claude-auth`, `codex-auth`, `pi-auth`, `opencode-auth`, `deepagents-auth`, `cloudflared-auth`, `gh-config`); `gh auth login` covers git over HTTPS without per-overlay setup. Postgres remains opt-in via `docker-compose.postgres.yml`. Removed companion infrastructure: `install/cloudflared-tunnel.sh`, the `/cloudflared-tunnel` skill, the `*_HOST_BIND_MOUNT` / `SANDBOX_PASSWORD` / `INSTALL_BROWSER` / `INSTALL_CLOUDFLARED` / `GIT_COMMON_DIR` / `HOST_SSH_DIR` / `HOST_*_DIR` env plumbing in `.devcontainer/entrypoint.sh`, and the host-UID overlay-stripping block in `scripts/install.sh`. Deleted doc: `docs/integrations/cloudflare.md`. The `composeOverrides[]` mechanism in `config.json` is retained so downstream packs (Pi extensions, BYO harness packs) can still register their own overlays. ([#283](https://github.com/mifunedev/openharness/issues/283))
- Orphaned `.pi/onboard-steps/slack.ts` (and the empty `.pi/onboard-steps/` directory). The wizard logic lives in `apps/oh/src/config/slack.ts` now, with the bugs from the original mifune port fixed (correct tmux session name `agent-pi`, real `pi` launch path, token-prefix validation, allowlist-ID validation, atomic env-file merge). ([#281](https://github.com/mifunedev/openharness/issues/281))
- Stale documentation pages: `docs/onboarding.md`, `docs/sandbox-lifecycle.md`, `docs/connecting.md`, `docs/operations/{provision,destroy,repair}.md`, and `docs/architecture/{overview,container-runtime,crons-and-heartbeats}.md`. These pages described flows and tooling that no longer match the harness; rather than rewrite them, the docs site is trimmed to `intro`, `quickstart`, `installation`, and the `guide` / `agents` / `integrations` sections as the smaller accurate foundation. `context/rules/repo-layout-source.md` is also removed — it pinned `docs/architecture/container-runtime.md#repo-layout` as the canonical repo-tree source; with that file gone, the rule has no anchor. Cross-references in `AGENTS.md` (which is `CLAUDE.md` via symlink), `README.md`, `docs/intro.md`, `tasks/README.md`, `apps/README.md`, `crons/README.md`, `.claude/skills/harness-context/SKILL.md`, `context/rules/directory-readme.md`, `context/rules/memory.md`, `context/README.md`, `context/rules/README.md`, `.devcontainer/.example.env`, and `apps/docs/src/pages/index.tsx` updated to point at surviving docs or removed where they no longer apply. The brief `## Project Structure` block in `AGENTS.md` plus per-directory `README.md` files now stand in for the deleted repo-layout tree. ([#279](https://github.com/mifunedev/openharness/issues/279))
- `.pi/overlays/docker-compose.slack.yml` and all references to it. The in-tree Pi extension at `.pi/extensions/slack/` reads `SLACK_APP_TOKEN` / `SLACK_BOT_TOKEN` directly from `process.env` at `session_start`, so the compose-time wiring overlay is redundant. `docs/integrations/slack.md`, `README.md`, `docs/architecture/container-runtime.md`, `docs/guide/bring-your-own-harness.md`, `.devcontainer/docker-compose.yml`, and `.devcontainer/.example.env` no longer reference the overlay; `.pi/overlays/` directory removed. Source of truth for Slack env wiring is `.pi/extensions/slack/README.md`.

### Changed
- Migrate `.pi/extensions/slack/{index,tools}.ts` from the deprecated `@mariozechner/pi-coding-agent` (and `@mariozechner/pi-agent-core`) to the upstream-recommended successors `@earendil-works/pi-coding-agent` (and `@earendil-works/pi-agent-core`). Per `npm view @mariozechner/pi-coding-agent`'s deprecation notice, the old package is no longer maintained. The other two in-tree extensions (`path-guard.ts`, `plan-mode/index.ts`) had already migrated; slack was the laggard. `.devcontainer/Dockerfile`'s `[pi-coding-agent]` mapping in the `PKG` array, `docs/agents/pi.md`'s upstream link, `docs/installation.md`'s Pi row, and `.pi/UPSTREAM.md`'s lineage label all updated to match. ([#281](https://github.com/mifunedev/openharness/issues/281))
- `docs/integrations/slack.md` § 5 leads with the new `oh config slack` wizard; the manual `set -a; source ...; tmux new-session` ritual is demoted to a `<details>` "Manual fallback" collapsible. Sections renumbered (8→7, 9→8). The wizard explicitly **starts the Slack bridge for you** at the end — kills any existing `agent-pi` tmux session, launches a fresh one running `pi` with the new env sourced, and polls `tmux capture-pane` for the `connected and listening` log line. ([#281](https://github.com/mifunedev/openharness/issues/281))
- Sidebar label `Agents` → `Harnesses` in `docs/agents/_category_.json` (and corresponding `Harnesses Overview` H1 in `docs/agents/overview.md`). The "Pick your harness" section in `docs/quickstart.md` now links each harness to its per-harness page and adopts the t3code "authenticate at least one provider before use" framing across Codex (`codex login`), OpenCode (`opencode auth login`), Claude (OAuth on first launch), Pi (env vars), DeepAgents (`~/.deepagents/.env`), and the new T3 Code (`npx t3` + pairing URL). Per-harness pages updated with matching Authentication sections. ([#281](https://github.com/mifunedev/openharness/issues/281))
- Docusaurus theme: prose links inside `.markdown` content now render in terminal green (`#15803d` light / `#4ade80` dark) with a 1px underline (2px on hover). Sidebar / navbar / TOC / footer links unchanged — their position already signals affordance. Closes the "links blend with body text" UX issue. ([#281](https://github.com/mifunedev/openharness/issues/281))
- `Makefile` `help` target now prints a "Configuration wizards" section after the auto-generated target list, surfacing `oh config slack` and `oh --help` with a note that they run inside the sandbox after `make shell`. Existing Make targets remain pure compose wrappers (verb-only naming preserved); wizards are listed separately as a different category. Decision rationale: 3-agent council (PM/Implementer/Critic) reviewed Makefile-target vs CLI vs hybrid shapes — picked CLI with help-text discoverability over `docker exec -it` indirection (which has HIGH-severity TTY fragility through CI/non-interactive contexts). ([#281](https://github.com/mifunedev/openharness/issues/281))
- Slack setup docs (`docs/integrations/slack.md`, `.pi/extensions/slack/README.md`, `.devcontainer/.example.env`) now spell out the actual working flow: add `SLACK_APP_TOKEN` / `SLACK_BOT_TOKEN` / `SLACK_ALLOW_USERS` (or `SLACK_ALLOW_CHANNELS`) to `.devcontainer/.env` in Compose `KEY=value` format, then inside the sandbox run `set -a; source /home/sandbox/harness/.devcontainer/.env; set +a` before launching `pi` so the child process inherits exported vars. `pi` itself goes in a named tmux session (`tmux new-session -d -s agent-pi 'pi 2>&1 | tee /tmp/agent-pi.log'`) per `context/rules/sandbox-processes.md`. The `set -a` requirement is documented as a temporary band-aid — `.devcontainer/.env` is dual-purpose (Compose substitution + shell source), and a future change should remove the manual `set -a` step.
- Full rewrite of `docs/integrations/slack.md` to document ID allowlist semantics, Socket Mode smoke test, the token-freshness pattern, and migration from the legacy `@ryaneggz/mifune` pack; `docs/quickstart.md` adds a "Next steps" section pointing to integration pages. References to the stale `@ryaneggz/mifune` Slack context pack removed from `README.md`, `docs/installation.md`, `docs/agents/pi.md`, and `docs/agents/overview.md`. ([#279](https://github.com/mifunedev/openharness/issues/279))

### Fixed
- Docusaurus marketing landing page hero quickstart still showed `cd ~/openharness && make shell`, leaving new users at "no such directory" after the curl-pipe install. Updated `apps/docs/src/pages/index.tsx` to `cd ~/.openharness && make shell` to match the installer rename in [#200](https://github.com/mifunedev/openharness/issues/200) / [#210](https://github.com/mifunedev/openharness/issues/210). The same rename had already landed in `README.md`, `docs/quickstart.md`, and `docs/installation.md`; the JSX template literal in the home page was the last copy still pointing at the old path.
- `pi` failing on a fresh sandbox with `Failed to load extension "/home/sandbox/harness/.pi/extensions/slack/index.ts": Cannot find module '@slack/socket-mode'`. The in-tree Slack Pi extension imports `@slack/socket-mode`, `@slack/web-api`, and `chalk`, all declared in the root `package.json` but never installed automatically — the harness is bind-mounted, so a Dockerfile-time install would be shadowed at runtime. `.devcontainer/entrypoint.sh` now runs `pnpm install --prefer-offline` at the harness root on first boot when `node_modules/` is missing, before the cron runtime starts (which itself imports `croner` from the same `package.json`). Set `SKIP_PNPM_INSTALL=1` to opt out.
- Docs landing page supported-agent copy and agent grid now include DeepAgents with a matching icon tile.
- `opencode --version` (and any `opencode` invocation) failing with `EACCES: permission denied, mkdir '/home/sandbox/.config/opencode'` on a fresh sandbox. Docker auto-creates `~/.config` as root to satisfy the `gh-config:/home/sandbox/.config/gh` named-volume mount, which then blocks the sandbox user from creating siblings like `~/.config/opencode`. The entrypoint's non-recursive parent-chown loop now also covers `~/.config` alongside the existing `~/.local` / `~/.local/share` parents, while leaving the bind-mounted `~/.config/gh` contents untouched. ([#247](https://github.com/mifunedev/openharness/issues/247))
- `opencode` and `opencode /connect` failing with `EACCES: permission denied, mkdir '/home/sandbox/.local/state'` on a fresh sandbox. Docker auto-creates `~/.local` and `~/.local/share` as root to satisfy the `opencode-host` bind-mount path; the entrypoint now non-recursively chowns those parent dirs to `sandbox:sandbox` so the user can create siblings like `.local/state`. The bind-mounted `~/.local/share/opencode` itself stays untouched. Re-applies a fix that was lost when [#241](https://github.com/mifunedev/openharness/pull/241) merged a conflicting `entrypoint.sh`. ([#245](https://github.com/mifunedev/openharness/issues/245))

### Added
- `oh config slack` — in-sandbox CLI wizard that sets up the Slack integration end-to-end. Lives at `apps/oh/` (Node TypeScript, esbuild-bundled to ~13 kb, no runtime deps); installed via `.devcontainer/Dockerfile` to `/usr/local/bin/oh` (built from `/opt/oh/`, outside the bind-mount so the binary survives the runtime overlay). Walks the user through token entry (`xapp-`/`xoxb-` prefix validation), allowlist-mode prompt (users / channels / both with `U…`/`C…` ID validation and trim), atomic `.devcontainer/.env` merge (per-key, comments preserved, `<path>.tmp.<pid>` + `renameSync` for crash safety), and `agent-pi` tmux session restart with `connected and listening` poll (15s). Pluggable via `oh config <integration>` namespace — future `oh config github`/`postgres`/`cloudflare` are one-file additions under `apps/oh/src/config/`. Replaces the orphaned `.pi/onboard-steps/slack.ts` with bugs fixed (correct tmux session name, real `pi` launch path, prefix validation, redaction). ([#281](https://github.com/mifunedev/openharness/issues/281))
- `docs/agents/t3code.md` — new harness page documenting T3 Code (Theo Browne / ping.gg), the browser-based coding agent that wraps Claude/Codex/OpenCode as backends and listens on port 3773. Page includes pairing-URL auth model, `npx t3` invocation pattern, and tmux launch convention. Added to the harnesses overview, supported-agents table, authentication bullets, and the home-page agent grid in `apps/docs/src/pages/index.tsx`. ([#281](https://github.com/mifunedev/openharness/issues/281))
- `build-essential` (gcc, g++, make, libc6-dev, dpkg-dev) in the default sandbox image (`.devcontainer/Dockerfile`) so native-deps install hooks succeed out of the box — e.g. `node-pty` falling through to `node-gyp rebuild` when no Linux prebuild is shipped, which was the root cause of the `npx t3` silent-failure investigated upstream at [pingdotgg/t3code#2621](https://github.com/pingdotgg/t3code/issues/2621). Adds ~250MB to the image; intentional trade-off — the harness optimizes for "things work out of the box" over "smallest possible image". Supersedes the 707-LOC documentation-only PR #264, which was closed without merge. ([#263](https://github.com/mifunedev/openharness/issues/263), [#271](https://github.com/mifunedev/openharness/issues/271))
- `context/rules/recursive-delegation.md`: project rule encoding the inference-time *structural* patterns from *Recursive Agent Optimization* (arXiv 2605.06639, Gandhi et al., May 2026) — bounding compute (`Max depth` / `Max children per level` / `Step budget` fields in briefings), `Context` block for context-as-parameter, structured-return schemas in `Acceptance criteria`, multi-level delegation protocol, and the orchestrator-vs-sandbox recursion boundary. The rule explicitly disclaims RAO's *training-result* benefits (depth-12 generalization, 2.5× wall-clock) — those require an RL-trained policy this harness does not have — and notes the harness `Agent` tool is stateless/synchronous, unlike the paper's `async launch_subagent` REPL primitive. `context/rules/advisor-model.md` gains a `Multi-level (recursive)` row in its Pipeline Variants table cross-linking the new rule; `.claude/skills/delegate/SKILL.md` gains a distinct-scope decomposition rule and a recursion-authorization gate (Step 5 a.1) that confirms `Max depth` / `Max children per level` / `Step budget` are all present before allowing a worker to spawn grandchildren; `.claude/protected-paths.txt` registers the new rule as load-bearing. Already-covered principles (fresh context per child, parallel-when-independent, quality-over-quantity, don't-delegate-trivial) intentionally not duplicated. ([#269](https://github.com/mifunedev/openharness/issues/269), [#270](https://github.com/mifunedev/openharness/pull/270))
- Slack integration as a Pi extension at `.pi/extensions/slack/` ([#261](https://github.com/mifunedev/openharness/issues/261)).
- Project-local Pi single-line footer extension showing cwd/branch, context usage, model/thinking state, extension statuses, token usage, and session name with width-aware truncation. Header copy now uses a plain `MIFUNE` wordmark with an `agent harness` subtitle.
- Project-local Pi config under `.pi/` standing the agent up at the harness root: OpenAI-default `settings.json` (`gpt-4o`, model-cycling list scoped to GPT/o-series), `mifune-banner.ts` extension replacing pi's startup header with a flush-left `MIFUNE` wordmark + `pi harness · github.com/ryaneggz/mifune` subtitle, `path-guard.ts` extension intercepting `tool_call` events to confirm before write/edit on sensitive paths (`.env*`, `secrets/`, `credentials.{json,yaml}`, `*.pem`, `*.key`, ssh keys) and risky bash (`rm -rf`, `sudo`, `git push --force`, `git reset --hard`, bare `> /dev/sd*|nvme*|hd*|disk*`) with case-insensitive matching and `ctx.hasUI` guards for headless/RPC mode, generic consumer-agnostic `APPEND_SYSTEM.md`, `prompts/review.md` template invokable as `/review`, and `skills` + `rules` directory symlinks to `../.claude/skills` / `../.claude/rules` matching the single-source-of-truth pattern already in `.codex/`. New canonical `.claude/skills/harness-context/SKILL.md` reachable from pi via the symlink. Test suite at `.pi/extensions/__tests__/{path-guard,mifune-banner}.test.ts` (32 + 7 cases, hand-rolled mock `ExtensionAPI`, no pi devDep); pi source verified via `gh api` for tool-name casing (lowercase) and write/edit canonical input key (`path`). `vitest.config.ts` `include` extended to `.pi/**/__tests__/**/*.test.ts`; `.github/workflows/ci-harness.yml` `paths:` filter extended (`.pi/**`, `scripts/**`, `vitest.config.ts`) and a new "Root tests" step runs `pnpm test:scripts`, also bringing the previously-orphaned `scripts/__tests__/cron-runtime.test.ts` into CI. Project Pi settings pin `npm:@tintinweb/pi-subagents@0.7.1` so PR #256 starts with Claude Code-style sub-agents available in pi. ([#255](https://github.com/mifunedev/openharness/issues/255))
- Project-local Pi plan-mode extension under `.pi/extensions/plan-mode/`, with `/plan`, `/plan approve`, `--plan`, read-only tool/bash gating, plan approval prompts, and `[DONE:n]` progress tracking.
- Directory README pattern: tracked `README.md` in `apps/`, `crons/`, `scripts/`, `tasks/`, and `.worktrees/` documenting each directory's intent, subfolder conventions, and lifecycle. New rule `.claude/rules/directory-readme.md` codifies the pattern; `.gitignore` exempts only `.worktrees/README.md` (replacing the unused `.gitkeep` exemption); `.claude/rules/git.md` § Worktrees points to the new README and documents the `.worktrees/project/<project-name>/<repo>/` slot for independent project clones; `docs/architecture/container-runtime.md` moves `.worktrees/` into the canonical tree (README tracked, contents ignored). Three session lessons appended to `context/IDENTITY.md` covering README-over-`.gitkeep`, "treat X same as Y" interpretation discipline, and semantic-subfolder enumeration.
- OpenCode as a core sandbox agent: install `opencode-ai` in the base image, persist `~/.local/share/opencode`, add the `opencode-host` overlay, support `scripts/ralph.sh --harness=opencode`, and document OpenAI OAuth as the default OpenCode provider path.
- DeepAgents CLI support as a first-class supported sandbox runtime alongside Claude Code, Codex, OpenCode, and Pi. `.devcontainer/Dockerfile` installs `deepagents-cli` via `uv tool install` into `/usr/local/bin`; `.devcontainer/docker-compose.yml` adds a `deepagents-auth` named volume at `/home/sandbox/.deepagents` so provider keys, memory, skills, and sessions persist across rebuilds; `.devcontainer/docker-compose.deepagents-host.yml` is an opt-in host-state overlay (off by default — DeepAgents stores raw provider API keys with no scope/expiry, so the blast radius is wider than the Claude/Codex host overlays); `.devcontainer/entrypoint.sh` extends its chown loop to include `.deepagents` and skips it when `DEEPAGENTS_HOST_BIND_MOUNT=1`. `scripts/ralph.sh` accepts `--harness=deepagents` (or `RALPH_HARNESS=deepagents`) and dispatches to a constrained default invocation `deepagents -y --shell-allow-list recommended -q --no-stream --max-turns "$RALPH_DEEPAGENTS_MAX_TURNS" -n "$task"` (default 25 turns); `RALPH_DEEPAGENTS_FLAGS` overrides the safety/output flag string, and `--max-turns` is always appended after so the per-iteration cap cannot be silently dropped. `--shell-allow-list all` combined with the sandbox's mounted Docker socket can affect sibling containers or host Docker — only enable for trusted tasks via the env-var override. `install/banner.sh` reports DeepAgents `[installed]` from `command -v deepagents` and `[configured]` only when `~/.deepagents/.env` or `~/.deepagents/config.toml` is non-empty (an empty mounted directory is no longer treated as authenticated). New agent doc at `docs/agents/deepagents.md`; `docs/agents/overview.md`, `README.md`, `docs/quickstart.md`, `docs/onboarding.md`, `docs/installation.md`, `docs/operations/{provision,destroy,repair}.md`, `docs/guide/{overlays,permissions,configuration}.md`, and `docs/architecture/{container-runtime,overview}.md` enumerate the new agent, volume, overlay, and Ralph harness option. Repo-local `.deepagents/` is project data and follows normal git review/ignore rules; secrets and provider keys belong only in `~/.deepagents` or ignored local files. Claude remains the default Ralph harness; existing Claude/Pi/Codex execution and fallback behavior are unchanged. ([#237](https://github.com/mifunedev/openharness/issues/237))
- `docs/agents/pi.md` stub (linking upstream to `badlogic/pi-mono`) and a Pi entry in the home-page agent grid at `apps/docs/src/pages/index.tsx`, matching Pi's restoration as a default-installed CLI. ([#233](https://github.com/mifunedev/openharness/pull/233))
- Top-level `Makefile` wrapping `docker compose -f .devcontainer/docker-compose.yml` so users type `make sandbox` / `make shell` / `make destroy` instead of the 50-character compose lines. Targets: `sandbox`, `shell`, `destroy`, `stop`, `logs`, `ps`, `restart`, `help` (default, self-documenting via awk). Uses `-include .devcontainer/.env` so `SANDBOX_NAME` flows through. No agent-CLI targets — the harness's value is sandbox isolation, agent choice (`claude` / `codex` / `pi`) happens inside `make shell`.

### Changed
- Sandbox image-name convention flipped from `<name>-sandbox` (Compose's default `<project>-<service>` tag) to `sandbox-<name>` so all harness images sort/group together in `docker images` and `docker ps`. `.devcontainer/docker-compose.yml` now sets an explicit `image: sandbox-${SANDBOX_NAME:-openharness}` on the sandbox service; container name (`${SANDBOX_NAME}`) and the postgres/devnet sibling-service names (`<name>-postgres`, `<name>-devnet`) are unchanged. Existing built images on each host are left in place — the new tag takes effect on the next `make sandbox` rebuild and old `*-sandbox` images can be `docker rmi`'d at leisure. `docs/operations/destroy.md` updated to match. ([#265](https://github.com/mifunedev/openharness/issues/265))
- Move project rules from `.claude/rules/` to `context/rules/` as the canonical, tool-agnostic location, alongside the existing `SOUL.md`, `IDENTITY.md`, `TOOLS.md`, `USER.md`. `.claude/rules` is retained as a directory symlink to `../context/rules` so Claude Code's auto-loader keeps working unchanged. The previously inert `.codex/rules` and `.pi/rules` symlinks are removed — neither CLI has a rules-loading feature. New `context/README.md` and `context/rules/README.md` per `directory-readme.md`. Canonical layout tree at `docs/architecture/container-runtime.md` and high-visibility teaching references (`AGENTS.md`, `CLAUDE.md`, `.claude/agents/rule-builder.md`) updated; ~50 scattered references in skills/agents/crons/archived PRDs left as-is — the `.claude/rules` symlink resolves them. ([#257](https://github.com/mifunedev/openharness/issues/257))
- **BREAKING**: Installer now clones to `~/.openharness/` (was `~/openharness/`); `scripts/install.sh` auto-migrates existing `~/openharness/` checkouts in place.
- **BREAKING**: In-repo `.openharness/` subdirectory removed entirely.
- **BREAKING**: `composeOverrides[]` config moved from `.openharness/config.json` to repo-root `config.json` (gitignored, seeded from tracked `config.example.json`).
- **BREAKING**: `pi-host.yml` dropped from default `composeOverrides` — Pi users add it back manually in `config.json`.
- `/cloudflared-tunnel` skill reconstructs a missing `<tunnel-id>.json` from `cloudflared tunnel token` instead of failing — fixes the case where a tunnel exists in the Cloudflare account (e.g. created on another host) but only `cert.pem` is on the running machine. Documents that token-mode runs (`--token <T>`) silently drop the local `--url` flag, leaving Cloudflare to return 530s while connections register. ([#249](https://github.com/mifunedev/openharness/issues/249))
- Quickstart collapsed from two parallel paths (installer + manual) to a single linear flow in `README.md` and `docs/quickstart.md`. Manual setup demoted to a `<details>` block. New "Configuration" section in both docs surfaces `.devcontainer/.env` as the customization surface (`make destroy && make sandbox` to re-apply).
- `install.sh` moved to `scripts/install.sh` to colocate with `cron-runtime.ts` and `ralph.sh` per the orchestrator-scripts convention. Repo-detection logic and help-text self-references updated for the new path. **Action required for the curl-pipe URL**: update the `https://oh.mifune.dev/install.sh` redirect to `https://raw.githubusercontent.com/ryaneggz/open-harness/refs/heads/main/scripts/install.sh`.
- `scripts/install.sh` smarter: auto-detects host `gh auth token` and prompts to share it with the sandbox (skips in-sandbox `gh auth login` when accepted); auto-populates `.devcontainer/.env` with host timezone and git identity; new `prompt_yn` helper for interactive Y/n prompts; idempotency guard preserves existing `.devcontainer/.env` on re-runs. Dropped the unconditional `SANDBOX_PASSWORD` prompt (only honored by the opt-in sshd overlay; defaults silently to `changeme`). Build-time message added before the cold-cache build to set ~10-min expectations. "Next steps" output restructured to lead with the `.devcontainer/.env` path so the customization surface is discoverable.
- `CLAUDE.md` Lifecycle section uses the new `make` targets (`make sandbox`, `make shell`, `make ps`, `make destroy`); deeper inspection commands (`docker exec ... tmux ls`) keep their docker-native form.
- `.devcontainer/.example.env`: top header rewritten to document the file as the single customization surface; `INSTALL_AGENT_BROWSER` default flipped from `true` to `false` (~1 GB / +3 min on first build, opt-in only when needed); `SANDBOX_PASSWORD` comment clarified to note it's only honored with the sshd overlay.
- Consolidate repository layout documentation into `docs/architecture/container-runtime.md#repo-layout`; reduce duplicate tree blocks in `AGENTS.md` and `README.md` to one-line pointers; add `.claude/rules/repo-layout-source.md` to prevent the next drift round. Remove phantom heartbeat env vars (`HEARTBEAT_ACTIVE_START`, `HEARTBEAT_ACTIVE_END`, `HEARTBEAT_AGENT`, `HEARTBEAT_INTERVAL`) from `docs/architecture/container-runtime.md` and `.devcontainer/.example.env`. Fix broken `README.md` croner link (was `.claude/specs/structure-spec-v0.7.md`, now `docs/architecture/crons-and-heartbeats.md`).

### Added
- Restore `@mariozechner/pi-coding-agent` as a default-installed CLI alongside Claude Code and Codex. `Dockerfile` `AGENTS` default goes back to `claude-code,codex,pi-coding-agent`; `.devcontainer/docker-compose.pi-host.yml` overlay restored and listed in `.openharness/config.json` `composeOverrides`; `.devcontainer/entrypoint.sh` extends its host-bind skip-chown loop with `.pi`/`PI_HOST_BIND_MOUNT`. Pi auth shares the same trust tradeoffs as the claude/codex host overlays (host UID = 1000 + pre-existing `~/.pi`). The Mom Slack bot stays out of base — install [`@ryaneggz/mifune`](https://github.com/ryaneggz/mifune) for that. `docs/installation.md`, `docs/onboarding.md`, `docs/operations/{provision,repair}.md` updated; stale `mom` shell alias dropped from `install/.zshrc` (leftover from #209).
- `"type": "module"` in root `package.json` to silence the Node `MODULE_TYPELESS_PACKAGE_JSON` warning emitted on every `cron-runtime.ts` invocation. The script already uses ESM `import` syntax; no CommonJS in `scripts/` to break.
- `## Active items` watchlist inside `crons/heartbeat.md` — session-curated section the hourly heartbeat reads each pulse to validate deferred follow-ups across session boundaries via an enumerated permitted-checks list (`gh pr view`, `gh issue view`, `gh run list`, `gh release list`, date-based reminders). Cron is read-only on the section; sessions add and remove lines. New `crons/.cron.log` (gitignored under `crons/*.log`) appended each pulse as a one-line ISO-8601 liveness signal — additive to the existing `memory/<today>/log.md` writes. `context/USER.md` gains a 3-sentence "Tracking deferred follow-ups" pointer at the `Authorization patterns` boundary. Plan at `.claude/plans/polished-spinning-dijkstra.md` survived 3 critic rounds (round 1 rejected a separate `memory/HEARTBEAT.md`, round 2 rejected `AGENTS.md` documentation, round 3 converged on the v5 shape). ([#224](https://github.com/mifunedev/openharness/issues/224))
- Memory concept: long-term `memory/MEMORY.md`, daily logs at `memory/YYYY-MM-DD/log.md`, and canonical write-side protocol in `context/rules/memory.md`; auto-loaded via CLAUDE.md session-start list.

### Removed
- `oh` CLI references and the multi-agent / N-worktree / heartbeat-daemon copy from the marketing landing page at `apps/docs/src/pages/index.tsx`; quickstart code block now uses `make shell`, hero + architecture + WHY sections rewritten to "we provide the sandbox, you choose the harness". `docs/agents/overview.md` similarly drops the "Why run multiple agents" framing and the deleted `oh harness add` invocation. ([#233](https://github.com/mifunedev/openharness/pull/233))
- Gemini hidden from the docs sidebar and sitemap via `unlisted: true` in `docs/agents/gemini.md` (Docusaurus 3); page stays reachable by direct URL for old links. The home-page agent grid swaps the Gemini tile for Pi. ([#233](https://github.com/mifunedev/openharness/pull/233))
- Stale files missed in #223: `.devcontainer/tests/cli-binaries.sh`, `.devcontainer/init-env.sh` (orphan helper — `install.sh` writes `.devcontainer/.env` directly and docker compose loads it from that folder by default), `.claude/posts/{linkedin,x}.md`, `tsconfig.base.json`, `.claude/plans/crispy-beaming-torvalds.md`. Doc tree comments in `docs/architecture/container-runtime.md` and `.openharness/README.md` updated to drop the `init-env.sh` references. ([#228](https://github.com/mifunedev/openharness/issues/228))
- Pruned four orphaned files in `.openharness/` left over from the v0.7 convergence: `agents` (broken symlink to deleted `workspace/.claude/agents/`), `agent/settings.json` (referenced non-existent `gpt-5.4` model and `lastChangelogVersion: 0.66.1`; no loader on `main`), `banner.json` (custom-banner skill deleted in `fb71365`), and `settings.json` (pointed at non-existent `./.claude/skills/`). Refreshed `.openharness/.gitignore` header and added `.openharness/README.md` documenting the remaining live file (`config.json`) and the dormant status of `Caddyfile` / `exposures.json`. The directory's load-bearing role — `composeOverrides` consumed by `install.sh` and the container build — is unchanged. ([#222](https://github.com/mifunedev/openharness/issues/222))
- Remove 0 eligible stale remote branches after critic-gated review; preserve all live remote branches because none met the >60-day deletion gate, and the backup branch still contains unique commits. ([#213](https://github.com/mifunedev/openharness/issues/213))
- Remove five stale or corrupted `.worktrees/` agent directories and stale worktree registrations, reducing the ignored worktree footprint below 100 MB. ([#213](https://github.com/mifunedev/openharness/issues/213))
- Remove the dead legacy install-side setup, entrypoint, and tmux wrapper scripts now that the Dockerfile wires `.devcontainer/entrypoint.sh` directly. ([#213](https://github.com/mifunedev/openharness/issues/213))
- Remove the empty untracked `packages/web-ui/` and `packages/slack/` husks, plus the stale Slack workspace and entrypoint references. ([#213](https://github.com/mifunedev/openharness/issues/213))
- Remove the obsolete on-disk SPEC v0.6 artifact so `.claude/specs/` only exposes canonical tracked specs. ([#213](https://github.com/mifunedev/openharness/issues/213))
- **BREAKING**: `oh harness add` deprecated; `mifune` (and any other harness pack) installs via `git clone` + manual `cp` per the pack's README for v1. The CLI-driven pack installer was deleted along with `packages/sandbox/` in US-006; its `fetchGitPack` (command-injection-prone) and `registerComposeOverlays` (path-traversal-prone) plumbing is gone with it. A redesigned, security-first standalone installer (~50 LOC) tracked for v0.8 in [#211](https://github.com/mifunedev/openharness/issues/211): URL allowlisting, signature/SHA verification, sandboxed install, command-injection-safe `spawnSync`, path-traversal-safe overlay resolution, atomic registry write. Per SPEC v0.7 §"Pack mechanism status in v0.7". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: workspace template emptied of pi-coupled scaffolding. Deleted `workspace/HEARTBEAT.md`, `workspace/IDENTITY.md`, `workspace/MEMORY.md`, `workspace/SOUL.md`, `workspace/TOOLS.md`, `workspace/USER.md` (six identity files belonging to the v0 multi-agent persona); `workspace/heartbeats/` (replaced by root `crons/` per US-002), `workspace/.ralph/`, `workspace/.codex` symlink, `workspace/.openharness` symlink, and `workspace/memory/` (instance state in template); `workspace/.claude/skills/{compress,content-gate,eval-conciseness,quality-gate,wiki-ingest,wiki-lint,wiki-query,ralph,prd,ci-status}/` (ten workspace skills); `workspace/.claude/agents/{council,critic,implementer,pm,agent-builder,command-builder,rule-builder,skill-builder}.md` (eight sub-agents); and `workspace/.claude/hooks/notify_slack.sh` (Slack hook + its references in `workspace/.claude/settings.local.json`). Rewrote `workspace/AGENTS.md` (and the `CLAUDE.md` symlink) as a generic ~22-line agent-runtime stub. Kept: `workspace/AGENTS.md`, `workspace/startup.sh`, `workspace/.claude/.example.env.claude`, `workspace/.claude/rules/{code-quality,git,token-conservation}.md`, `workspace/.claude/settings.local.json`, `workspace/.claude/screenshots/.gitkeep`. The workspace template is now agent-runtime-agnostic; identity, skills, sub-agents, and routines come from a harness pack (e.g. `@ryaneggz/mifune`), not from this directory. Per SPEC v0.7 §"Harness Packs". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: orchestrator skill set triaged to a defensible 4-skill core. Deleted `.claude/skills/{delegate,harness-audit,strategic-proposal,skill-lint,prd,ralph}/` (six skill directories that mapped to v0 multi-agent / autonomous-agent flows). Converted `.claude/skills/{provision,destroy,repair}/` to plain Markdown documentation at `docs/operations/{provision,destroy,repair}.md` (the operations are now `docker compose` invocations, not skills). Surviving orchestrator skills: `/release`, `/ci-status`, `/cloudflared-tunnel`, `/agent-browser`. Stale heartbeat/daemon/worktree-discovery docs (`docs/heartbeats/`, `docs/architecture/daemon.md`, `docs/architecture/worktrees.md`) deleted alongside; incidental `workspace/heartbeats/` references in `docs/guide/{configuration,workspace}.md` and `docs/agents/{overview,claude-code}.md` rewritten to point at the croner runtime + `crons/`. Pack-side counterparts for `cloudflared-tunnel` and `agent-browser` tracked in `ryaneggz/mifune#1` and `#2`. Per SPEC v0.7 §"Orchestrator vs workspace agent skills". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: multi-agent comparison narrative archived. Deleted the still-draft `blog/2026-04-29-worktree-per-agent.md`; moved `blog/2026-04-28-byoh.md` to `blog/archive/2026-04-28-byoh.md` with `archived: true` frontmatter and a top-of-post note that v1 framing differs. Stripped the residual "side-by-side" wording from `docs/agents/overview.md`. Per SPEC v0.7 §"oh CLI" + §"Documentation source"; the v1 ICP statement at `.claude/ICP.md` now explicitly rejects multi-agent comparison-shopping framing as out-of-scope. ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: `@openharness/sandbox` CLI removed; `docker compose` is the canonical substrate. Deleted the `packages/sandbox/` workspace package (CLI binary + harness/onboard/lib/tools source + tests), the entry from `pnpm-workspace.yaml`, the `link:cli` script from root `package.json`, the now-stale `.github/workflows/ci.yml` (sandbox-only CI), and the duplicate `.github/workflows/build.yml` (which had been pushing a drifted `ghcr.io/ryaneggz/open-harness` image alongside the canonical `ghcr.io/ryaneggz/openharness` produced by `release.yml`). The CLI build/symlink block in `.devcontainer/entrypoint.sh` that wired `/usr/local/bin/openharness` and `/usr/local/bin/oh` is gone. `release.yml` is now the sole image producer. Per SPEC v0.7 §"oh CLI". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: heartbeat daemon removed. Deleted `packages/sandbox/src/lib/heartbeat/` (~1,752 LOC), `packages/sandbox/src/cli/heartbeat-daemon.ts`, `packages/sandbox/src/cli/actions/heartbeat.ts`, `packages/sandbox/src/tools/heartbeat.ts`, the `oh heartbeat {start,stop,status}` command group, the `heartbeat-daemon` package bin, the seven `heartbeat-*.test.ts` suites, and `.claude/skills/heartbeat/`. Heartbeat env vars (`HEARTBEAT_ACTIVE_START`/`_END`/`_AGENT`) stripped from `.devcontainer/docker-compose.yml`; the legacy install-side daemon-startup blocks and the `/usr/local/bin/heartbeat-daemon` symlink in `.devcontainer/entrypoint.sh` are gone; `install/banner.sh` no longer advertises `heartbeat-daemon status`. The croner runtime (`scripts/cron-runtime.ts`, US-002/US-003/US-004) is the canonical replacement per SPEC v0.7 §"Croner runtime". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: `@mariozechner/pi-coding-agent` is no longer a dependency. The pi agent CLI and Mom Slack bot have been extracted to a separate harness pack at [`ryaneggz/mifune`](https://github.com/ryaneggz/mifune) (npm: `@ryaneggz/mifune`). Install with `oh harness add @ryaneggz/mifune`. ([#208](https://github.com/mifunedev/openharness/pull/208))

### Added
- `context/` folder at repo root — orchestrator identity layer per the gist v0.6 spec §"context/" (https://gist.github.com/ryaneggz/8f6811efa144ac94d3cdc383846134d8), adapted for single-user harness scope. Four files: `SOUL.md` (voice and disposition, ~30 lines), `IDENTITY.md` (operating principles + append-only lessons learned, ~80 lines), `TOOLS.md` (environment inventory, ~40 lines), `USER.md` (working-relationship patterns, living document, ~45 lines). `AGENTS.md` bootloader updated with a session-start read instruction; `.claude/protected-paths.txt` extended with the four new entries so future cleanup passes do not delete them. Implements the session-loaded identity layer the v0.7 convergence (PR #212, US-013) correctly removed from `workspace/` (pack territory) but never created at the orchestrator level. The `IDENTITY.md` lessons-learned section captures the meta-principles surfaced this session (revealed > stated preference, critic-gate before destructive actions, short feedback loops over speculative infrastructure). ([#220](https://github.com/mifunedev/openharness/issues/220))
- `.claude/protected-paths.txt` — list of load-bearing skills, scripts, and config paths that critics must NOT propose deleting without an explicit override note in the finding's RECOMMENDATION column. Wired into `.claude/agents/critic.md` (general adversarial review) and `.claude/skills/ship-spec/SKILL.md` stage 3 (PRD critic gate). Replaces the per-invocation usage-logging proposal (closed as #217) after Critic B's proportionality argument: zero runtime overhead, works on day 1, ~20 LOC vs. an 8-story PRD with hooks. The v0.7 convergence (PR #212, US-012) deleted six load-bearing skills under "defensible 5-skill set" rationale; this list is the gate that would have caught it. ([#218](https://github.com/mifunedev/openharness/issues/218))
- **`/ship-spec` v1.1**: stages 3 and 5 reordered — critics run BEFORE the GH issue is opened (was: issue first, then critics). Earlier ordering created dangling issues when critics halted; now no GitHub-side state changes until the spec passes the critic gate. Surfaced during the dogfood run on issue #217. ([#218](https://github.com/mifunedev/openharness/issues/218))
- `/ship-spec` orchestrator skill at `.claude/skills/ship-spec/SKILL.md`. Composes `/prd` → 2 critics (parallel, asymmetric: implementer + user lens) → critique gate → `/ralph` → `gh issue` → branch + push → `gh pr create --draft` into one durable invocation. Critics write to `tasks/<slug>/critique.md`; pipeline HALTs on any high-severity finding without mitigation. v1 stops at draft PR; loop launch + CI verification stay manual until the critic phase has been proven valuable across 2-3 real runs. Idempotent at every stage. ([#215](https://github.com/mifunedev/openharness/issues/215))
- v1 Ideal Customer Profile statement at `.claude/ICP.md`: primary persona (single working developer, one project, agent-tended), top three jobs-to-be-done, top three needs, and explicit anti-ICP (not multi-tenant SaaS, not non-developers, not production agent operators, not comparison-shopping tinkerers). ([#210](https://github.com/mifunedev/openharness/issues/210))
- Seeded the two SPEC-reserved crons: `crons/heartbeat.md` (hourly pulse, `0 * * * *`) and `crons/cleanup-tasks.md` (Sunday 23:00, `0 23 * * 0`). Bodies follow SPEC v0.7 §"Croner runtime" + §"Weekly cleanup cron"; substrate stays empty otherwise so users add their own. ([#210](https://github.com/mifunedev/openharness/issues/210))
- `.devcontainer/entrypoint.sh` now starts `scripts/cron-runtime.ts` in a `system-cron` tmux session on container boot, replacing the legacy `heartbeat-daemon` watchdog block. Idempotent (`tmux has-session` guard); restart policy is per-container-start per SPEC v0.7 §"Croner runtime". ([#210](https://github.com/mifunedev/openharness/issues/210))
- `scripts/cron-runtime.ts` — minimal croner-backed runtime (≤120 LOC) that reads `crons/*.md` frontmatter, schedules per-cron tasks, runs the body as a prompt to `claude -p` (or `$CRON_AGENT_BIN`), logs to `crons/.cron.log`, and uses a PID-lock at `crons/.pid` to prevent double-firing. Replaces the 1,900-LOC heartbeat daemon per SPEC v0.7. ([#210](https://github.com/mifunedev/openharness/issues/210))
- SPEC v0.7 amendments at `.claude/specs/structure-spec-v0.7.md` — additive-only revision resolving seven v0.6 conflicts (canonical image name, docs path, `oh` CLI deprecation, croner runtime contract, hook execution mechanism, memory directory format, tasks vs `.ralph` paths) and adding sections for harness packs, orchestrator-vs-workspace skill scope, and shrunk prose on Docusaurus config / eval flakiness / wiki-sync rubric / blog authorship / weekly cleanup. ([#210](https://github.com/mifunedev/openharness/issues/210))
- README "Docker only (no installer)" section — concise compose-based deploy path for hosts that have only Docker + git.
- Per-page OpenGraph + canonical link tags via theme.config.tsx head function ([#140](https://github.com/mifunedev/openharness/issues/140)).
- Blog post draft: Worktree-per-agent — stages for Wed publish ([#145](https://github.com/mifunedev/openharness/pull/145)).
- Blog section at /blog and About page nav entry (#143).
- Blog post: BYOH — stop installing agent CLIs on your laptop (#144).
- Root `CHANGELOG.md` and Keep-a-Changelog workflow documented in `.claude/rules/git.md`; `/release` now promotes `[Unreleased]` to the new version section at tag time.
- Generate `sitemap.xml` and `robots.txt` during docs build via `next-sitemap` ([#141](https://github.com/mifunedev/openharness/issues/141)).
- Launch runbook consolidating manual cutover steps (DNS, GH settings, OG validation, GSC, promotion).
- Docusaurus v3 docs site at `apps/docs` deployed to oh.mifune.dev ([#164](https://github.com/mifunedev/openharness/issues/164)).

### Changed
- Consolidated cron runtime log onto `crons/.cron.log`; the daemon no longer writes `crons/cron-runtime.log`. Both files were already gitignored under `crons/*.log`. ([#228](https://github.com/mifunedev/openharness/issues/228))
- Aligned the remaining flat-format memory references in tracked docs to the directory-per-day form per SPEC v0.7 §"Memory structure". Updated `docs/guide/workspace.md` (tree comment + daily-logs paragraph), `docs/guide/identity.md` (file table + memory protocol step + how-it-works paragraph), and `docs/architecture/container-runtime.md` (workspace tree comment) to use `memory/YYYY-MM-DD/log.md`. Flat `memory/<date>.md` files are deprecated. ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: `CLAUDE.md` rewritten for `docker compose`-only orchestrator instructions. The Lifecycle (Setup/Validate/Teardown), Permissions, "What You Do", "Exposing apps", and Project Structure sections no longer reference `openharness sandbox/list/shell/clean/heartbeat` or `oh expose`/`oh harness add`; commands now use `docker compose -f .devcontainer/docker-compose.yml up -d --build`, `docker compose ps`, `docker exec -it -u sandbox openharness zsh`, and `docker compose ... down -v`. Skills table trimmed to the 4-skill triaged set (`/release`, `/ci-status`, `/cloudflared-tunnel`, `/agent-browser`); provision/destroy/repair are documented in `docs/operations/`. The orphaned `packages/sandbox/` entry in the project-structure block is removed and `scripts/` + `crons/` are added. `.claude/skills/cloudflared-tunnel/SKILL.md` no longer references `oh expose` (the gateway relationship is now described via `.claude/rules/gateway-routing.md`); `.claude/skills/ci-status/SKILL.md` updated to the actual workflow name `CI: Harness`. Per SPEC v0.7 §"oh CLI" + §"Orchestrator vs workspace agent skills". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: `docs/installation.md`, `docs/onboarding.md`, `docs/sandbox-lifecycle.md`, and `docs/connecting.md` rewritten to remove `oh` CLI references. Installation is now `docker compose -f .devcontainer/docker-compose.yml up -d --build`; sandbox lifecycle uses `docker compose up/stop/down -v`; connecting uses `docker exec -it -u sandbox <name> zsh`. Onboarding is now a manual checklist run inside the sandbox (gh auth, claude, optional ssh / cloudflared / pack-supplied Slack). Deleted the entire `docs/cli/` reference tree (overview, oh-sandbox, oh-shell, oh-clean, oh-onboard) and aligned stray `oh expose` / `oh clean` mentions in `docs/integrations/{cloudflare,github,postgres}.md`, `docs/architecture/{overview,container-runtime}.md`, and `docs/guide/overlays.md`. Per SPEC v0.7 §"oh CLI". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: `README.md`, `docs/intro.md`, and `docs/quickstart.md` rewritten for single-project framing. The "run Claude, Codex, Gemini, and Pi side-by-side" tagline is gone; the harness is now framed as a Docker-based agent harness for **one project**, agent-tended over time. Quickstart leads with `docker compose -f .devcontainer/docker-compose.yml up -d --build`; all `oh` CLI references in these three files are gone (multi-agent setups now point at the [`@ryaneggz/mifune`](https://github.com/ryaneggz/mifune) harness pack). Per SPEC v0.7 §"oh CLI". ([#210](https://github.com/mifunedev/openharness/issues/210))
- **BREAKING**: `install.sh` rewritten as a docker-compose-only installer. Removed `--cli`, `--with-cli`, `--install-node` flags and the auto-detect / 3-way `choose_path` prompt; removed `detect_node`, `install_nvm`, the nvm SHA-256 pin, the `pnpm setup` + `pnpm link --global ./packages/sandbox` block, and the local-repo `packages/sandbox/package.json` detection at the repo-resolution step. The script now only checks Docker + git, clones / pulls into `~/openharness`, prepares host auth dirs (`~/.claude`, `~/.codex`, `~/.claude.json`), writes `.devcontainer/.env`, runs `docker compose -f .devcontainer/docker-compose.yml up -d --build`, and prints next-steps using `docker exec -it -u sandbox <name> zsh` and `docker compose ... down -v`. Per SPEC v0.7 §"oh CLI". ([#210](https://github.com/mifunedev/openharness/issues/210))
- `install.sh` curl-pipe target renamed `~/.openharness` → `~/openharness` so the cloned project no longer collides with the in-repo `.openharness/` config dir (`~/.openharness/.openharness/` was visually and semantically confusing). One-time migration: existing `~/.openharness` clones are `mv`'d to `~/openharness` automatically when the new path is absent. In-container paths (`/home/sandbox/.openharness`) are unchanged. ([#200](https://github.com/mifunedev/openharness/issues/200))
- Reverted the sandbox/orchestrator vocabulary realignment ([#170](https://github.com/mifunedev/openharness/issues/170), [#172](https://github.com/mifunedev/openharness/issues/172)). The in-container Linux user is `sandbox` again (UID/GID 1000 preserved); the `ORCHESTRATOR_USER`/`ORCHESTRATOR_HOME` constants are removed. The package name `@openharness/sandbox`, compose service, container default, `SANDBOX_NAME` env var, `POSTGRES_USER`, and `sandbox_*` Pi tool prefix were never changed. `install/banner.sh` carries an inverted migration guard for users coming back from a renamed image. ([#198](https://github.com/mifunedev/openharness/issues/198))
- Consolidate documentation: `/docs/` is now the single source of truth, served by Docusaurus from `apps/docs/` via `path: '../../docs'`. Enabled blog at `/blog/`. Removed the duplicate `apps/docs/docs/` tree. ([#178](https://github.com/mifunedev/openharness/pull/178))
- Installer auto-detects Node 20+; new `--cli` / `--docker-only` / `--install-node` flags; `--with-cli` deprecated. ([#176](https://github.com/mifunedev/openharness/pull/176)).
- README and installation docs now use the short `https://oh.mifune.dev/install.sh` URL (302 redirect to the raw GitHub install script on `main`) instead of the long `raw.githubusercontent.com` URL.
- `/release` now executes the `[Unreleased]` → `[$VERSION]` promotion (was prose-only) and `release.yml` sources the GitHub Release body from the promoted CHANGELOG section via `body_path` instead of `generate_release_notes`, so the GitHub Release notes match the changelog byte-for-byte.
- Revert prior secondary product name; "Open Harness" is the sole brand across README, docs, and onboarding ([#157](https://github.com/mifunedev/openharness/issues/157)).
- Cloudflare onboarding step now requires an explicit public hostname (no default domain) ([#157](https://github.com/mifunedev/openharness/issues/157)).
- Slim README to ~110 lines, lead with oh CLI flow (#139).
- Wiki promoted from `workspace/wiki/` to `docs/wiki/` — same structure (`pages/`, `sources/`, `index.md`, `log.md`), now top-level alongside human-curated docs.
- 26 docs pages converted from Nextra MDX to plain markdown rendered by GitHub.

### Fixed
- `make sandbox` and `scripts/install.sh` now expand `.openharness/config.json` `composeOverrides` into `-f` flags so the default claude/codex/pi host-auth bind-mounts actually apply on a fresh install. Previously both paths ran the base compose file only, silently falling through to named-volume auth despite the documented intent. Also pre-creates `~/.pi` alongside `~/.claude` and `~/.codex` in the install-time host-auth-dir loop, and removes residual references to the deleted `/provision` skill across docs and agent files.
- Pi icon on the home-page agent grid no longer renders dim. Restored the inline `PiIcon` React component pattern from #168 (commit `fbda6a5`) so `currentColor` inverts cleanly between eggshell light and pure-black dark themes — the previous `<img src="/img/agents/pi.svg">` wiring sandboxed the SVG document and lost the `currentColor` inheritance. `apps/docs/static/img/agents/pi.svg` reverted to `fill="currentColor"` so the file stays theme-adaptive if loaded directly elsewhere. ([#233](https://github.com/mifunedev/openharness/pull/233))
- `.claude/hooks/deny-env-dump.sh` no longer false-denies on `gh ... --body "$(cat <<'EOF' ... EOF)"` invocations whose prose contains words like `history` or phrases like `cat .env`. Heredoc bodies are now stripped via `perl -0777` before pattern matching (handles `<<DELIM`, `<<'DELIM'`, `<<"DELIM"`, `<<-DELIM`). The Tier-2 `ASK` rule for `echo|printf` of any uppercase `$VAR` was also removed — too broad on benign `echo "Branch: $BRANCH"` patterns; the deny tier above already catches secret-named variables (`TOKEN`, `SECRET`, `AUTH`, `KEY`, `GH_TOKEN`, `AWS_*`, …). `.claude/skills/ship-spec/SKILL.md` updated to note heredoc bodies are now safe (was: "prefer `--body-file` over heredoc"). (commit `3ec98fe`)
- Correct the container runtime documentation to show Dockerfile `ENTRYPOINT ["entrypoint.sh"]` invoking `.devcontainer/entrypoint.sh` directly, with `install/banner.sh` only sourced for interactive shell status. ([#213](https://github.com/mifunedev/openharness/issues/213))
- Restored six orchestrator skills removed in error during the v0.7 convergence (US-012, commit `fb71365`): `/ralph` (PRD → `prd.json` converter), `/prd` (PRD generator), `/harness-audit` (parallel-sub-agent codebase audit), `/skill-lint` (skill staleness scorer), `/delegate` (parallel execution coordinator), and `/strategic-proposal` (council-based roadmap planning). The triage rationale ("workspace-agent skills, not orchestrator") was wrong: the orchestrator actively uses the `prd.json` schema for its own task management (e.g. `tasks/openharness-v07-convergence/prd.json`), so removing the tools that produce that schema was a regression. `scripts/ralph.sh` (the runner) was kept all along — only the wrapper skills were lost. Restored from `fb71365^` unchanged; CLAUDE.md skills table updated.
- `install.sh` now auto-disables the `*-host.yml` overlays in `.openharness/config.json` when host UID ≠ 1000 (was: a multi-line warning telling the user to do it by hand). Mode-0600 credential files in `~/.claude` / `~/.codex` / `~/.pi` can't be read across UIDs, and mode-0700 dirs (e.g. `~/.pi/agent/sessions/`) reject writes from non-owner UIDs — the sandbox user (UID 1000) would EACCES on `oh` / `claude` / `codex` / `pi`. The new logic uses `jq` to filter `claude-host.yml`, `codex-host.yml`, `pi-host.yml` out of `composeOverrides` so the base named volumes take over. The original warning is retained as a `jq`-missing fallback. ([#202](https://github.com/mifunedev/openharness/issues/202))
- Host auth (`~/.claude`, `~/.codex`, `~/.pi`) is shared into the sandbox again. `install.sh` now pre-creates the three auth dirs and `~/.claude.json` (empty if missing) before sandbox bring-up so docker can't auto-create them root-owned, and `claude-host.yml` / `codex-host.yml` / `pi-host.yml` are restored to default `composeOverrides`. UID-mismatch hosts (UID ≠ 1000) get a loud install-time warning telling them to drop the overlays from `.openharness/config.json` (mode-0600 credential files can't be read across UIDs). Together this restores the convenience that was lost in #194 / #195 without re-introducing the EACCES failure mode that motivated those PRs. ([#196](https://github.com/mifunedev/openharness/issues/196))
- `oh` / `claude` / `codex` no longer fail with `EACCES` (or silent re-auth) on fresh `install.sh` installs. Removed `docker-compose.claude-host.yml`, `docker-compose.codex-host.yml`, and `docker-compose.pi-host.yml` from the default `composeOverrides` in `.openharness/config.json`. Each overlay's own header documents that it requires host UID 1000 + the source dir to pre-exist; shipping them as defaults broke any host where either condition wasn't met (docker auto-creates missing source dirs as root → sandbox user (UID 1000) can't write). The base compose's `claude-auth` / `codex-auth` / `pi-auth` named volumes now take effect by default; `entrypoint.sh` chowns them to UID 1000 on first boot. Single-laptop developers wanting cross-sandbox auth sharing can opt back in by appending the relevant overlay(s) to `.openharness/config.json`. ([#194](https://github.com/mifunedev/openharness/issues/194))
- `oh sandbox` no longer fails on fresh `install.sh` installs with a Caddyfile bind-mount error. Removed `docker-compose.gateway.yml` and `docker-compose.cloudflared.yml` from the default `composeOverrides` in `.openharness/config.json`; both are additive overlays now loaded only when needed (`oh expose` re-adds the gateway via `SandboxConfig.addOverride()`). ([#192](https://github.com/mifunedev/openharness/issues/192))
- `install.sh` next-steps now leads with a numbered "source your shell rc" step that detects `$SHELL` (`bash` → `~/.bashrc`, `zsh` → `~/.zshrc`, `fish` → `~/.config/fish/config.fish`) so users don't have to exit + reopen their shell to pick up `oh` / `node` / `pnpm`. Curl-piped install runs in a child bash whose exports cannot reach the parent shell — the source step is now Step 1 of next-steps (was a buried "Reminder").
- `install.sh` bootstraps `PNPM_HOME` before `pnpm link --global` so the CLI install no longer dies with `ERR_PNPM_NO_GLOBAL_BIN_DIR` on fresh nvm-installed Node. Runs `pnpm setup` (writes `PNPM_HOME` export to `~/.bashrc` + `~/.zshrc`) and also exports `PNPM_HOME`/`PATH` inline so the link step succeeds in the current shell regardless of rc state.
- `install.sh` no longer silently exits when sourcing a pre-existing `~/.nvm/nvm.sh` under `set -euo pipefail`. nvm + corepack calls are bracketed with relaxed strict mode and explicit `$?` checks, an `ERR` trap surfaces unexpected failures with the failing command and line, and pnpm is pinned to `10.33.0` (matches `package.json#packageManager`) instead of resolving `pnpm@latest` over the network on every install.
- Slack bot no longer drops oversized agent replies with cascading `msg_too_long` errors. Main message is capped at 2,900 chars with a `_message truncated — full response in thread_` footer; full content spills to thread replies; `setWorking(false)` always clears the working indicator. ([#135](https://github.com/mifunedev/openharness/issues/135))

### Removed
- Removed legacy `/docs/{plans,wiki,launch-runbook,blog}/` and `apps/docs/docs/` tree (consolidated into `/docs/`). ([#178](https://github.com/mifunedev/openharness/pull/178))
- Nextra docs site and the `.github/workflows/docs.yml` deployment — documentation is now plain markdown in `docs/`, read in the GitHub UI.
- Reference Next.js application at `workspace/projects/next-app/`, along with its CI jobs (`workspace/projects/next-app` paths in `ci.yml` / `release.yml`) and the release pre-flight gate referencing it.
- Root `package.json` scripts: `dev`, `docs:dev`, `docs:build`, `docs:preview`.

### Deprecated
### Security

---

Release history prior to this file: see [git tags](https://github.com/mifunedev/openharness/tags) and [GitHub Releases](https://github.com/mifunedev/openharness/releases). Most recent pre-changelog tag: `2026.4.22`.
