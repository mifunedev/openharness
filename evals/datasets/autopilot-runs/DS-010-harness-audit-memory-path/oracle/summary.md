# Oracle: DS-010 — Correct the /harness-audit long-term memory path

The trajectory fixes a stale path in the `/harness-audit` skill. The skill was tailing a repo-root `MEMORY.md` that does not exist; the tracked long-term memory file lives at `memory/MEMORY.md`. Because empty-queue autopilot research depends on `/harness-audit` to load recent long-term lessons into its context snapshot before the four auditors rank findings, the broken path silently dropped that signal from every research-mode run. The change repoints `.claude/skills/harness-audit/SKILL.md` at `memory/MEMORY.md`, restoring long-term-memory context to empty-queue autopilot selection. It also adds an eval probe (`evals/probes/harness-audit-memory-path.sh`) that fails if the skill ever points back at the nonexistent root path, records the fix in `CHANGELOG.md` under `## [Unreleased]`, refreshes `evals/RESULTS.md`, and carries the full task scaffold under `.oh/tasks/correct-harness-audit-memory-path/` (prd.md, prd.json, critique.md, prompt.md, progress.txt). Nine files, +282/-31.

Provenance — why this is an `autopilot-run` example: PR #177 carries the `autopilot` label and was loop-selected, not human-requested. The autopilot loop files its own `autopilot`-labelled ticket (#176) from a top-ranked `/harness-audit` finding when the issue queue is empty, then builds it end-to-end via `/ship-spec --issue`. That self-filed, self-built provenance is what distinguishes this from a human-requested ship-spec PR, where a person authors the ticket and the loop only executes it.

Reward: scored by `diff_similarity` — changed-file overlap of a candidate diff against `oracle/changed-files.txt` (the 9 files in merged PR #177) — plus `artifact_presence`, satisfied when PR #177 is merged and carries the `autopilot` label.
