# progress

## US-001 — 2026-06-12 14:30 UTC
- **Title**: Add workspace typecheck step to release validate job
- **Files changed**: `.github/workflows/release.yml`
- **Commit**: a864ed9
- **Result**: PASS

Added the `Typecheck (workspace packages)` step to `release.yml`'s validate job, inserting it between the `Format check` step and the `Build` step. The step runs `pnpm --filter './packages/**' -r --if-present run typecheck`, byte-identical to `ci-harness.yml` line 80-81. YAML validity was confirmed via structural checks and absence of tab indentation errors. Step order verified: Lint → Format check → Typecheck (workspace) → Typecheck (oh) → Build → Test → Root tests.

## US-002 — 2026-06-12 14:30 UTC
- **Title**: Add packages/oh standalone typecheck step
- **Files changed**: `.github/workflows/release.yml`
- **Commit**: a864ed9
- **Result**: PASS

Added the `Typecheck (oh CLI — npm standalone)` step immediately after the workspace typecheck step and before `Build`. The multi-line run block executes `npm --prefix packages/oh ci` first (install) then `npm --prefix packages/oh run typecheck`, matching ci-harness.yml lines 83-86 exactly including the em-dash in the step name. This covers the pnpm-filter-workspace-trap where `packages/oh` is silently skipped by pnpm workspace filters.

## US-003 — 2026-06-12 14:30 UTC
- **Title**: Verify step order and scope-guard parity
- **Files changed**: `.github/workflows/release.yml`
- **Commit**: a864ed9
- **Result**: PASS

Verified that `release.yml`'s validate job step order is exactly: Lint → Format check → Typecheck (workspace packages) → Typecheck (oh CLI — npm standalone) → Build → Test → Root tests (scripts + .pi extensions). Scope guard confirmed CLEAN: all 12 steps in `ci-harness.yml`'s `ci` job (lines 47-95) are present in `release.yml`'s validate job. The `boot-lint` job in ci-harness is a separate job (lines 97+) and is correctly out of scope — not added.

## US-004 — 2026-06-12 14:30 UTC
- **Title**: Record the change in CHANGELOG.md
- **Files changed**: `CHANGELOG.md`
- **Commit**: b3fd943
- **Result**: PASS

Appended one bullet to the existing `### Changed` block under `## [Unreleased]` in CHANGELOG.md (line 38), immediately before the `### Fixed` heading. The bullet describes both typecheck steps, names the commands, references the pnpm-filter-workspace-trap rationale, and links issue #53. No second `### Changed` heading was created. Confirmed with grep that the entry appears at the correct location.

STATUS: COMPLETE
