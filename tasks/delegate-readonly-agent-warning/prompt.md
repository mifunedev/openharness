# Ralph iteration — delegate-readonly-agent-warning

You are one iteration of a Ralph loop implementing the `delegate-readonly-agent-warning` task. The full plan is in `tasks/delegate-readonly-agent-warning/prd.md` and the structured task list is in `tasks/delegate-readonly-agent-warning/prd.json`. The loop calls you again until `progress.txt` contains a line `STATUS: COMPLETE`.

## Your job in one iteration

Pick **one** user story, implement it, commit, mark it `passes: true`, and append a progress entry. Then exit. The next iteration handles the next story. Do not attempt multiple stories per iteration.

## Scope reminder (harness-infra only)

This task touches exactly these files — do NOT touch anything else:
- `.claude/skills/delegate/SKILL.md` (US-001, US-002)
- `evals/probes/rl-delegation-write-worker.sh` (US-003, new file)
- `evals/RESULTS.md` (US-004, generated by the eval runner)
- `CHANGELOG.md` (US-005)

Do NOT modify `.claude/agents/*.md` (the agent definitions are protected + out of scope), `/delegate` execution logic, or any other skill. Do NOT fix the pre-existing stale `Agent: Council` row in the Key Resources table.

## Steps every iteration

1. **Read context** — in this order:
   - `tasks/delegate-readonly-agent-warning/prd.json` — find the lowest-`priority` story where `passes: false`. That is your story for this iteration.
   - `tasks/delegate-readonly-agent-warning/prd.md` — the full PRD with detailed ACs.
   - `tasks/delegate-readonly-agent-warning/progress.txt` — read the "Codebase Patterns" section at the top (if any) and the most recent few iterations.
   - `tasks/delegate-readonly-agent-warning/critique.md` — the critic findings the stories must satisfy.
   - `.claude/rules/git.md` for branch + commit conventions.
   - `evals/probes/clean-restore.sh` and `evals/README.md` for the probe convention (US-003).

2. **Verify branch** — your branch is `feat/57-delegate-readonly-agent-warning` (per `prd.json` `branchName`). If you are not on it:
   ```bash
   git fetch origin
   git checkout -b feat/57-delegate-readonly-agent-warning origin/development 2>/dev/null \
     || git checkout feat/57-delegate-readonly-agent-warning
   ```
   Never push to `development` or `main` directly.

3. **Implement the chosen story** — make the file changes specified in the story's `acceptanceCriteria`. Confine the work to that story; resist scope creep.

4. **Run quality checks** before commit (this is a docs/skill/shell change — there is no TypeScript to typecheck, but lint/format run repo-wide in CI):
   ```bash
   pnpm run lint 2>/dev/null || true
   pnpm run format:check 2>/dev/null || true
   ```
   For US-003 / US-004 also run the probe + suite directly:
   ```bash
   bash evals/probes/rl-delegation-write-worker.sh; echo "probe exit=$?"
   bash .claude/skills/eval/run.sh --probe rl-delegation-write-worker; echo "runner exit=$?"
   ```
   If checks fail, fix them. Do not commit broken code.

5. **Commit** with this message format (per `.claude/rules/git.md`):
   ```
   feat: US-<NNN> — <story title>
   ```
   Use `feat` for the warning/probe stories, `task` for the changelog/benchmark refresh. Stage only files touched by this story.

6. **Update `prd.json`** — set `passes: true` for the completed story, and set `notes` to a one-line summary including iteration number and date, e.g. `"Completed iteration 1 on 2026-06-11; commit abc1234"`.

7. **Append to `progress.txt`** — append (never replace) using this format:
   ```markdown
   ## US-<NNN> — <YYYY-MM-DD HH:MM UTC>

   **Title**: <story title>
   **Files changed**: <list>
   **Commit**: <short SHA>
   **Result**: PASS | BLOCKED | DEFERRED

   ### What I did
   <2-4 sentences>

   ### Learnings for future iterations
   <patterns, gotchas, useful context>

   ---
   ```

8. **Codebase Patterns** — if you discovered a reusable pattern, append it to the `## Codebase Patterns` section at the **top** of `progress.txt`.

9. **Stop condition** — after step 7, check whether all stories in `prd.json` now have `passes: true`. If yes, append a final line on its own to `progress.txt`:
   ```
   STATUS: COMPLETE
   ```
   This terminates the Ralph loop.

10. **End response normally** — do not emit any completion sentinel in your reply text. Only the `STATUS: COMPLETE` line in `progress.txt` matters.

## Blocked or deferred stories

- **Blocked** (external dependency, missing context, ambiguity): append with `**Result**: BLOCKED` and an explanation. Do NOT mark `passes: true`.
- **Deferred** (out of scope per Non-Goals): append with `**Result**: DEFERRED`. Do NOT mark `passes: true`.

## Critical rules

- **One story per iteration.** Resist doing two.
- **Never push** to `development` or `main`. Branch is `feat/57-delegate-readonly-agent-warning`.
- **Never skip pre-commit hooks** (`--no-verify`).
- **Never run `git clone` or `git init`** — you're already in a checkout.
- **Confine writes** to the file set named in the Scope reminder.
- **Phase ordering matters.** US-001 → US-002 → US-003 → US-004 → US-005 in `priority` order. US-003's probe checks the warning US-001/US-002 add; US-004 runs the suite after the probe exists.
- **CHANGELOG discipline.** US-005 adds the `## [Unreleased]` entry.

## Reference

- PRD: `tasks/delegate-readonly-agent-warning/prd.md`
- Structured stories: `tasks/delegate-readonly-agent-warning/prd.json`
- Branch: `feat/57-delegate-readonly-agent-warning`
- Issue: #57
