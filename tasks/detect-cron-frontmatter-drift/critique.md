# Critique — detect-cron-frontmatter-drift

Generated 2026-06-16; reviews `prd.md` post-/prd, pre-/ralph.

## Critic A — Implementer lens

CRITIC_A — IMPLEMENTER LENS
[SEVERITY: M] [STORY: US-001] [FINDING] | [EVIDENCE: AC says “extracts and fingerprints at least `schedule`, `enabled`, `agent`, `tmux`, `worktree`, and `preflight`” but the only reporting condition is “mtime is newer than `RUNTIME_START`”; no AC verifies that the fingerprint is compared to anything or that body-only mtime drift is distinguished from frontmatter drift] | [RECOMMENDATION: Define the actual oracle: either compare current frontmatter fingerprint against a persisted/runtime snapshot, or narrow wording to “mtime-based stale config warning”; add an AC for body-only edits not being reported as restart-required frontmatter drift if true fingerprinting is intended.]
[SEVERITY: H] [STORY: US-001] [FINDING] | [EVIDENCE: PRD introduction says frontmatter fields “require restart or reschedule,” while US-001 AC says they “require runtime restart/reschedule”; existing `scripts/cron-runtime.ts` has a SIGHUP reschedule handler that “re-reads crons/ and re-arms via scheduleAll() — so schedule edits and added/removed crons/*.md files take effect without restarting”] | [RECOMMENDATION: Resolve the architectural contradiction before implementation: guidance should say SIGHUP reschedule or runtime restart, not restart-only; ensure drift-check recommendation includes safe reschedule path if that is supported.]
[SEVERITY: M] [STORY: US-001] [FINDING] | [EVIDENCE: AC requires Step C-2 to fingerprint fields, but no storage dependency is specified; Non-Goals say “Do not create an always-on runtime snapshot database.”] | [RECOMMENDATION: State where the baseline fingerprint comes from. If no baseline exists, remove “fingerprint” and call it source-text extraction used for messaging/probe coverage.]
[SEVERITY: M] [STORY: US-002] [FINDING] | [EVIDENCE: AC says README-style docs, disabled crons, missing/empty schedule, and Croner-invalid schedules remain excluded, but omits runtime qualification already enforced by `loadCrons`: valid cron id, basename id match, valid agent bin] | [RECOMMENDATION: Extend the schedulable predicate AC to include id validation, basename match, and agent validation, or explicitly document why drift-check intentionally uses a weaker predicate.]
[SEVERITY: M] [STORY: US-003] [FINDING] | [EVIDENCE: AC says `wiki/cron-runtime.md` must include “line-cited claims,” but no source-line capture/update method is specified; line numbers are inherently unstable while implementation is changing] | [RECOMMENDATION: Require citations to be gathered after code edits, and verify with a deterministic grep/probe or reviewer checklist that cited paths and line numbers exist.]
[SEVERITY: M] [STORY: US-003] [FINDING] | [EVIDENCE: Story combines skill docs, new/updated architecture wiki page, DeepWiki-style citations, and generated `wiki/README.md` refresh] | [RECOMMENDATION: Split into two stories: operator skill wording, and wiki/index update. This is 2+ deliverables with different verification surfaces.]
[SEVERITY: L] [STORY: US-004] [FINDING] | [EVIDENCE: AC says the eval probe “extracts the drift-check Step C-2 block” and asserts source text includes field names; this can pass even if runtime behavior still only does mtime-only reporting] | [RECOMMENDATION: Add a behavioral fixture test/probe that creates a schedulable cron with non-schedule frontmatter drift after runtime start and asserts the emitted `DRIFT-CHECK (C)` wording names restart-required config.]
[SEVERITY: L] [STORY: US-004] [FINDING] | [EVIDENCE: AC requires `bash .claude/skills/eval/run.sh` exits 0 with no new green→red regression; existing eval contract tracks delta in `evals/RESULTS.md`, so local prior state can affect result] | [RECOMMENDATION: Specify whether implementation must preserve/update `evals/RESULTS.md` in the PR or run in a clean baseline; otherwise this AC may be environment-dependent.]
[SEVERITY: L] [STORY: *] [FINDING] | [EVIDENCE: Protected paths include `drift-check` and `scripts/cron-runtime.ts`; PRD edits `.claude/skills/drift-check/SKILL.md` and explicitly says not to change `scripts/cron-runtime.ts`, with no deletion/deprecation proposed] | [RECOMMENDATION: No protected-path blocker, but keep implementation edit-only for `drift-check`; do not delete/deprecate protected entries without explicit override note.]

## Critic B — User lens

CRITIC_B — USER LENS
[SEVERITY: H] [STORY: US-001/US-003] [FINDING] [PROTECTED-PATH] PRD requires editing protected `drift-check` skill without an override note. | [EVIDENCE: `.claude/protected-paths.txt` lists `drift-check`; PRD US-001/US-003 require `.claude/skills/drift-check/SKILL.md` changes] | [RECOMMENDATION] Add an explicit protected-path override note explaining why this protected skill must be modified and confirming no deletion/deprecation is proposed.
[SEVERITY: M] [STORY: US-001] [FINDING] “Detect frontmatter drift” overpromises precision: acceptance criteria still trigger on file mtime newer than runtime start, so body-only edits may be reported as possible frontmatter/config drift. | [EVIDENCE: PRD Goals say “Detect restart-required cron frontmatter drift”; US-001 says “file mtime is newer than `RUNTIME_START` emits” drift line] | [RECOMMENDATION] Clarify Non-Goals: this does not prove frontmatter changed unless a runtime snapshot/diff exists; it conservatively warns that restart-required config may be stale when a schedulable cron changed after runtime start.
[SEVERITY: M] [STORY: US-004] [FINDING] Eval probe is scoped to source-text assertions, not behavior, so users may expect real regression protection but only get wording/field-presence checks. | [EVIDENCE: US-004 requires probe “extracts the drift-check Step C-2 block” and verifies source text includes field names] | [RECOMMENDATION] State this is a contract/source-shape probe, not an end-to-end drift simulation; add behavioral fixture coverage only if implementation can do it without runtime mutation.
[SEVERITY: L] [STORY: US-002] [FINDING] Non-Goals do not explicitly exclude cron schema/frontmatter normalization changes, which could balloon implementation beyond single-project bugfix scope. | [EVIDENCE: USER.md says “Don’t pre-build for hypothetical needs” and “Don’t add features beyond what was asked”; PRD Non-Goals omit schema/contract changes] | [RECOMMENDATION] Add Non-Goal: do not introduce new cron frontmatter fields, validation schema, or migration/normalization layer.

## Synthesis

- **High-severity findings**: 2, both mitigated in `prd.md` before implementation.
- **Medium-severity findings**: 6.
- **Recommendation**: PROCEED.

Mitigations applied: `prd.md` now includes a Protected-Path Override Note limiting `.claude/skills/drift-check/SKILL.md` work to targeted edit-only changes and explicitly keeping `scripts/cron-runtime.ts` read-only; restart wording now says “SIGHUP reschedule or runtime restart”; “fingerprint” overclaim was narrowed to a conservative source-field extraction and stale-config warning; non-goals now exclude new cron fields/schema/migration layers; probe scope is documented as a deterministic source-shape contract rather than a live runtime simulation.
