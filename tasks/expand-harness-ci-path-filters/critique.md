# Critique — expand-harness-ci-path-filters

Generated 2026-06-14; reviews `prd.md` post-/prd, pre-/ralph.

## Critic A — Implementer lens

CRITIC_A — IMPLEMENTER LENS
[SEVERITY: H] [STORY: US-002] [Probe AC is semantically too weak; a naive file-wide grep can PASS with the literals in the wrong place, or false-fail on YAML formatting-only edits.] | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` US-002 AC: "checks `.github/workflows/ci-harness.yml` for `Makefile` and `harness.yaml` in the workflow path filters"; US-001 separately requires both `push.paths` and `pull_request.paths`] | [RECOMMENDATION: Require the probe to assert both literals exist under both `on.push.paths` and `on.pull_request.paths`, with quote/indent-agnostic matching rather than raw string presence anywhere in the file.]
[SEVERITY: M] [STORY: US-002] [The "register the probe in the eval runner" AC invites unnecessary edits to load-bearing eval surfaces even though the runner already auto-discovers probes.] | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` US-002 AC: "Register the probe in the eval runner if the runner requires an explicit manifest"; `.claude/skills/eval/run.sh` discovers `"$PROBES_DIR"/*.sh`; `.claude/protected-paths.txt` lists `eval` as protected] | [RECOMMENDATION: Replace this with an explicit no-runner-change guard: do not modify `.claude/skills/eval/**` unless a real manifest is found.]
[SEVERITY: M] [STORY: US-002] [The PRD relies on "existing eval conventions" without spelling out the minimum probe contract, so an implementer can ship a non-executable or metadata-free probe and still claim success.] | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` US-002 AC: "The probe follows existing eval conventions"; `evals/README.md` requires an executable `evals/probes/<id>.sh`, `# tier:`/`# source:`/`# desc:` headers, and `${BASH_SOURCE[0]}` repo-root resolution] | [RECOMMENDATION: Add explicit ACs for `chmod +x`, required headers, `${BASH_SOURCE[0]}` root resolution, and one-line stderr reasons for PASS/REGRESSION/SKIPPED.]
[SEVERITY: M] [STORY: *] [Verification ACs are vague; "Typecheck passes", "Tests pass", and "Run the new probe successfully" do not identify the commands that constitute done.] | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` US-001/US-002 AC text; `package.json` has `pnpm typecheck`; `.github/workflows/ci-harness.yml` runs separate workspace-package and `packages/oh` typecheck steps] | [RECOMMENDATION: Pin exact signoff commands, or explicitly reference the CI-equivalent commands from `.github/workflows/ci-harness.yml`.]
[SEVERITY: L] [STORY: US-001] [Protected-path handling is implicit; `Makefile` is load-bearing and must not be renamed, moved, or deleted while satisfying this story.] | [EVIDENCE: `.claude/protected-paths.txt` includes `Makefile`; `tasks/expand-harness-ci-path-filters/prd.md` FR-4 only says runtime behavior must not change] | [RECOMMENDATION: Add an additive-only guard: no rename/move/delete of `Makefile` or `harness.yaml`; only append CI path-filter entries and add the probe.]

## Critic B — User lens

CRITIC_B — USER LENS
[SEVERITY: M] [STORY: US-002] The PRD invents a maybe-manifest step even though the current eval runner auto-discovers `evals/probes/*.sh`, which invites needless runner churn and scope creep. | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` US-002 acceptance criteria; `.claude/skills/eval/run.sh`] | [RECOMMENDATION: Delete the “register the probe if the runner requires an explicit manifest” clause and state explicitly that no eval-runner changes are expected for this ticket.]
[SEVERITY: M] [STORY: US-002] The probe requirement is underspecified, so an implementer can over-engineer a YAML-aware parser when the repo already uses cheap string-presence CI self-guards. | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` US-002; `evals/probes/eval-ci-gate.sh`] | [RECOMMENDATION: Specify the intended guard style up front: a lightweight string-presence probe, repo-root resolved, matching existing `eval-ci-gate` conventions.]
[SEVERITY: M] [STORY: *] “Core sandbox configuration files” is broader than the actual requested scope, so users will reasonably expect other root entrypoints to be covered too. | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` Introduction/Overview and Goals vs. stories naming only `Makefile` and `harness.yaml`] | [RECOMMENDATION: Narrow the language to “for this ticket, only `Makefile` and `harness.yaml`” or enumerate why other root config files are intentionally excluded.]
[SEVERITY: H] [STORY: US-002] The PRD adds a new CI gate without documenting the rollback path, even though the eval system explicitly has no merge-bypass label and false failures can strand a PR. | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` has no rollback/escape-hatch section; `evals/README.md` “Escape hatch”] | [RECOMMENDATION: Add an operator note defining the allowed unblock path: rerun if transient, otherwise push a follow-up fix or temporary SKIP/revert commit.]
[SEVERITY: M] [STORY: US-001] `Makefile` is a protected path, but the PRD never says this is a coverage-only change on that asset, leaving room for “while I’m here” edits that violate protected-path expectations. | [EVIDENCE: `.claude/protected-paths.txt` lists `Makefile`; `tasks/expand-harness-ci-path-filters/prd.md` Goals/FR-4] | [RECOMMENDATION: Add an explicit guardrail: this ticket may reference `Makefile` in CI filters but must not rename, move, delete, or change `Makefile` behavior.]
[SEVERITY: L] [STORY: *] Repeating “Typecheck passes” inside both stories is user-misaligned; the request is about trigger coverage, and duplicated repo-wide verification language encourages unrelated churn. | [EVIDENCE: `tasks/expand-harness-ci-path-filters/prd.md` US-001 and US-002 acceptance criteria; `context/USER.md` “Don’t add features beyond what was asked.”] | [RECOMMENDATION: Collapse this into one global verification statement such as “existing CI remains green” instead of per-story typecheck requirements.]

## Synthesis

- **High-severity findings**: 2
- **Medium-severity findings**: 7
- **Low-severity findings**: 2
- **Recommendation**: PROCEED

High findings were AC-tightening concerns, not architectural halts. They are mitigated in `prd.md` by requiring the probe to verify both `push.paths` and `pull_request.paths`, adding explicit eval-probe metadata/executable/root-resolution conventions, removing eval-runner edits, adding protected-path guardrails, narrowing scope to `Makefile` and `harness.yaml`, and documenting the false-positive rollback/escape hatch.
