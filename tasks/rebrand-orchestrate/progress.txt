# progress

## Codebase Patterns

- Protected skill edits are allowed only when the PRD carries an explicit override; keep them to the named non-destructive command-reference replacements and avoid restructuring the protected skill.
- After a `git mv` directory rename, re-stage the destination file after content edits before committing; otherwise the commit can capture only the pure rename and leave identity edits unstaged.
- Eval contract probes deliberately pin non-command contract literals; during skill rebrands, update paths/messages/frontmatter assertions while leaving the literal array unchanged unless the story says otherwise.
- For rebrand metadata-only stories, keep `evals/RESULTS.md` edits narrowly scoped to the renamed probe row; do not regenerate or restamp unrelated rows unless the story runs the whole eval suite.

## US-001 — 2026-06-16 04:50 UTC

**Title**: Rename the runner skill identity
**Files changed**: `.claude/skills/orchestrate/SKILL.md`, `tasks/rebrand-orchestrate/prd.json`, `tasks/rebrand-orchestrate/progress.txt`
**Commit**: 682c516
**Result**: PASS

### What I did
Moved the runner skill directory from `.claude/skills/loop-runner/` to `.claude/skills/orchestrate/` using `git mv`. Updated the skill frontmatter name, trigger text, H1, memory-log heading, and all command-name references in the skill body from `/loop-runner` to `/orchestrate` while preserving the loop routing contract and `context/rules/loop.md` references. Verified the old skill path is gone and the renamed skill no longer contains `loop-runner` text.

### Learnings for future iterations
The worktree has `pnpm run typecheck`, not `pnpm run type-check`; the exact Ralph check reports the missing script but the repository typecheck passes via `pnpm run typecheck`. For rename stories, inspect `git status` after the first commit attempt because a partially staged rename can leave content edits unstaged.

---

## US-002 — 2026-06-16 04:54 UTC

**Title**: Update sibling runner references
**Files changed**: `.claude/skills/strategic-proposal/SKILL.md`, `.claude/skills/benchmark/SKILL.md`, `context/rules/loop.md`, `tasks/rebrand-orchestrate/prd.json`, `tasks/rebrand-orchestrate/progress.txt`
**Commit**: a3db582
**Result**: PASS

### What I did
Updated the remaining sibling runner references in `/strategic-proposal`, `/benchmark`, and the `context/rules/loop.md` runner row from `/loop-runner` to `/orchestrate`. Kept `--loop-candidate-only`, `LOOP MODE: candidate-only`, `context/rules/loop.md`, The Loop, and loop node names unchanged, and limited the protected `strategic-proposal` edit to non-destructive command-reference replacements.

### Learnings for future iterations
The protected-path override for `strategic-proposal` is already encoded in the PRD AC; no separate protected-path file edit is needed. `rg -n "/loop-runner|loop-runner"` scoped to the three US-002 files is the fastest sanity check before moving on.

---

## US-003 — 2026-06-16 04:58 UTC

**Title**: Rename and update eval probes
**Files changed**: `evals/probes/orchestrate-contract.sh`, `evals/probes/loop-repeat-gate.sh`, `tasks/rebrand-orchestrate/prd.json`, `tasks/rebrand-orchestrate/progress.txt`
**Commit**: ff00559
**Result**: PASS

### What I did
Renamed the loop runner contract probe to `evals/probes/orchestrate-contract.sh` with `git mv` semantics and updated its skill path, frontmatter assertion, comments, and PASS/SKIPPED/REGRESSION output for `/orchestrate`. Updated `evals/probes/loop-repeat-gate.sh` to read `.claude/skills/orchestrate/SKILL.md` and report `/orchestrate` runner messages while preserving the repeat gate checks. Verified both targeted probes, typecheck, lint, and the full test suite.

### Learnings for future iterations
Use `python3` or direct edits in this environment; `python` is not installed on PATH. For eval-probe rebrands, keep the fixed-string `LITERALS` list stable unless the acceptance criteria explicitly asks to alter a contract literal.

---
## US-004 — 2026-06-16 05:01 UTC

**Title**: Update capability and eval result metadata
**Files changed**: `evals/capability/tasks/CB-002-walk-the-loop.md`, `evals/capability/RESULTS.md`, `evals/RESULTS.md`, `tasks/rebrand-orchestrate/prd.json`, `tasks/rebrand-orchestrate/progress.txt`
**Commit**: 732c21e
**Result**: PASS

### What I did
Updated the CB-002 capability task metadata, success signal, rubric, evidence basis, and scoring method from `/loop-runner` to `/orchestrate`. Updated the capability scoreboard basis and the eval scoreboard row/source so the current benchmark surfaces point at `orchestrate-contract`. Verified the target metadata no longer contains `loop-runner`, the renamed contract probe passes, and the repository typecheck/lint/test checks pass.

### Learnings for future iterations
`evals/RESULTS.md` can be edited narrowly for a rebrand metadata row when the story is not regenerating the suite; keep unrelated timestamps/statuses untouched so the diff remains auditable.

---
