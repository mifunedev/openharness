STATUS: COMPLETE

Implemented release validate CI parity:
- Added release.yml boot-lint job matching ci-harness shellcheck + SHA-pinned hadolint coverage, with contents: read permissions.
- Added release.yml eval-probes job running bash .claude/skills/eval/run.sh, with contents: read permissions.
- Gated release job on [validate, boot-lint, eval-probes].
- Updated context/rules/git.md and CHANGELOG.md for #111.

Verification:
- Structural grep checks for boot-lint, eval-probes, hadolint config, eval run command, and release needs.
- Confirmed ci-harness.yml and .claude/skills/eval/run.sh unchanged.
- pnpm test:scripts passed.
