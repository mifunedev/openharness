# progress

## Codebase Patterns

(Seeded from the 2-critic gate — read before US-001/US-002. These are the mitigations the stories MUST honor.)

- **Five-file scope, no exceptions.** This PR changes EXACTLY: `AGENTS.md` (narrative "The Loop" → "## The Workflow" section only), `context/rules/loop.md` (top banner + its own stale "The Loop" cross-refs), `evals/probes/workflow-boundaries.sh` (new), `evals/RESULTS.md` (one row), `CHANGELOG.md` (one line). Nothing else.
- **Protected paths — DO NOT TOUCH this PR.** `crons/cleanup-tasks.md` and `scripts/cron-runtime.ts` are on `.claude/protected-paths.txt`. Their deletion/edit is deferred to backlog Issue B behind an override note. US-004 only FILES issues (text-only) — it deletes/edits nothing.
- **Do NOT edit the AGENTS.md Skills table.** The `/autopilot` row literals are pinned by `evals/probes/autopilot-executor-toggle.sh` and `watchdog-draft-prs.sh`. Editing them flips those probes REGRESSION. Replace only the narrative loop section.
- **loop.md probe scoping.** `loop-handoff-consistency.sh` / `loop-benchmark-gate.sh` / `loop-repeat-gate.sh` scope sections via `awk '/^## 2\\./{f=1} /^## 3\\./{f=0}'`. The US-002 banner MUST be a blockquote after the H1 and before the first `---`, with NO `## ` heading inside it. Do not change §2/§7 node tables or STATUS tokens.
- **loop.md self-references to reconcile (US-002).** loop.md's intro (~line 8-10) and See-Also (~line 167) say `AGENTS.md "The Loop"`. After the AGENTS.md rename they are stale → update to `AGENTS.md § Workflow`, and rephrase the "this file leads" authority claim to be coherent (loop.md owns the executable node tree; AGENTS.md § Workflow owns the operative path — different layers).
- **Probe anchors (US-003), not prose.** `workflow-boundaries.sh` scopes to the `## The Workflow` section and requires the `<!-- workflow-canonical -->` comment + operative-path marker (`select`…`build`…`merge`) + `designated sole runner` + a `loop.md` pointer. 3-state: 0 PASS / 2 SKIPPED (AGENTS.md absent) / 1 REGRESSION. So US-001 MUST place the `## The Workflow` heading + the `<!-- workflow-canonical -->` comment.
- **Honest single-runner language.** AGENTS.md must say "autopilot is the designated sole runner; /orchestrate is being retired via Issue A and still runs until then" — NOT that orchestrate is already gone (it still runs this PR).
- **Real call boundary, not "planning".** autopilot owns issue selection + pm decompose, then calls `/ship-spec --issue`; ship-spec owns the build from `/prd` onward. PRD generation lives in ship-spec, not autopilot.
- **Conciseness gate does NOT cover root AGENTS.md** (`.github/workflows/conciseness.yml` path-filters to `workspace/*.md`). No CI filler check runs here — self-impose <~200 words for the new section.
- **RESULTS.md** — hand-insert one alphabetical row only; no wholesale regen (the eval-results-new-probe-row rule).

