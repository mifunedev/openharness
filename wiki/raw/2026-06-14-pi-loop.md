# Raw snapshot: pi-loop

Source package page: https://pi.dev/packages/@trevonistrevon/pi-loop?name=monitor
Source README/package metadata: npm package `@trevonistrevon/pi-loop@0.5.5`
Fetched: 2026-06-14

## pi.dev package page

- Package: `@trevonistrevon/pi-loop`
- Version: `0.5.5`
- Published: Jun 10, 2026
- Author: trevonistrevon
- License: MIT
- Types: extension
- Description: "A pi extension for cron/event-based agent re-wake loops and background process monitoring."
- Install: `pi install npm:@trevonistrevon/pi-loop`
- Repo: https://github.com/trvon/pi-loop
- Home: https://github.com/trvon/pi-loop#readme
- Pi manifest JSON:

```json
{
  "extensions": [
    "./src/index.ts"
  ]
}
```

## package metadata

```json
{
  "name": "@trevonistrevon/pi-loop",
  "version": "0.5.5",
  "description": "A pi extension for cron/event-based agent re-wake loops and background process monitoring.",
  "keywords": ["pi-package", "pi", "pi-extension", "loop", "cron", "monitor", "scheduler"],
  "homepage": "https://github.com/trvon/pi-loop#readme",
  "repository": "git+https://github.com/trvon/pi-loop.git",
  "license": "MIT",
  "author": "trevonistrevon",
  "dependencies": {"typebox": "^1.1.34"},
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.75.5",
    "@earendil-works/pi-tui": "^0.75.5"
  },
  "pi": {"extensions": ["./src/index.ts"]}
}
```

## README excerpt

`@trevonistrevon/pi-loop` provides cron and event loops for the pi coding agent, background monitors, scheduled re-wakes, `pi-tasks` integration, and native task fallback.

Install:

```bash
pi install npm:@trevonistrevon/pi-loop
```

Quick start:

```text
LoopCreate trigger="5m" prompt="Check if the build passed"
LoopCreate trigger="tool_execution_start" prompt="Log the tool being used" triggerType="event"
LoopList
LoopDelete id="1"
```

```text
MonitorCreate command="tail -n0 -f build.log" description="Watch build"
MonitorCreate command="python train.py" onDone="Analyze results and report best loss"
MonitorList
MonitorStop monitorId="1"
```

When `pi-tasks` is not installed, `pi-loop` also exposes native task tools after startup detection:

```text
TaskCreate subject="Fix deploy polling" description="Switch deploy check to event-driven loop"
TaskList
TaskUpdate id="1" status="in_progress"
TaskDelete id="1"
```

Commands:

- `/loop [interval] [prompt]` — interactive loop creation.
- `/tasks` — interactive native task viewer/manager, only registered when `pi-tasks` is absent.

Tools:

| Tool | What it does |
|---|---|
| `LoopCreate` | Schedule a prompt on a cron timer, a pi event, or both with debounce |
| `LoopList` | Show active loops with IDs, triggers, and next-fire times |
| `LoopDelete` | Delete or pause a loop |
| `MonitorCreate` | Run a background command, stream output as `monitor:output` events. Use `onDone` for auto-notify on completion |
| `MonitorList` | Show monitors with status, uptime, and output line count |
| `MonitorStop` | Stop a monitor (SIGTERM → 5s → SIGKILL) |
| `TaskCreate` | Create a native fallback task when `pi-tasks` is absent |
| `TaskList` | List native fallback tasks |
| `TaskUpdate` | Update native fallback task status/details |
| `TaskDelete` | Delete native fallback task |

Trigger types: `cron` (`5m`, `1h`, `0 9 * * 1-5`), `event` (any pi event source), or `hybrid` (both, debounced).

With `pi-tasks`, pass `autoTask: true` on `LoopCreate` and each loop fire auto-creates a tracked task. Detection happens over pi's event bus.

Without `pi-tasks`, the package registers a native fallback task system for the session: session- or project-scoped task files under `.pi/tasks/` depending on `PI_LOOP_SCOPE`, native task tools, `/tasks`, and compact status-line task tracking.

Status line examples:

```text
1 loop · 1 monitor
2 tasks | active: Fix deploy polling
1 loop · 2 monitors · 3 tasks | next: Update README
```

Configuration:

| Variable | Effect | Default |
|---|---|---|
| `PI_LOOP` | Store path override. `off` to disable, absolute or project-relative path | unset → derived from `PI_LOOP_SCOPE` |
| `PI_LOOP_SCOPE` | `memory` (ephemeral), `session` (per-session file), `project` (shared) | `session` |
| `PI_LOOP_DEBUG` | Debug logging to stderr | unset |

In `session` scope (default), loop and task files are saved per session ID (for example, `.pi/loops/loops-<sessionId>.json` and `.pi/tasks/tasks-<sessionId>.json`) so concurrent sessions and worktree agents do not share state. In `memory` scope nothing persists to disk. `project` should be opt-in for intentionally shared automation.

Limits: 25 active loops, 25 running monitors. Recurring loops expire after 7 days.
