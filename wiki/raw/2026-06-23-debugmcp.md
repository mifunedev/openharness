# DebugMCP integration source snapshot — 2026-06-23

This snapshot records the source evidence for the `debugmcp` wiki entry.
Tool surface and feasibility verdicts are anchored to DebugMCP **v2.0.1**.

## Identity

- Project: [DebugMCP](https://github.com/microsoft/DebugMCP) — MIT-licensed VS Code extension
- Extension id: `ozzafar.debugmcpextension`
- Version anchor: **v2.0.1**
- Endpoint: `http://localhost:3001/mcp` (Streamable HTTP)
- `extensionKind`: `workspace` (activates in the remote/workspace extension host, not a UI/browser host)

## Tool schema (v2.0.1) — 13 tools

| Tool | Key params | Purpose |
| --- | --- | --- |
| `start_debugging` | program/target, config name | Launch a debug session against a target |
| `stop_debugging` | — | Terminate the active debug session |
| `restart_debugging` | — | Restart the active debug session |
| `step_over` | — | Step over the current line |
| `step_into` | — | Step into the call at the current line |
| `step_out` | — | Step out of the current frame |
| `continue_execution` | — | Resume until the next breakpoint/end |
| `add_breakpoint` | file, line | Set a breakpoint at file:line |
| `remove_breakpoint` | file, line | Remove a breakpoint at file:line |
| `clear_all_breakpoints` | — | Remove all breakpoints |
| `list_breakpoints` | — | Enumerate current breakpoints |
| `get_variables_values` | scope/frame | Read locals/variables at the current pause |
| `evaluate_expression` | expression | Evaluate an arbitrary expression in the debuggee's context |

## Feasibility evidence (local source paths)

- Devcontainer base: `FROM debian:bookworm-slim` (`.devcontainer/Dockerfile:1`) — no VS Code server binary installed.
- Only `code` tokens in the Dockerfile are the `claude-code` npm package (`.devcontainer/Dockerfile:102,108`) and the two VS Code Attach-to-Container comments (`.devcontainer/Dockerfile:197,200`).
- `.devcontainer/devcontainer.json` declares no `extensions` list and no `forwardPorts`.
- Attach-to-Container prep: `.devcontainer/Dockerfile:197-201` writes `/.devcontainer/devcontainer.json` and a `devcontainer.metadata` LABEL.
- `CLAUDE.md` Lifecycle Option B (Attach-to-Container) and Option C (Remote-SSH + Attach) supply the VS Code server binary from the operator's host IDE.

### Feasibility verdicts

| Path | Tier | Verdict |
| --- | --- | --- |
| `code serve-web` | container-side (headless) | BLOCKED — no `code`/server binary in the image |
| code-server (apt/binary, Open VSX) | container-side (headless) | UNVERIFIED — installable headlessly but no runtime install + Open VSX availability check done |
| VS Code Attach-to-Container (Option B) | operator-side | VIABLE — host IDE provisions the server |
| Remote-SSH + Attach (Option C) | operator-side | VIABLE — host IDE provisions the server over SSH |

## Security

- `evaluate_expression` runs arbitrary expressions in the debuggee's context (full read/write reach).
- `debugmcp.bindHost` must stay loopback; a non-loopback bind publishes an unauthenticated code-exec endpoint. No auth layer to add.
- Host/Origin validation is a DNS-rebinding browser defense, not a process boundary — any local process that can open `:3001` has full tool access.

## Agent MCP registration

- Codex: `.codex/config.toml` `[mcp_servers.debugmcp]` `url = "http://localhost:3001/mcp"` (or `codex mcp add debugmcp --url ...`).
- Claude Code: project-local `.mcp.json` `mcpServers.debugmcp` `{ "type": "http", "url": "http://localhost:3001/mcp" }` (or `claude mcp add --transport http ...`). File is NOT created by this integration.
- Pi / Hermes: unverified — MCP support not confirmed; no snippet provided.

## Source

- `docs/integrations/debugmcp.md` (integration contract + feasibility verdicts), this repo.
- DebugMCP repository: https://github.com/microsoft/DebugMCP
